# 漫画阅读器开发文档

> 文档性质：持续迭代的开发文档。后续所有产品决策、技术取舍、数据结构变更、阶段任务都优先记录在本文档中。

## 0. 2026-06-21 书库分页与 Tag 分组颜色修复

- 书库瀑布流改为 UI 层分页：筛选、搜索、排序、Tag 条件仍先得到完整结果，再只渲染当前页，降低大书库一次性布局、绑定和封面加载压力。
- 每页数量提供 `25 / 50 / 100`，默认 `50`，保存到 `app_settings` 的 `library.page_size`。筛选/搜索/排序变化回到第一页，翻页按钮只切换当前缓存结果，不重新扫描书库。
- 批量选择语义固定为当前页范围：`全选`、Shift 范围选择和框选都只作用于当前页可见列表，避免跨页误批量修改。
- Tag 颜色模型固定为分组统一颜色：编辑任意 Tag 的颜色会同步同组 Tag，不再把 Tag 名误写成新分组名。
- 对历史污染数据做轻量归一：如果 `managed_tags.category` 等于 Tag 名，或命中另一个 Tag 名但不是合法预设分组，显示与再次保存时归为 `自定义`。
- 修复 `ReaderWindow.xaml` 和 `LibraryDatabase.cs` 文件头重复 BOM，恢复 `dotnet build` 可编译。

## 1. 项目定位

本项目是一个专门用于 Windows 的本地漫画书库管理与阅读软件。

核心方向：

- 离线优先：不依赖联网，不做在线漫画源、不做云同步、不抓取远程元数据。
- 文件夹优先：不支持 zip / cbz / rar / cbr 等压缩包，漫画以本地文件夹内的图片序列为基本单位。
- 管理优先：重点是作者、标签、封面、阅读进度、筛选与本地书库整理。
- 阅读舒适：阅读器应支持键盘操作、沉浸模式、单页/双页/连续滚动等常用模式。
- 视觉气质：整体 UI 参考苹果图书，强调干净、卡片式、柔和、少干扰。
- 双语言显示：软件界面至少支持中文与英文显示，可在设置中切换。

## 2. 已确认需求

### 2.1 硬约束

- 目标平台：Windows。
- 最终交付形态：Windows `.exe`，优先支持安装包，后续可追加绿色便携版。
- 项目目录：`C:\Users\Admin\Desktop\MySelf\Manga`。
- 不支持 zip / cbz / rar / cbr。
- 不需要联网功能。
- 支持双语言显示。
- 支持软件内快捷键设置。
- 支持作者分类与筛选。
- 支持自定义标签。
- 支持把第一页或指定页设置为封面。

### 2.2 推荐 MVP

第一版只做最核心闭环：

- 扫描一个本地书库根目录。
- 每个漫画文件夹识别为一本书。
- 读取文件夹内图片并按文件名自然排序。
- 默认使用第一页作为封面。
- 支持手动指定封面页。
- 支持编辑标题、作者、标签。
- 支持按作者和标签筛选。
- 支持基础阅读器：上一页、下一页、跳页、单页、双页、适应宽度、适应高度。
- 支持阅读进度记忆。
- 支持中文 / 英文界面切换。
- 支持快捷键设置与保存。

## 3. GitHub 同类项目调研

### 3.1 YACReader

- 项目地址：https://github.com/YACReader/yacreader
- 技术倾向：C++ / Qt。
- GitHub 状态：公开仓库，约 1.2k stars，仍有近期提交。
- 可参考点：
  - 书库与阅读器分离的产品结构。
  - 本地数据库管理漫画元数据。
  - 作者、系列、标签、阅读进度等书库管理能力。
  - 漫画阅读方向、双页阅读、沉浸式阅读。
- 不采用点：
  - 不照搬多格式压缩包体系。
  - 不做本地服务器与多设备同步。
  - 不使用其偏传统桌面软件的 UI 气质。

### 3.2 OpenComic

- 项目地址：https://github.com/ollm/OpenComic
- 技术倾向：桌面端跨平台漫画阅读器。
- 可参考点：
  - 多阅读模式：日漫右到左、双页、webtoon 纵向滚动。
  - 拖拽打开、主文件夹管理、阅读进度、书签。
  - 快捷键与输入设备自定义的设计方向。
- 不采用点：
  - 不做云存储、远程服务器、OPDS、AI 图像增强。
  - 不做压缩档直接读取。

### 3.3 Yomikiru

- 项目地址：https://github.com/mienaiyami/yomikiru
- 技术倾向：Electron / React / TypeScript。
- 可参考点：
  - 离线优先。
  - 多阅读模式、Zen Mode、双页视图、快捷键自定义。
  - 现代前端技术构建 UI 的方式。
- 不采用点：
  - 不做 EPUB / PDF / 小说功能。
  - 不做 Anilist 等联网集成。
  - 不做压缩包支持。

### 3.4 Koodo Reader

- 项目地址：https://github.com/koodo-reader/koodo-reader
- 技术倾向：JavaScript，现代电子书管理器。
- GitHub 状态：公开仓库，约 27k stars，社区规模大。
- 可参考点：
  - 现代书架 UI。
  - 多语言界面。
  - 书籍导入、书架组织、阅读记录、主题系统。
- 不采用点：
  - 不做综合电子书阅读器。
  - 不做同步、备份、在线服务。
  - 不让格式支持范围膨胀。

## 4. 产品架构

### 4.1 推荐技术路线

当前采用：

- WPF / .NET 8 原生 Windows 桌面软件。
- 使用 C# 处理文件扫描、图片读取、缩略图缓存、SQLite 数据库。
- 使用 WPF 构建书架、阅读器、设置、快捷键、多语言等界面。
- UI 方向从“苹果图书式纸质书隐喻”调整为“本地漫画库 + 极简图片查看器”：书库负责管理，阅读器负责干净看图。
- 已读、未读、在读、搁置属于书库筛选维度，不作为侧栏一级页面；侧栏只保留主页、书库、收藏等高频入口。
- 漫画源文件只读访问，所有用户劳动数据必须写入 SQLite，并通过自动备份保护。

理由：

- 最终形态是真正的 Windows `.exe` 软件，不依赖浏览器预览。
- 更适合本地图片查看器、文件夹扫描和原地存档。
- WPF 对图片显示、滚动、缩放、快捷键和窗口管理支持直接。
- 测试版可以发布为可直接运行的 `.exe`。
- SQLite 文件适合本地离线元数据保存，便于原地备份、迁移和恢复用户 Tag。
- 阅读器不再追求纸质书视觉，避免压缩图片显示空间，控件应保持可隐藏、轻量、统一。

备选：

- Tauri + React：UI 迭代快，但底层仍是 WebView，不符合当前“必须是软件”的方向。
- Electron + React：开发更快，但体积和内存占用偏大。
- WinUI 3：Windows 原生感更强，但工程复杂度高于当前 MVP 需要。
- Qt：适合传统桌面软件，但不符合“苹果图书式现代 UI”的快速迭代诉求。

### 4.2 发布与 exe 交付

目标交付结果：

- 开发期：可通过本地命令启动调试版桌面程序。
- 测试期：生成可直接运行的 Windows `.exe`，不依赖安装流程，双击即可验证。
- 发布期：生成 Windows 安装包，用户双击安装后使用。
- 追加目标：生成绿色便携版 `.exe`，可放在任意目录直接运行。

WPF 路线下的交付策略：

- 使用 `dotnet publish` 生成 Windows `.exe`。
- 测试版应用数据默认写入 `.exe` 所在目录下的存档目录，便于直接运行和整体搬移。
- 漫画源文件只读访问，软件只保存元数据、封面缓存和阅读进度。
- 后续需要图标、应用名、版本号、安装目录、卸载信息等发布配置。

注意：

- 测试版采用原地存档策略，启动时在 `.exe` 同级目录创建数据目录。
- 安装版更适合正式使用，便携版更适合临时测试和移动硬盘场景。

### 4.3 模块划分

- `LibraryScanner`：扫描书库根目录，识别漫画文件夹。
- `BatchImportAnalyzer`：分析拖入的作者文件夹，递归查找可导入的漫画目录，并在首次导入时给出基础 Tag 预判。
- `LibraryDatabase`：管理书籍元数据、作者、标签、阅读进度、状态、收藏和数据库备份。
- `ImageProvider`：读取本地图片，提供页面路径与尺寸信息。
- `CoverService`：生成与缓存封面缩略图。
- `ImageLoader`：只加载当前阅读页，并限制解码尺寸，避免大图一次性占用过多内存。
- `NaturalPathComparer`：按自然排序处理 `1, 2, 10` 这类页码。
- `ReaderState`：保存当前阅读模式、页码、缩放、阅读方向。
- `ShortcutManager`：管理快捷键配置、冲突检测与持久化。
- `I18nManager`：管理中文 / 英文语言包与运行时切换。
- `SettingsService`：保存软件设置。

### 4.4 数据安全约束

- 用户手动录入的 `书名 / Tag / 作者 / 简介 / 封面页 / 阅读次数 / 阅读状态 / 收藏` 属于高价值数据，优先级高于 UI 效果。
- 启动迁移、删除库记录、路径重定位、保存核心元数据前必须保留数据库备份。
- 自动备份保存在 `.exe` 同级 `MangaReader_Data/backups/`，保留最近 40 份。
- `打开备份` 只负责打开备份目录；恢复时应先关闭软件，再把目标备份文件复制为数据目录中的 `app.db`。恢复前建议保留当前 `app.db` 的副本，避免误覆盖。
- 删除库记录只删除软件数据库记录，绝不删除硬盘漫画源文件。
- 扫描、同步、再次导入、批量导入都不得覆盖已有用户手工 Tag；智能推断只能在新书首次创建时写入。
- 后续如增加批量编辑，必须先做备份或可撤销机制。

## 5. 本地文件组织规则

### 5.1 书库根目录

用户选择一个或多个本地目录作为漫画库根目录。

示例：

```text
MangaLibrary/
  作者A/
    作品01/
      001.jpg
      002.jpg
      003.png
    作品02/
      001.webp
      002.webp
  作者B/
    作品03/
      page_001.jpg
      page_002.jpg
```

### 5.2 识别策略

- 包含图片文件的目录识别为一本书。
- 目录名默认作为书名。
- 上级目录可作为作者候选值，但不强制。
- 图片按自然排序排列，例如 `1.jpg`、`2.jpg`、`10.jpg` 应按 1、2、10 排序。
- 普通同步会递归扫描书库根目录，识别所有直接包含图片的目录。
- 作者批量导入会递归分析拖入的作者文件夹，找到所有直接包含图片的子目录作为候选漫画。

### 5.3 作者文件夹批量导入规则

- 用户可拖入一个或多个作者文件夹；拖拽数据中的所有文件夹都必须进入处理队列，不能只取第一个。
- 拖入文件夹时必须有明确视觉反馈，包括覆盖层、状态栏提示和复制光标。
- 每个作者文件夹会弹出批量导入确认窗口，允许修改作者名与每本漫画标题。
- 弹窗统计必须显示实际识别到的候选漫画数量，而不是只统计第一层子文件夹。
- 导入写入以 `folder_path` 为唯一合并依据；同一路径再次导入是更新，不同路径必须保留为不同漫画。
- 导入过程中不能清空当前书库集合，否则连续拖入多个作者文件夹会造成“覆盖”或“只剩一本”的体验。
- 已有书籍再次导入时保留原 Tag；智能推断结果只对新书首次创建生效。

后续可增强：

- 支持手动合并系列。
- 支持从目录名解析作者、系列、卷数。

## 6. 数据模型草案

### 6.1 Book

```text
id: string
title: string
author_id: string | null
library_root: string
folder_path: string
page_count: number
cover_page_index: number
last_read_page_index: number
reading_status: unread | reading | finished
favorite: boolean
created_at: datetime
updated_at: datetime
last_opened_at: datetime | null
```

### 6.2 Author

```text
id: string
name: string
alias: string | null
note: string | null
created_at: datetime
updated_at: datetime
```

### 6.3 Tag

```text
id: string
name: string
color: string
created_at: datetime
updated_at: datetime
```

### 6.4 BookTag

```text
book_id: string
tag_id: string
```

### 6.5 Shortcut

```text
action_id: string
keybinding: string
scope: global | library | reader
enabled: boolean
```

### 6.6 AppSettings

```text
language: zh-CN | en-US
theme: light | dark | system
library_roots: string[]
default_reading_mode: single | double | scroll
default_reading_direction: ltr | rtl
```

## 7. 数据保存策略

### 7.1 总原则

- 漫画源文件默认只读访问，不在漫画图片目录内写入元数据。
- 测试版启动时在 `.exe` 所在目录生成存档目录，用户在软件内产生的数据统一保存到该目录。
- 结构化数据使用 SQLite 保存，便于查询、筛选、备份和迁移。
- 可再生成的数据使用缓存目录保存，例如封面缩略图。
- 关键用户配置使用设置文件或 SQLite 保存，但必须有统一入口管理。
- 所有写入操作应通过 Repository / Service 层完成，不允许 UI 组件直接写文件或数据库。

### 7.2 测试版数据目录

测试版 `.exe` 目标是直接运行，因此采用原地存档策略：程序启动时检查 `.exe` 所在目录，并在同级创建一个固定的数据目录。

建议策略：

- 测试版优先使用 `.exe` 同级的 `MangaReader_Data/` 目录。
- 如果目录不存在，启动时自动创建。
- 如果目录已存在，直接读取其中的数据库、设置和缓存。
- 整个测试版文件夹可以整体复制到另一台电脑，数据跟随移动。
- 如果 `.exe` 所在目录无写入权限，应弹出错误提示并引导用户移动到可写目录。
- 首次启动时自动创建数据目录、数据库文件和缓存目录。
- 数据目录路径需要在设置页中展示，方便用户备份和排查。

建议目录结构：

```text
MangaReader.exe
MangaReader_Data/
  app.db
  settings.json
  cache/
    covers/
  logs/
```

### 7.3 SQLite 保存内容

SQLite 保存所有需要稳定持久化和查询的数据：

- 书籍信息：标题、作者、路径、页数、封面页、收藏状态。
- 作者信息：作者名、别名、备注。
- 标签信息：标签名、颜色。
- 书籍与标签关系。
- 阅读进度：最后阅读页、阅读状态、最后打开时间。
- 快捷键绑定：功能 ID、快捷键组合、作用域、启用状态。
- 书库根目录：用户添加过的本地漫画库路径。

### 7.4 设置文件保存内容

`settings.json` 保存启动时需要快速读取、但不适合频繁查询的配置：

- 当前语言：`zh-CN` 或 `en-US`。
- 当前主题：浅色、深色、跟随系统。
- 窗口大小与位置。
- 最近打开的页面。
- 当前数据目录路径。

如果某项设置需要被筛选或复杂查询，则应迁移进 SQLite。

### 7.5 缓存保存内容

缓存目录只保存可重建内容：

- 封面缩略图。
- 页面尺寸索引。
- 后续可能的低清预览图。

缓存原则：

- 删除缓存不应导致用户数据丢失。
- 封面缓存文件名应基于 `book_id`、`cover_page_index` 和源文件修改时间生成。
- 当用户修改封面页时，应主动刷新对应缓存。
- 切换漫画时优先读取已有封面缓存；缓存不存在或源图变更时再生成。
- 阅读页本身不应全部缓存为位图，阅读器只加载当前页，必要时预留“上一页/下一页预读”。

### 7.6 保存时机

不同操作使用不同保存策略：

- 编辑作者、标题、标签：用户确认后立即写入 SQLite。
- 设置封面页：确认后立即写入 SQLite，并刷新封面缓存。
- 阅读进度：翻页时可防抖保存，例如 1-3 秒内合并写入；退出阅读器时强制保存一次。
- 快捷键修改：确认无冲突后立即保存。
- 语言切换：立即保存并刷新界面。

### 7.7 数据安全与恢复

- SQLite 写入应使用事务，避免批量操作中途失败导致数据半写入。
- 重新扫描书库不应删除用户手动编辑过的标题、作者、标签和封面设置。
- 批量导入不得把当前内存书库清空后只显示本次导入结果；应按 `FolderPath` 合并更新，保证连续导入不会让之前导入的漫画从列表中消失。
- `books.tags` 是每本漫画 Tag 的真实保存位置；`managed_tags` 只是候选池和管理视图。
- 默认预设标签被删除或重命名后，需要写入 `suppressed_tags`，避免重启后硬编码预设反弹。
- 如果漫画文件夹移动，应标记为“路径失效”，不要直接删除书籍记录。
- 提供“重新定位文件夹”功能用于修复移动后的路径。
- 后续可提供导出/导入元数据功能，方便迁移到另一台电脑。

## 8. 双语言显示方案

### 8.1 基础原则

- 所有 UI 文案不得硬编码在组件里。
- 使用语言 key，例如 `library.allBooks`、`reader.nextPage`。
- 中文为主语言，英文为第二语言。
- 用户可在设置中切换语言。
- 切换语言后界面即时刷新，不要求重启。

### 8.2 语言文件示例

```text
locales/
  zh-CN.json
  en-US.json
```

示例 key：

```json
{
  "library.allBooks": "全部书籍",
  "library.authors": "作者",
  "library.tags": "标签",
  "reader.nextPage": "下一页",
  "reader.previousPage": "上一页",
  "settings.shortcuts": "快捷键"
}
```

```json
{
  "library.allBooks": "All Books",
  "library.authors": "Authors",
  "library.tags": "Tags",
  "reader.nextPage": "Next Page",
  "reader.previousPage": "Previous Page",
  "settings.shortcuts": "Shortcuts"
}
```

### 8.3 需要注意的问题

- 英文文本长度通常更长，按钮和侧边栏需要预留弹性布局。
- 标签名、作者名、书名是用户数据，不参与界面翻译。
- 快捷键动作名需要翻译，但快捷键组合本身不翻译。

## 9. UI 方向

### 9.0 圆角规范

圆角不是全项目一个裸数值。统一圆角应按控件语义收敛为少量 Token，避免每个控件凭感觉写 `CornerRadius`：

| Token | 数值 | 用途 |
| --- | ---: | --- |
| `RadiusClip` | 4 | 封面内部裁切、书脊、小图层 |
| `RadiusProgress` | 5 | 进度条、极细状态条 |
| `RadiusControl` | 9 | 普通按钮、列表项、紧凑控件 |
| `RadiusPanel` | 10 | 信息卡、筛选区、列表容器 |
| `RadiusField` | 11 | 输入框、下拉框、筛选胶囊 |
| `RadiusTag` | 16 | Tag 芯片、较大的状态胶囊 |
| `RadiusDialog` | 32 | 弹窗、大遮罩、大容器 |
| `RadiusPill` | 999 | 真正的胶囊按钮、徽章、圆形选择控件 |

原则：

- 同一类控件必须使用同一 Token，不再新增 `13 / 17 / 21 / 23` 这类无语义半径。
- 封面裁切、书本装订、圆形勾选、弹窗外壳允许有独立语义，但必须映射到 Token。
- 后续清理 XAML 时优先替换主窗口高频控件，再逐步处理弹窗和阅读器。

### 9.1 书架页

- 左侧边栏：全部书籍、最近阅读、收藏、作者、标签、设置。
- 顶部区域：搜索框、排序、筛选、视图密度。
- 主区域：大封面卡片网格。
- 卡片内容：封面、标题、作者、阅读进度。
- 视觉关键词：圆角、柔和阴影、大留白、低饱和背景。

### 9.2 详情页 / 侧面板

- 显示封面、标题、作者、标签、页数、阅读状态。
- 支持设置封面页。
- 支持编辑书名、作者、收藏、阅读状态和基础元数据；作者批量重命名以独立作者页为准。
- 支持添加 / 删除标签。

### 9.3 阅读器

- 默认使用适应高度阅读。
- 顶部与底部工具栏由用户显式隐藏/显示；不再根据鼠标停留时间自动消失。
- 支持单页、双页、连续滚动。
- 双页模式提供“间距”滑块，控制左右两页之间的距离；调整后写入本地设置，翻页、刷新和重新打开阅读器都保持该值。
- 支持从左到右 / 从右到左。
- 支持快捷键翻页。
- 自动保存阅读进度。

### 9.4 设置页

- 常规设置：语言、主题、默认书库路径。
- 阅读设置：默认阅读模式、默认方向、背景色。
- 快捷键设置：按功能分组，支持点击后重新录入。
- 数据设置：重新扫描书库、清理封面缓存、导出/导入元数据。

## 10. 快捷键系统

### 10.1 默认快捷键草案

```text
下一页: ArrowRight / Space
上一页: ArrowLeft
全屏: W
适应高度: E
适应宽度: Q
隐藏/显示 UI: D
切换单页/双页: S
切换滚轮状态: Z
退出阅读器: X / Escape
切换左右阅读顺序: C
切换背景色: A
收藏/取消收藏: F
打开设置: Ctrl+,
```

### 10.2 设计要求

- 快捷键按作用域区分：书库、阅读器、全局。
- 修改快捷键时必须检测冲突。
- 冲突时提示用户覆盖或取消。
- 快捷键配置保存在本地数据库或设置文件中。

## 11. 封面系统

### 11.1 默认逻辑

- 新扫描到的书默认使用第一页作为封面。
- 封面缩略图生成后写入缓存目录。
- 缓存命名基于书籍 id 和封面页索引。

### 11.2 手动指定封面

- 在详情页输入页码或从页面缩略图中选择。
- 页码显示使用 1-based，内部索引使用 0-based。
- 修改封面后立即刷新书架卡片。

## 12. 后续任务清单

### 12.1 产品任务

- 确认软件名称。
- 确认最终技术路线：Tauri 还是 Electron。
- 确认书库文件夹识别规则是否允许多层目录。
- 确认默认阅读方向：日漫优先使用右到左，还是普通左到右。
- 确认 UI 参考程度：强苹果图书风格，还是保留更多漫画管理器信息密度。

### 12.2 技术任务

- 初始化项目骨架。已完成：Vite + React + TypeScript 前端原型。
- 初始化 WPF 原生桌面工程。已完成：`MangaReader.Native`。
- 引入 SQLite。已完成：`Microsoft.Data.Sqlite`。
- 实现书库扫描。已完成：递归扫描含图片的文件夹。
- 实现图片自然排序。已完成：`NaturalPathComparer`。
- 实现 SQLite 数据保存层。已完成：书籍、书库路径、阅读进度基础保存。
- 实现应用数据目录初始化。已完成：`.exe` 同级 `MangaReader_Data/`。
- 实现封面缩略图缓存。已完成：`MangaReader_Data/cache/covers/`。
- 实现基础书架 UI。已完成：静态书架、作者筛选、标签筛选、搜索、双语言切换。
- 实现基础阅读器。已完成：上一页、下一页、缩放、适应宽度、适应高度。
- 实现大图安全加载。已完成：只加载当前页，并限制解码宽度。
- 实现作者与标签编辑。已完成：作者、Tag 输入并保存 SQLite。
- 实现中英文语言包。已完成：前端原型语言字典。
- 实现快捷键设置。已完成：下一页/上一页快捷键输入、冲突检测与保存。
- 实现文件夹移动后的重新定位。已完成：路径失效显示与单本漫画重新定位。
- 实现双页模式：已完成：单页/双页切换、左右方向切换、横图跨页识别。
- 实现作者文件夹批量导入。已完成：拖拽文件夹、作者确认、候选列表、标题编辑、递归识别、多文件夹队列导入。
- 实现导入时智能 Tag 推断。已完成：首次导入时判断内容形态、色彩规格、画质规格；再次导入不覆盖手工 Tag。
- 清理或归档早期 Web 原型，避免主线混乱。

### 12.3 风险点

- 大型图片书库扫描可能卡顿，需要后台任务与进度显示。
- 大量封面生成可能占用 CPU，需要队列化与缓存。
- 图片命名不规范会影响排序，需要可手动修正或重新排序。
- 阅读进度频繁写入可能影响性能，需要防抖保存与退出时强制保存。
- 重新扫描书库可能覆盖手动元数据，需要明确“扫描结果”和“用户编辑数据”的优先级。
- 批量导入如果只取第一个拖拽路径或导入前清空当前集合，会造成连续导入互相覆盖，必须按路径合并。
- 拖拽导入如果没有进入态反馈，用户会误以为操作失败，书库页必须保留可见覆盖层提示。
- 双页模式需要处理横图、跨页图、奇偶页偏移。
- 中英文 UI 长度差异会影响布局，需要早期就按 i18n 思路设计。
- 文件夹移动后不能删除数据库记录，应标记路径失效，并提供重新定位入口。

## 13. 开发记录

### 2026-06-11 原型 0.1

- 已创建 Vite + React + TypeScript 项目骨架。
- 已实现苹果图书气质的静态书架首页。
- 已实现模拟漫画数据、作者筛选、标签筛选、搜索。
- 已实现中文 / 英文双语言界面切换。
- 已实现默认快捷键展示区。
- 已实现原地存档策略的 UI 说明：`MangaReader.exe` 同级 `MangaReader_Data/`。
- 已通过 `npm run build` 构建检查。
- 已启动开发预览：`http://127.0.0.1:5173/`。
- 已停止作为主线；该原型仅保留为 UI 参考。

### 2026-06-11 原生软件 0.1

- 已安装 .NET 8 SDK。
- 已创建 WPF 原生桌面工程：`MangaReader.Native`。
- 已实现 `.exe` 同级原地存档目录：`MangaReader_Data/`。
- 已实现 SQLite 数据库：`MangaReader_Data/app.db`。
- 已实现漫画库文件夹选择与扫描。
- 已实现图片自然排序，避免 `1,10,2` 错误顺序。
- 已实现封面缩略图缓存：`MangaReader_Data/cache/covers/`。
- 已实现基础阅读器：上一页、下一页、缩放、适应宽度、适应高度。
- 已实现当前页懒加载，避免一次性把整本漫画读入内存。
- 已实现阅读进度保存。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布测试版 `.exe`：`_release/MangaReader-Test/MangaReader.Native.exe`。
- 已发布自包含测试版 `.exe`：`_release/MangaReader-Test-SelfContained/MangaReader.Native.exe`。
- 待补：中文/英文切换迁移到 WPF、系列视图、缩略图批量队列优化。

### 2026-06-11 原生软件 0.2

- 已实现作者与 Tag 编辑 UI，并保存到 SQLite。
- 已实现指定封面页 UI，并刷新封面缓存。
- 已实现路径失效显示：扫描时数据库中已不存在的漫画路径会显示为“路径失效”。
- 已实现单本漫画重新定位：选择移动后的文件夹后保留原作者、Tag、封面页和阅读进度。
- 已实现双页模式：支持单页/双页切换。
- 已实现双页方向：支持从左到右、从右到左。
- 已实现横图跨页识别：横向大图在双页模式下按单张跨页图显示。
- 已实现快捷键设置 UI：下一页/上一页按键可输入保存，带基础冲突检测。
- 已重新发布测试版与自包含测试版 `.exe`。

### 2026-06-11 原生软件 0.3

- 已将主窗口改为“书架窗口”，不再直接承载阅读器。
- 已新增独立阅读窗口：双击书本后打开 `ReaderWindow`。
- 已将书架与阅读器解耦：主窗口负责管理，阅读窗口负责翻页、缩放、单双页。
- 已新增彩色圆角 Tag 池。
- 已支持搜索/输入 Tag，并可将 Tag 拖到书本卡片上快速分配。
- 已新增角色名称、出品时间、录入时间、简介字段。
- 已将录入时间与出品时间拆分为独立属性。
- 已新增“打开所在文件夹”按钮，但界面不直接显示文件夹路径。
- 已重新发布测试版与自包含测试版 `.exe`。

### 2026-06-11 原生软件 0.3.1

- 修正书架选中态：移除默认蓝色大框，选中边框与书本卡片外轮廓保持一致。
- 修正 Tag 重复问题：保存前统一拆分、裁剪、去重，并标准化为逗号分隔。
- 修正空选择状态：未选中任何书本时，右侧只显示空状态提示，不显示详细信息表单。
- 录入时间改为可编辑字段，并新增“今日”快捷按钮。
- 出品时间与录入时间保存时会校验并规范化标准日期格式，如 `2002-03-09`。
- 已重新发布测试版与自包含测试版 `.exe`。

### 2026-06-11 原生软件 0.3.2

- 调研 GitHub 与 TRAE 社区 UI Skill 方向：参考 `WPF UI`、`iNKORE.UI.WPF.Modern`、社区 `UI-Interface-Design-Review` Skill 的现代 UI/Fluent/设计规范思路。
- 当前未直接引入第三方 WPF UI 依赖，避免测试版增加安装与主题接入风险。
- 主窗口进行苹果图书式视觉重构：柔和暖色背景、半透明侧栏、弱化工具按钮、书架标题区、独立书本卡片。
- 书本卡片新增封面阴影、轻微旋转底影、书脊暗部渐变，以增强“书”的实体感。
- `重新扫描` 改名为 `同步书架`，定位从工程按钮调整为书库同步动作。
- 存档提示不再直接展示本地路径，只说明数据保存在 `MangaReader_Data`，避免界面暴露文件系统细节。
- 已重新发布测试版与自包含测试版 `.exe`。

### 2026-06-11 原生软件 0.3.3

- 书本卡片新增 Hover 动画：鼠标悬停时会轻微放大并上浮，移开后回落。
- 书本外观从单一“大书脊”改为多款自动分配样式：不同宽高、书脊宽度、底影角度和底色。
- 右侧详情栏在未选中任何书本前彻底隐藏，不再占据窗口空间。
- 选中书本后右侧详情栏展开，但默认进入只读模式。
- 新增“编辑 / 取消编辑”模式切换，只有编辑模式下才能修改作者、角色、日期、Tag、封面页和简介。
- 输入框高度与内边距调整，改善文字在输入框中显示不全的问题。
- 已重新发布测试版与自包含测试版 `.exe`。

### 2026-06-11 原生软件 0.3.4

- 修正只读模式与编辑模式几乎一致的问题：只读模式改为纯信息展示卡，不再显示输入框。
- 编辑模式才显示表单输入框、保存信息、设置封面等编辑控件。
- Hover 动画从“放大+上浮”改为仅轻微上浮，避免书本图标突破布局边界。
- 移除书本下方的旋转底影层，解决视觉上像叠着一张图的问题。
- 保留多款书本样式，但以封面尺寸、书脊宽度和底色差异体现，不再依赖额外叠图。
- 已重新发布测试版与自包含测试版 `.exe`。

### 2026-06-11 原生软件 0.3.5

- 阅读窗口视觉重构：从普通黑底工具窗改为“打开一本书”的阅读表面。
- 新增阅读窗口启动动画：窗口淡入，书页容器从窄封面横向展开，并轻微上浮到位。
- 阅读区新增纸张底色、页边、书脊/中缝阴影和书页投影，弱化普通二级窗口感。
- 翻页时新增短淡入动画，避免图片硬切。
- 顶部与底部工具栏改为更柔和的深暖色，按钮改为圆角胶囊样式。
- 已重新发布测试版与自包含测试版 `.exe`。

### 2026-06-11 原生软件 0.3.6

- 修正阅读界面比例问题：移除书页容器的横向缩放展开动画，避免图片被压成细条。
- 阅读窗口打开动画改为淡入 + 轻微上浮，不再改变内容比例。
- 修正适应宽度/适应高度按钮：缩放计算改用 WPF 设备无关尺寸，而不是原始像素尺寸。
- 阅读窗口打开后默认执行一次适应高度，避免大图初始比例异常。
- 修正书架 Hover 穿模：移除 Hover 位移动画，只保留轻量选中/悬停高亮。
- 支持手动切换漫画书样式，并将样式写入 SQLite 的 `book_style` 字段。
- 支持点击书架空白处取消选中并收起详情栏。
- 已重新发布测试版与自包含测试版 `.exe`。

### 2026-06-11 原生软件 0.3.7

- 修正扫描失败：`UpsertBook()` 扫描写库路径不再显式写入 `book_style`，避免 `$bookStyle` 参数绑定缺失导致扫描中断。
- `book_style` 保留为数据库字段，默认值为 `-1`。
- 漫画书样式只在用户点击“切换书本样式”时通过 `SaveMetadata()` 保存。
- 已重新发布测试版与自包含测试版 `.exe`。

### 2026-06-11 原生软件 0.3.8

- 增加书架搜索框，支持按书名、作者、角色名称、Tag、简介、出品时间、录入时间过滤。
- 增加隐藏作品功能：默认书架不显示隐藏作品，侧栏可勾选“显示隐藏作品”查看和恢复。
- 数据库增加 `is_hidden` 字段，隐藏是软标记，不删除任何漫画源文件。
- 增加删除库记录功能：只删除软件内书籍记录，不删除硬盘里的漫画图片文件。
- 删除前会弹出确认框，并明确说明不会删除源文件。
- 标签池会自动排除隐藏作品，除非勾选“显示隐藏作品”。
- 已重新发布测试版与自包含测试版 `.exe`。

### 2026-06-11 原生软件 0.3.9

- 增加作者筛选下拉框，扫描和保存元数据后会自动刷新作者列表。
- 增加点击 Tag 筛选书架：标签池中的彩色 Tag 既可拖拽分配，也可点击作为筛选条件。
- 增加清空筛选按钮，一键清除搜索词、作者筛选、Tag 筛选和隐藏作品显示状态。
- 增加书架排序：支持按标题、作者、阅读进度、录入时间、出品时间排序。
- 筛选与排序统一基于 `ICollectionView` 视图层完成，不修改漫画源文件，也不额外写入数据库。
- 修正点击搜索框、下拉框和按钮时误触发空白区域取消选中的问题。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已重新发布普通测试版 `.exe`：`_release/MangaReader-Test/MangaReader.Native.exe`。
- 已重新发布自包含测试版 `.exe`：`_release/MangaReader-Test-SelfContained/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.10

- 增加“阅读次数”字段，可手动标记一本漫画读过几次。
- SQLite `books` 表增加 `read_count` 字段，旧数据库启动时会自动迁移，默认值为 `0`。
- 详情栏只读模式新增 `+1` / `-1` 快捷按钮，用于快速记录或修正阅读次数。
- 编辑模式新增“阅读次数”输入框，可直接填写准确次数，保存时校验必须为大于等于 `0` 的数字。
- 书架卡片会在阅读次数大于 `0` 时显示“读过 N 次”，未标记时不增加额外噪音。
- 书架排序新增“阅读次数优先”，搜索也可匹配阅读次数显示文本。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已重新发布普通测试版 `.exe`：`_release/MangaReader-Test/MangaReader.Native.exe`。
- 已重新发布自包含测试版 `.exe`：`_release/MangaReader-Test-SelfContained/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.11

- 修复启动失败：作者筛选下拉框刷新时会触发 `SelectionChanged`，再递归刷新作者列表，导致启动阶段堆栈溢出。
- 为作者筛选刷新增加 `_isRefreshingAuthorFilters` 保护，刷新列表期间不再响应筛选事件回流。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动验证，程序不再启动即崩。
- 已重新发布普通测试版 `.exe`：`_release/MangaReader-Test/MangaReader.Native.exe`。
- 已重新发布自包含测试版 `.exe`：`_release/MangaReader-Test-SelfContained/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.12

- 增加书籍状态体系：每本漫画可标记为“未读 / 在读 / 已读 / 搁置”。
- 增加收藏字段：每本漫画可独立标记收藏。
- SQLite `books` 表增加 `reading_status` 和 `is_favorite` 字段，旧数据库启动时会自动迁移。
- 详情栏只读模式显示阅读状态和收藏状态，编辑模式可修改状态与收藏。
- 书架卡片显示状态徽标，收藏作品显示为“收藏 · 状态”。
- 书架顶部新增状态筛选，侧栏新增“只看收藏”。
- 搜索支持匹配阅读状态和收藏文本，并与作者、Tag、隐藏作品、阅读次数筛选共用同一视图过滤器。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动验证。
- 已重新发布普通测试版 `.exe`：`_release/MangaReader-Test/MangaReader.Native.exe`。
- 已重新发布自包含测试版 `.exe`：`_release/MangaReader-Test-SelfContained/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.13

- 书架首页进行第一阶段视觉重构：顶部改为“标题区 + 当前筛选摘要”，不再只是单行标题。
- 新增概览统计卡片：当前书架、收藏、在读、已读四块摘要面板。
- 统计卡片会随当前书库与筛选结果刷新，形成更明确的视觉层级。
- 新增书架空状态面板：当筛选结果为空时，会显示原因提示，而不是直接留白。
- 当前筛选摘要会显示搜索词、作者、状态、Tag、收藏筛选、隐藏作品等条件组合。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动验证。
- 已重新发布普通测试版 `.exe`：`_release/MangaReader-Test/MangaReader.Native.exe`。
- 已重新发布自包含测试版 `.exe`：`_release/MangaReader-Test-SelfContained/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.14

- 修正阅读器边界反馈：到第一页或最后一页时，不再像按键失灵，会在页码区域明确提示“已经是第一页 / 已经是最后一页”。
- 阅读器上一页 / 下一页按钮会根据当前页自动禁用，边界状态更直观。
- 修正适应宽度 / 适应高度逻辑：不再使用固定魔法值减法，而是按 `ScrollViewer` 的真实可用阅读区尺寸与当前图片边距计算缩放。
- 收紧阅读区内部留白：缩小阅读面板外围与图片容器边距，减少“适应后还空很大一截”的问题。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动验证。
- 已重新发布普通测试版 `.exe`：`_release/MangaReader-Test/MangaReader.Native.exe`。
- 已重新发布自包含测试版 `.exe`：`_release/MangaReader-Test-SelfContained/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.15

- 主窗口从“单页管理器”重构为“导航 + 主页 + 书库”结构，更接近苹果图书的信息架构。
- 左侧改为固定导航：主页、书库、收藏、在读、已读、搁置，并保留导入书库、同步书架入口。
- 新增主页视图：包含“继续阅读 / 之前读过 / 收藏 / 最近加入”四个横向分区，去掉每日目标。
- 主页卡片可直接打开漫画，首页职责改为浏览内容，而不是堆管理控件。
- 书库页保留搜索、作者/状态筛选、排序、Tag 池、隐藏作品、只看收藏等管理能力。
- 右侧详情栏继续只服务于书库页，避免主页被编辑表单打断。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动验证。
- 已重新发布普通测试版 `.exe`：`_release/MangaReader-Test/MangaReader.Native.exe`。
- 已重新发布自包含测试版 `.exe`：`_release/MangaReader-Test-SelfContained/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.16

- 继续把主页卡片视觉往苹果图书靠：`继续阅读` 改为更大的封面导向卡片，弱化管理感。
- `之前读过 / 收藏 / 最近加入` 改为更轻量的横向短卡，减少此前“像同一种列表”的问题。
- 主页头部说明改为随书库内容变化的提示文案，不再是固定描述。
- 为主页四个分区补空状态提示，避免无数据时只剩一条空白横带。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动验证。
- 已重新发布普通测试版 `.exe`：`_release/MangaReader-Test/MangaReader.Native.exe`。
- 已重新发布自包含测试版 `.exe`：`_release/MangaReader-Test-SelfContained/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.17

- 修复 0.3.16 引入的启动失败：XAML 初始化 `ComboBox.SelectedIndex` 时提前触发筛选事件，导致标签池控件尚未创建就刷新标签。
- `RefreshVisibleTags()` 增加初始化防护，`TagSearchBox` 尚未创建时直接跳过刷新。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动验证。
- 已重新发布普通测试版 `.exe`：`_release/MangaReader-Test/MangaReader.Native.exe`。
- 已重新发布自包含测试版 `.exe`：`_release/MangaReader-Test-SelfContained/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.18

- 产品方向调整：不再继续强行追求“纸质书 / 苹果图书”感，转向更符合漫画查看逻辑的本地漫画库界面。
- 修复主界面滚动不灵敏：主页主滚动、横向漫画分区、书库列表、Tag 池和右侧详情栏都接入更快的滚轮处理。
- 横向漫画分区支持滚轮横向移动，避免必须拖很细的横向滚动条。
- 将部分显眼文案从“图书 / 书本”改为“漫画库 / 卡片样式”，降低纸质书语义。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动验证。
- 已重新发布普通测试版 `.exe`：`_release/MangaReader-Test/MangaReader.Native.exe`。
- 已重新发布自包含测试版 `.exe`：`_release/MangaReader-Test-SelfContained/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.19

- 针对“漫画库页顶部极丑”的问题做止血式 UI 重排。
- 将原先“巨大标题卡 + 右侧挤一排筛选控件”的灾难布局，拆成标题区、筛选工具条、Tag 工具条、统计条四个清晰层级。
- 全局主色从暖棕书房色退回中性灰白，减少伪纸质书气质。
- 统一标题卡、筛选条、Tag 条、统计卡的白底、边框、阴影和间距。
- 将导航选中色从棕黑改为中性灰黑。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动验证。
- 已重新发布普通测试版 `.exe`：`_release/MangaReader-Test/MangaReader.Native.exe`。
- 由于原自包含目录中的 exe 正在运行并锁定文件，另行发布自包含测试版：`_release/MangaReader-Test-SelfContained-UIFix/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.20

- 阅读窗口改为更接近普通图片查看器的极简结构，不再保留“书页 / 纸张 / 书脊”外壳。
- 顶部改为简洁标题栏和翻页 / 适应按钮，底部改为缩放与模式状态栏，中间尽量全部留给图片。
- 新增“隐藏控件”开关，并支持 `Tab` / `H` 快捷隐藏或显示顶部与底部控制条。
- 新增滚轮模式切换：`滚轮翻页 / 滚轮缩放 / 滚轮滚动`。
- 新增长按左键临时局部放大，松开后恢复，作为更轻量的查看辅助。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动验证。
- 已重新发布普通测试版 `.exe`：`_release/MangaReader-Test/MangaReader.Native.exe`。
- 已重新发布自包含测试版 `.exe`：`_release/MangaReader-Test-SelfContained-ReaderLite/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.21

- 继续把阅读器往专业看图器方向收敛：顶部和底部控件改成覆盖在图片上的薄悬浮条，不再硬切走图片高度。
- 增加轻量自动显隐：移动鼠标时显示控件，停留一段时间后自动收起。
- 滚轮模式现在会持久化保存，重新打开阅读器会记住上次选择的 `翻页 / 缩放 / 滚动`。
- `Esc` 可在控件隐藏时快速恢复控件。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动验证。
- 已重新发布普通测试版 `.exe`：`_release/MangaReader-Test/MangaReader.Native.exe`。
- 已重新发布自包含测试版 `.exe`：`_release/MangaReader-Test-SelfContained-ReaderLite2/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.22

- 修正书库筛选栏的视觉不统一问题，为 `ComboBox` 补上统一的圆角、边框和下拉样式，避免三个系统默认控件混进页面。
- 去掉主页和漫画库页占空间的大标题卡，改成更薄的工具层，把纵向空间还给内容本身。
- 首页无内容时不再堆空标题和空分区，改成单一的导入引导面板，直接提示导入漫画或进入漫画库。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动验证。

### 2026-06-11 原生软件 0.3.23

- 增加数据库安全备份目录：`MangaReader_Data/backups/`。
- 启动数据库迁移前会自动备份已有 `app.db`，防止迁移失败损坏旧数据。
- 保存用户核心元数据前会节流备份，包括作者、Tag、简介、封面页、阅读次数、阅读状态、收藏等。
- 删除库记录和路径重定位前强制备份，避免误操作后无法找回用户辛苦整理的 Tag 和元数据。
- 备份文件保留最近 40 份，避免无限堆积。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-11 原生软件 0.3.24

- 阅读器顶部按钮、底部下拉框统一为深色极简胶囊样式，去掉白色系统控件混入问题。
- 侧栏移除 `在读 / 已读 / 搁置` 一级入口，这些状态统一回收到书库页的状态下拉筛选中。
- 书库切入和扫描完成后增加保护：如果 `Books` 有数据但当前视图为 0，会自动清空筛选并回到全部漫画，避免“首页有书、书库为空”。
- 技术路线补充写入文档：当前方向为 `WPF/.NET 8 + SQLite + 本地漫画库 + 极简图片查看器`，不再追纸质书隐喻。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-11 原生软件 0.3.25

- 主界面继续收敛控件系统：`CheckBox` 改为统一胶囊选择控件，避免默认方块控件混入。
- 详情栏和编辑栏按钮按语义拆分为主按钮、次级按钮、小按钮和危险按钮。
- `删除库记录` 改用统一危险按钮样式，不再使用零散的内联红色背景。
- Tag 添加、日期今日、阅读次数、快捷键保存等小控件统一使用圆角轻边框风格。
- 书库 Tag 搜索框接入筛选胶囊输入框样式，减少页面控件割裂感。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布到 `_release/MangaReader-Test` 和 `_release/MangaReader-Test-SelfContained-ControlFix`。

### 2026-06-11 原生软件 0.3.26

- 修复筛选下拉框点击后不展开的问题：移除空模板点击层，改为显式打开下拉框。
- 去掉筛选控件获得焦点时出现的系统虚线矩形，保持胶囊控件视觉干净。
- 收藏从侧栏一级入口回收到书库筛选栏，明确其本质是特殊筛选条件。
- 排序入口改为 `排序：标题 / 作者 / 进度 / 阅读次数 / 录入时间 / 出品时间`，作为书库第一排筛选控件常驻显示。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布到 `_release/MangaReader-Test` 和 `_release/MangaReader-Test-SelfContained-FilterFix`。

### 2026-06-11 原生软件 0.3.27

- 修复阅读器隐藏控件后，底部下拉弹层与 `显示控件` 入口重叠的问题。
- 隐藏控件时会强制关闭滚轮模式、阅读模式、方向三个下拉框，避免 Popup 残留。
- 隐藏状态下不再因鼠标移动自动恢复工具条，必须点击 `显示控件`、按 `H / Tab / Esc` 或双击切换。
- `显示控件` 入口移到左上角并提升层级，避免和顶部/底部工具条抢同一块空间。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布到 `_release/MangaReader-Test` 和 `_release/MangaReader-Test-SelfContained-ReaderOverlayFix`。

### 2026-06-11 原生软件 0.3.28

- 数据安全入口显性化：侧栏工具区新增 `立即备份`，用户可手动把当前 `app.db` 复制到 `MangaReader_Data/backups/`。
- 侧栏工具区新增 `打开备份`，可直接打开备份目录，方便检查、复制和恢复。
- 手动备份文件命名使用时间戳与 `manual-backup` 原因标记，继续沿用最多保留 40 份的备份策略。
- 新增项目 `.gitignore`，忽略 `bin/`、`obj/`、`_release/`、`MangaReader_Data/` 等构建产物与本地数据，避免工程目录继续膨胀。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-BackupTools/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.29

- 修复阅读器长按放大镜基本无效的问题：旧实现只是按鼠标点缩放整张图片容器，实际查看局部细节时反馈很弱。
- 新增长按圆形放大镜覆盖层，镜片会跟随鼠标移动，并以当前图片区域作为取样源进行局部放大。
- 放大镜现在在隐藏控件状态下也能触发，不再因为工具条自动收起而失效。
- 双击仍保留为显示 / 隐藏控件，不与长按放大冲突。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-MagnifierFix/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.30

- 修复书库筛选下拉显示不完全的问题：胶囊下拉模板不再让文字区复用过大的左右 Padding，避免 `状态：全部` 被截断为 `状态：全`。
- 状态筛选胶囊宽度从 `132` 调整为 `148`，为中文状态文本和右侧箭头胶囊预留足够空间。
- 筛选面板位置决策：顶部只保留搜索、核心筛选摘要和少量高频入口；完整筛选面板后续放在右侧抽屉，与书籍详情栏共享区域，不再常驻占用纵向内容空间。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-FilterPanelPlan/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.31

- 调整长按查看逻辑：取消圆形局部放大镜，改回用户期望的全图临时缩放。
- 长按时不再使用 `RenderTransformOrigin` 做视觉假缩放，而是直接驱动现有 `ZoomSlider / ImageScale` 布局缩放，使缩放范围与滚动范围一致。
- 缩放锚点改为鼠标所在位置：缩放后会根据鼠标下的图像点反算 `ScrollViewer` 偏移，让该图像点保持在鼠标位置附近。
- 松开鼠标后恢复长按前的缩放倍率。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-HoldZoomPivot/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.32

- 新增基础 Tag 预设，默认常驻显示在书库 Tag 池中，即使当前没有漫画使用这些 Tag 也能直接拖拽分配。
- 默认 Tag 按语义分为三类：`内容形态`、`色彩规格`、`画质规格`。
- `内容形态`：单行本、同人志、CG、杂图合集、画集、短篇。
- `色彩规格`：全彩、黑白、部分彩色。
- `画质规格`：高清、超清、扫图、修图版。
- 用户自己录入的其他 Tag 归入 `自定义` 分类，并继续保留原有自动配色、点击筛选和拖拽分配逻辑。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-DefaultTags/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.33

- Tag 筛选从“单选覆盖”改为“可累计叠加”。
- 现在点击多个 Tag 会形成组合条件，例如 `黑白 + 同人志`，书库只显示同时命中全部已选 Tag 的漫画。
- Tag 区新增“已选 Tag”区域，明确显示当前累计了几个 Tag，并以胶囊形式列出当前筛选条件。
- 点击已选 Tag 胶囊可单独移除该条件；再次点击 Tag 池中的同名 Tag 也会切换移除。
- 顶部筛选摘要与空状态提示会同步反映多 Tag 条件。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-MultiTagFilter/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.34

- 已选中的 Tag 现在会在下方 Tag 池中显示为虚化禁用态。
- 禁用态 Tag 会降低不透明度、取消手型光标，并彻底禁用点击，避免“上方已选、下方还能再点一次”的重复交互。
- Tag 选中或移除后，Tag 池会立即刷新状态；上方已选区仍然是唯一的移除入口。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-TagDisabledState/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.35

- 为 Tag 组增加“组内互斥、跨组可叠加”规则。
- 当前先对 `色彩规格` 生效：`黑白 / 全彩 / 部分彩色` 同时只能保留一个。
- 其他组仍保持叠加，例如 `同人志 + 黑白 + 高清` 依然允许同时选中。
- 实现方式为：点击新的色彩 Tag 时，先移除同组已选项，再加入当前 Tag。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-TagGroupRule/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.36

- 统一 Tag 配色逻辑：上方“已选 Tag”区域不再使用写死的深色背景。
- 已选 Tag 现改为复用同一套 `TagChip.Color` 分类色，与下方 Tag 池保持一致。
- 已选态通过深色描边和关闭符号表达，避免出现“同一个 Tag 上下颜色完全不同”的视觉割裂。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-11 原生软件 0.3.37

- 侧栏新增一级入口：`标签`，与 `主页 / 书库` 并列。
- 新增独立“标签管理”页面，支持全局浏览、搜索、新建标签，并显示每个标签关联了多少本漫画。
- 书库页中的 Tag 现支持右键快捷操作：`重命名标签`、`删除标签`、`前往标签页`。
- 标签管理页支持全局操作：`重命名`、`删除`、`在书库查看`。
- 新增 `managed_tags` 数据表，用于持久化“独立标签”；即使某个标签当前未挂到任何漫画上，也不会从 Tag 池和标签页里消失。
- 当前仍沿用 `books.tags` 字符串作为书籍上的实际 Tag 存储；标签管理页的重命名 / 删除会批量改写所有相关书籍，并在操作前自动创建数据库备份。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-TagManager/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.38

- 明确 Tag 保存策略：`books.tags` 是每本漫画 Tag 的真实保存位置；标签页只是管理视图和辅助标签池，不推进重型独立 Tag 关系系统。
- 加强扫描保护：同步书架时不允许空扫描结果覆盖数据库中已有的手工 Tag，避免重扫后用户劳动成果丢失。
- 加强批量操作可靠性：全局重命名 / 删除 Tag 改为先计算所有受影响书籍，再通过数据库事务一次性批量写入，避免中途异常导致“只改了一半”。
- 批量 Tag 写入前强制创建数据库备份，满足误操作可恢复要求。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-TagSaveGuard/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.39

- 修正标签页语义：`新建标签` 改为 `新建候选标签`，明确未关联标签不会产生筛选结果，只会进入书库 Tag 池等待拖拽/分配。
- `在书库查看` 改为 `按此标签筛选`；当标签关联漫画数量为 0 时禁用该操作，避免点击后得到空列表。
- `内容形态` 组也改为互斥：`单行本 / 同人志 / CG / 杂图合集 / 画集 / 短篇` 同时只能保留一个；跨组组合仍允许，例如 `同人志 + 黑白 + 高清`。
- 新增 `suppressed_tags` 表，用于记录被用户删除或重命名掉的默认预设标签，避免默认标签因为硬编码预设在重启后重新出现。
- 标签重命名 / 删除继续走事务批量写入和强制备份，确保所有相关书籍同步更新或同步失败，不出现部分写入。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-TagSemanticsFix/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.40

- 修复标签页按钮交互反馈：`新建候选标签` 在输入框为空时不再静默无效，而是弹出输入窗口。
- `按此标签筛选` 不再因关联数量为 0 被禁用；无关联标签点击后会显示明确提示，告知需要先拖拽/分配到漫画。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-TagButtonFix/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.41

- 无关联标签现在也可以执行 `按此标签筛选`，会进入书库并显示空结果，而不是阻止操作。
- `重命名` 升级为 `编辑标签`，打开统一风格的圆角弹窗。
- 编辑标签弹窗支持：重命名标签、重新分组标签、查看关联漫画数量、显示最多 3 张关联漫画缩略图、查看最近更新时间。
- 编辑弹窗中的 `更多` 等同于 `按此标签筛选`，会跳到书库按该 Tag 查看；无关联标签也允许进入空结果。
- `managed_tags` 增加 `category` 字段，用于保存用户对标签分组的调整；重命名或重新分组都会刷新标签最近更新时间。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-TagEditDialog/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.42

- 重做 `编辑标签` 弹窗 UI，移除 WPF 默认按钮、默认文本框、默认下拉框导致的粗糙系统控件感。
- 弹窗改为统一圆角卡片结构：标题区、名称/分组一行排布、关联信息卡、关联漫画预览卡、底部胶囊操作按钮。
- 输入框、下拉框、按钮全部改为深色极简胶囊样式，避免混入系统默认白框和直角控件。
- 无关联漫画时显示明确空状态，而不是留出一块空白区域。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-TagEditUiFix/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.43

- 清理标签页输入语义：移除“搜索 + 新建输入框”并排造成的重复感，标签页只保留一个搜索框；新建候选标签通过按钮弹窗或直接使用搜索框内容创建。
- 书库 Tag 点击行为调整：当前选中漫画时，点击 Tag 会直接给该漫画添加 Tag；没有选中漫画时，点击 Tag 才作为筛选条件。
- 书库区域支持直接拖入文件夹；如果拖入的是“作者文件夹”，会进入作者批量导入模式。
- 新增作者文件夹批量导入弹窗：允许确认/修改作者名，显示子文件夹数量和将导入书籍数量，并允许逐本修改标题。
- 新增脚本级智能 Tag 推断：抽样第 10~20 张附近图片判断 `全彩 / 黑白`，按页数推断 `单行本 / 杂图合集`，横图占比高时推断 `CG`，页数较多时补 `高清`。
- 批量导入时优先保护已有手工 Tag：已有记录不为空时不会被智能推断 Tag 覆盖。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-AuthorBatchImport/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.44

- 明确智能 Tag 推断只在作者批量导入的新书首次创建时写入；普通同步、重扫、再次导入已有书籍都不会自动改写 Tag。
- 收紧 Tag 保护：即使已有书籍的 Tag 为空，再次导入时也不会被智能推断自动补 Tag，避免用户主动清空后被反向写回。
- `高清 / 超清` 推断从页数规则改为随机抽检图片体积：样本最大体积 `>= 1MB` 标记 `高清`，`>= 3MB` 标记 `超清`。
- 智能推断仍保留 `全彩 / 黑白` 与 `单行本 / 杂图合集 / CG` 的导入时一次性预判。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-ImportInferenceOnce/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.45

- 修复批量导入候选列表过少的问题：作者文件夹分析改为递归查找所有“直接包含图片”的漫画目录，不再只看第一层子文件夹。
- 修复连续拖入多个文件夹时的覆盖感：拖拽数据中的所有文件夹都会进入导入队列，不再只取第一个目录。
- 批量导入写入改为按 `FolderPath` 合并更新当前书库集合，不再在每次导入前清空 `Books`，避免第二次导入让前一次导入的漫画从列表中消失。
- 书库拖拽增加明确反馈：拖入文件夹时显示深色覆盖层、导入数量提示、状态栏提示，并设置复制光标。
- 作者批量导入弹窗的统计改为显示识别到的候选漫画数量，避免递归识别后“子文件夹数量”和实际导入数量不一致。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-ImportDropMergeFix/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.46

- 根据架构审查建议做第一轮稳定性优化，不进行大规模 MVVM 重构，优先处理会直接影响使用的性能与可靠性问题。
- 新增 `TagCatalog` 作为统一 Tag 预设、分类与颜色来源；书库卡片、Tag 池、标签管理页都应复用同一套颜色逻辑。
- Tag 使用统计改为先重建 `tag -> book[]` 索引，再由 `GetTagUsageCount()` 和 `GetTagBooks()` 查询索引，避免每个 Tag 重复遍历全书库。
- 批量导入改为逐本捕获 `IO / 权限 / 非法路径` 等异常，单本失败不会中断整批导入；结束后显示成功数量、失败数量和失败明细。
- SQLite 初始化时启用 `WAL` 与 `busy_timeout`，并为 `author / reading_status / is_favorite / is_hidden / folder_path` 创建索引。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-StabilityPass/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.47

- 继续落实架构审查中的刷新合批建议，新增 `RefreshLibraryViews()` 统一入口，集中处理 Tag 池、标签管理、作者筛选、排序和书库过滤刷新。
- 导入、保存元数据、隐藏/删除书籍、拖拽添加 Tag、编辑/删除 Tag 等高频路径改为调用统一刷新入口，减少同一用户动作内的重复遍历。
- Tag 索引重建上移到统一刷新入口；当一次刷新同时更新 Tag 池和标签管理页时，`tag -> book[]` 映射最多重建一次。
- 纯搜索类动作仍保留局部刷新，避免每次输入都触发不必要的全页面刷新。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-RefreshBatch/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.48

- 书籍元数据新增 `外文名` 字段，并写入 SQLite `foreign_name` 列；只读详情、编辑表单和搜索匹配都支持该字段。
- 右侧详情在非编辑模式下不再显示 `删除库记录` 按钮，删除入口仅保留在编辑模式中，降低误操作风险。
- 书库卡片在保持统一高度的前提下恢复可见样式差异：样式切换会改变卡片宽度、书脊宽度、轻微倾角和边框色。
- 详情面板中的阅读快捷键设置栏默认隐藏，不再出现在当前信息编辑流程中。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-DetailPanelFix/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.49

- 重做书库卡片样式切换，移除上一版厚重不透明圆角底板和固定倾斜，确保封面区域以透明叠层为主。
- 卡片样式收敛为 4 种明确语义：`书封凹槽`、`普通漫画装订鼓起`、`普通封面`、`白边封面`。
- 4 种样式保持统一封面高度，不再通过随机倾角制造差异；切换样式只改变封面内部的凹槽、装订阴影、白边等视觉层。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-TransparentCardStyles/MangaReader.Native.exe`。

### 2026-06-11 文档整理 0.3.50

- 整理 Claude 架构审查章节，将 0.3.46 - 0.3.49 已完成的建议标记为 `已落实`，避免后续重复处理旧待办。
- 将仍需观察或暂缓的项目单独归类：封面缓存淘汰、`MainWindow` 渐进拆分、Service 接口隔离。
- 修正章节顺序，将 `当前结论` 从错误的 `## 14` 调整为 `## 16`。
- 更新产品边界描述：压缩包支持从绝对不支持改为 `当前不支持，后续视真实资源形态评估`。

### 2026-06-11 原生软件 0.3.51

- 按 15.11 的渐进拆分方向新增 `TagService`，先抽离无 UI 依赖的 Tag 纯逻辑。
- `TagService` 统一负责 Tag 解析、格式化、分类、颜色、互斥分组判断和分类排序。
- `MangaBook` 的 Tag 规范化与 `MainWindow` 的 Tag 分类/互斥/排序调用已切换到 `TagService`，减少窗口代码继续膨胀。
- 本次不改数据库结构、不改 UI 行为，只做低风险逻辑收敛。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-TagServiceSplit/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.52

- 重做 `添加漫画` 入口，移除 WinForms 原生 `FolderBrowserDialog`，避免出现系统默认文件夹选择窗口。
- 新增 `ImportFolderDialog` 自定义导入窗口，视觉统一为浅灰背景、大圆角容器、胶囊输入框和胶囊按钮。
- 新导入窗口支持拖入一个或多个文件夹，也支持粘贴本地文件夹路径后加入列表。
- 确认后继续复用现有作者批量导入分析与确认流程，不改变数据库结构和导入规则。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-CustomImportDialog/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.53

- 补齐导入链路第二层窗口：重做 `AuthorBatchImportDialog`，解决作者批量导入确认窗仍然沿用旧暖色样式的问题。
- 批量确认窗统一为浅灰背景、白色信息卡、胶囊输入框、胶囊按钮，与 `ImportFolderDialog` 和主界面保持一致。
- 导入校验不再弹系统 `MessageBox`，改为窗口底部内联提示，避免系统默认弹窗破坏视觉一致性。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader-Test-SelfContained-BatchImportDialogFix/MangaReader.Native.exe`。

### 2026-06-11 原生软件 0.3.54

- 重构图片查看器 UI，转向更轻的沉浸式图片查看逻辑。
- 顶部工具条收敛为居中的轻量信息浮层，只显示标题与页码，不再混放翻页/缩放按钮。
- 底部工具条统一为单条半透明胶囊控制台，集中放置翻页、适宽/适高、缩放、滚轮模式、单双页和方向切换。
- 隐藏默认滚动条，收紧 ScrollViewer 与图片边距，减少 UI 对画面的侵入。
- Slider、Button、ComboBox 统一改为深色半透明控件，避免系统默认样式。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.54/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.55

- 针对 100 本级作者批量导入出现的真实卡顿做止血优化，优先解决“无反馈”和“可视区域过小”两个直接体验问题。
- 作者批量导入新增居中进度遮罩，显示当前作者、已处理数量、总数和当前作品名；进度条使用自定义胶囊样式，避免系统默认控件感。
- 书库列表从 `WrapPanel` 改为 `VirtualizingStackPanel`，开启 `VirtualizationMode=Recycling` 和页级缓存，优先降低大量漫画卡片同时参与布局的开销。
- 书库卡片改为横向紧凑列表卡片，作为虚拟化优先的性能取舍；后续如果需要恢复多列书架视觉，应使用支持虚拟化的自定义面板，而不是退回普通 `WrapPanel`。
- 书库滚动时当漫画数量达到 80 本以上，会自动进入专注浏览：隐藏搜索/筛选控件、Tag 池和统计卡片，只保留顶部摘要与“展开筛选”入口，释放可看漫画的垂直空间。
- 作者信息先按“特殊 Tag”的产品语义做止血处理；早期版本曾在书库筛选栏提供“编辑作者”，后续已迁移到独立作者页的“改名”入口，避免筛选栏承担编辑职责。批量写入前会强制数据库备份。
- `LibraryDatabase` 新增 `SaveBookAuthorsBatch()`，使用事务批量更新 `books.author`，避免逐本保存造成刷新和写库抖动。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.55/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.56

- 新增 `VirtualizingWrapPanel` 自定义面板，用固定卡片尺寸计算可见行，只生成可视区域和少量预读行内的漫画卡片。
- 书库从上一版单列性能止血列表恢复为多列封面书架，同时保留虚拟化与容器回收，避免退回普通 `WrapPanel` 后再次全量布局。
- 多列面板实现 `IScrollInfo`，支持纵向滚动、滚轮滚动、页面滚动和可见项定位，并与现有滚轮加速逻辑兼容。
- 书库卡片恢复封面主导的视觉结构，保留 4 种透明封面样式：书封凹槽、普通漫画装订鼓起、普通封面、白边封面。
- 卡片尺寸固定为虚拟化友好的统一规格，标题、作者、状态、进度、阅读次数和 Tag 保持紧凑展示，兼顾视觉一致性与滚动性能。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动级验证，窗口创建后保持运行。
- 已发布自包含测试版：`_release/0.3.56/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.57

- 新增 `CoverThumbnailPipeline`，封面缩略图从“扫描/导入时全量加载”改为“卡片进入可视区时异步加载”。
- 扫描书库和作者批量导入不再逐本调用 `CoverCache.LoadOrCreate()`，避免 4000+ 书库首次建库时被封面生成和位图加载阻塞。
- 书库卡片、首页大卡片和首页小卡片统一接入 `BookCoverHost_Loaded / Unloaded`，进入视图时加载缩略图，离开视图时释放 `MangaBook.CoverImage` 强引用。
- 内存缩略图缓存增加 LRU 上限，当前最多保留 320 张已解码缩略图；磁盘缩略图缓存仍保存在 `MangaReader_Data/cache/covers/`。
- 缩略图后台加载使用限流队列，最多 2 个并发，避免单作者 100 本导入后可视区封面生成把磁盘和 CPU 打满。
- 封面缓存继续只生成缩略图，不显示原图；本轮优化只改变加载时机和内存生命周期，不改变视觉结构。
- 发布目录命名从本版起统一使用纯版本号。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动级验证，窗口创建后保持运行。
- 已发布自包含测试版：`_release/0.3.57/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.58

- 根据第 17 节深度走读修复一批运行时正确性隐患，优先处理可崩溃、可卡死和低成本稳定性问题。
- `ReaderWindow` 关闭时停止 `_controlsRevealTimer`，避免窗口关闭后计时器回调继续访问控件。
- 阅读器长按放大入口增加异常保护，`UpdateHoldZoom()` 异常时会调用 `ReleaseHoldZoom()` 释放鼠标捕获。
- `BatchImportAnalyzer` 调整智能 Tag 预判：横图阈值统一为 `1.2`，画质阈值调整为 `高清 >= 2MB / 超清 >= 5MB`。
- `BatchImportAnalyzer` 抽样页索引改为去重输出，避免短作品重复解码同一张图片；图像分析继续使用缩略解码，不读取原始大图尺寸。
- `MangaBook.BookStyleIndex` 不再使用 `Math.Abs(GetHashCode())`，改用位掩码规避 `int.MinValue` 极端溢出。
- `MainWindow_Loaded` 增加顶层异常保护，启动扫描失败时显示状态栏错误，不让 `async void` 异常直接终止应用。
- 书库虚拟化缓存从 `CacheLength=2` 提高到 `4`，降低快速滚动时卡片重建和封面闪白概率。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动级验证，窗口创建后保持运行。
- 已发布自包含测试版：`_release/0.3.58/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.59

- 继续针对 4000+ 书库和单作者 100 本导入做性能优化，重点处理“重复扫盘”和“逐本写库事务”。
- `BatchImportCandidate` 新增 `Pages`，作者文件夹分析阶段枚举出的图片列表会随候选项保留下来。
- 作者批量导入正式写入时优先复用 `candidate.Pages`，不再对每本漫画目录重复 `Directory.EnumerateFiles()`。
- `LibraryDatabase` 新增 `UpsertBooksBatch()`，扫描书库和作者批量导入都改为单连接、单事务批量 `UPSERT`。
- 书库扫描阶段不再逐本 `_database.UpsertBook()`，扫描结果和失效路径统一批量落库，减少 4000+ 首次建库时的 SQLite 提交开销。
- 作者批量导入先逐本构建内存对象和失败报告，再统一批量写入数据库；写库完成后再合并到可见书库集合。
- `SaveBookTagsBatch()` 和 `SaveBookAuthorsBatch()` 复用单个 `SqliteCommand`，不再每本创建新命令对象。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动级验证，窗口创建后保持运行。
- 已发布自包含测试版：`_release/0.3.59/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.60

- 针对 4000+ 大书库输入体验做搜索防抖，避免中文输入法候选阶段每个中间字母都触发全量过滤。
- 书库搜索 `BookSearchBox` 从即时 `RefreshBookFilter()` 改为 `220ms` 防抖刷新。0.3.91 起搜索只过滤书库并刷新统计摘要，不再连带刷新首页书架。
- 书库 Tag 池搜索 `TagSearchBox` 从即时 `RefreshVisibleTags()` 改为 `220ms` 防抖刷新，减少大 Tag 集合下的重复重建。
- 标签管理搜索 `TagManagerSearchBox` 同步接入防抖，降低管理页搜索时的刷新抖动。
- 清空筛选、自动重置筛选和窗口关闭会停止防抖计时器，避免过期延迟刷新干扰用户明确动作。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动级验证，窗口创建后保持运行。
- 已发布自包含测试版：`_release/0.3.60/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.61

- 修正 Tag 互斥规则：`内容形态`、`色彩规格`、`画质规格` 三类内置规格 Tag 都按互斥处理，避免同一本书同时出现 `超清 / 高清 / 扫图 / 修图版`。
- 用户自建 Tag 增加“规则”概念：新建或编辑标签时可选择 `互斥 / 不互斥`，题材类如热血、恋爱、异世界可保留为不互斥。
- `managed_tags` 数据表新增 `is_exclusive` 字段，已有旧数据会自动迁移；旧的规格类标签会按类别自动视为互斥。
- 给漫画添加 Tag、拖拽 Tag、手动保存详情面板 Tag 时都会执行互斥归一化：同一互斥分组只保留最后选择的一个。
- 标签管理页会显示标签规则为 `互斥 / 可叠加`，减少管理时的隐性状态。
- 书库顶部折叠入口文案改为 `专注浏览`，折叠后只保留摘要与展开入口，筛选控件、Tag 池和统计卡片都会收起。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动级验证，窗口创建后保持运行。
- 已发布自包含测试版：`_release/0.3.61/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.62

- 新增全局动画调度入口 `MotionService`，统一短动效的时长、缓动和调用方式，避免各窗口继续手写零散 `Storyboard`。
- 统一动画节奏：`Fast=120ms`、`Normal=160ms`、`Slow=220ms`，缓动默认使用 `CubicEase EaseOut`，微反馈使用 `QuadraticEase EaseOut`。
- `ReaderWindow` 的窗口淡入从局部 `Storyboard` 改为 `MotionService.FadeIn()`。
- 阅读器翻页反馈从局部 `DoubleAnimation` 改为 `MotionService.PlayPageSwapFeedback()`。
- 批量导入进度遮罩接入 `MotionService.ShowWithFade()` / `HideWithFade()`，导入反馈不再是硬切显示/隐藏。
- 本轮只建立全局调度规范，不主动增加复杂动效，避免破坏阅读和大书库滚动的稳定性。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 `dotnet run` 启动级验证，窗口创建后保持运行。
- 已发布自包含测试版：`_release/0.3.62/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.63

- 补全真实日志系统：新增 `AppLogger`，启动时初始化 `.exe` 同级 `MangaReader_Data/logs/`，写入每日 `app-YYYYMMDD.log`。
- 增加 App 级异常兜底：捕获 UI Dispatcher、AppDomain 与未观察 Task 异常，未处理异常会生成 `crash-YYYYMMDD-HHmmss-fff.log`。
- 修正全局异常处理策略：未处理 UI 异常只记录一次 crash 快照并主动关闭应用，避免 `Handled=true` 后进入 WPF 布局/渲染异常循环并刷爆日志。
- 修复标签页高概率闪退：`TagChip.TypeText` 是只读展示属性，XAML 中的 `Run.Text` 绑定必须显式 `Mode=OneWay`，避免 WPF 尝试回写只读属性导致 `XamlParseException`。
- 已通过实际日志定位本次崩溃根因：`无法对 MangaReader.Native.Models.TagChip.TypeText 只读属性进行 TwoWay 或 OneWayToSource 绑定`。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过启动验证：程序保持运行，日志文件正常生成，未再新增同类 crash 文件。
- 已发布自包含测试版：`_release/0.3.63/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.64

- 修复 Tag 规则重启不一致：用户管理标签启动加载时严格尊重 `managed_tags.is_exclusive`，不再被分类名强行覆盖，避免“不互斥”标签重启后变成互斥。
- 修复标签管理页空输入新建失效：`新建候选标签` 在搜索框为空时也会打开 `TagCreateDialog`，用户可直接在弹窗内填写标签名、分类和互斥规则。
- 强化日志系统健壮性：`AppLogger` 的普通日志与 crash 快照写入现在会吞掉日志 IO / 权限异常，避免日志落盘失败反向触发全局崩溃。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.64/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.65

- 侧边栏底部的日志/存档信息改为可切换显示，不再常驻占位。
- 日志入口进一步收敛为短胶囊按钮：默认显示 `日志`，展开后切换为 `收起日志`，降低侧边栏视觉噪音。
- 展开的信息块继续保留 `本地存档已启用`、`MangaReader_Data` 存档说明和运行状态文案，兼顾可见性与沉浸感。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.65/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.66

- 右侧详情区改为贴右侧浮层展开，不再通过修改 `DetailColumn.Width` 挤压中间书库列宽；书架布局保持稳定，详情区以自然展开方式覆盖在主内容右侧。
- 修复书库点击闪退：通过 `AppLogger` 实际日志确认崩溃根因是标签管理模板里的 `TagChip.TypeText` 展示绑定在 WPF 模板测量阶段触发 `XamlParseException`，而不是详情浮层布局本身。
- 将 `TypeText` 的显示从依赖 `Run` 的内联绑定改为普通 `TextBlock` 文本拼接，避免 WPF 对只读展示属性走到错误的回写绑定路径。
- 已完成实际复现、日志定位、修复后复测，`_release/0.3.66/MangaReader.Native.exe` 中点击“书库”后可正常进入书库页。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.66/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.68

- 去掉书库卡片外层常驻的浅色矩形壳：`LibraryListItem` 的 hover 容器不再额外铺底，卡片根 `Border` 改为透明，避免封面外再套一层不必要的圆角矩形。
- 修正导入窗口四角发灰与阴影缺失：`ImportFolderDialog` 和 `AuthorBatchImportDialog` 改为“外层阴影板 + 内层内容板”结构，阴影不再被窗口边界裁掉，四角透明过渡也更干净。
- 本轮只处理视觉宿主层，不改动导入流程与书库数据逻辑，避免影响现有导入稳定性。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.68/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.69

- 右侧书籍详情抽屉改为完全不透明白底，取消半透明背景，避免封面/列表内容透到详情面板下方造成阅读干扰。
- 新增右侧详情抽屉的侧拉/侧收按钮：选中书籍后显示侧边胶囊按钮，可收起详情面板保留书库浏览空间，再次点击可滑入展开。
- 书库卡片恢复轻量选中反馈：轻点书籍后允许出现轻微暗色背景块，用于明确当前选中对象，但不再使用系统默认选中样式。
- 书库卡片接入统一动效：Hover 时轻微放大，按下时轻微压缩，释放后回弹；双击仍保持打开书本。
- `MotionService` 新增 `ScaleTo()`、`PressBounce()`、`ShowDrawer()`、`HideDrawer()`，本轮新增动效统一经全局调度入口实现，没有在窗口层散写 `Storyboard`。
- 阅读器首图加载从构造函数同步解码改为窗口显示后异步解码，避免打开大图漫画时窗口未显示、UI 看起来像卡死；打开阅读器和加载页面增加 `reader-open` / `reader-load-page` 日志。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.69/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.70

- 修复导入书本后闪退：日志确认根因不是导入写库，而是导入后鼠标进入新书卡片触发 Hover 动效时，发布版中的 `ScaleTransform` 已被 WPF 冻结，导致 `BeginAnimation(ScaleX)` 抛出 `InvalidOperationException`。
- `MotionService.EnsureScaleTransform()` 和 `EnsureTranslateTransform()` 增加冻结对象保护：遇到 `IsFrozen` 的 Transform 时保留当前数值并替换为新的可动画实例，避免 hover、按压回弹、详情抽屉滑入滑出因为冻结对象导致 UI 线程崩溃。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.70/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.71

- 修复阅读器打开透明 PNG / 贴图类图片时“全黑”的观感问题：阅读器图片承载层增加浅色实底，透明区域不再直接透出黑色阅读背景。
- 修复书库虚拟化面板在导入、打开阅读器、返回书库等布局刷新期间的空容器风险：`VirtualizingWrapPanel.RealizeItems()` 对 `GenerateNext()` 返回空对象做保护，避免 UI 线程 `NullReferenceException`。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.71/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.72

- 继续做 bug 巡检：补强 `VirtualizingWrapPanel.MakeVisible()`，不再直接把 `Visual` 强转为 `UIElement`，避免 WPF 滚动定位传入非容器视觉对象时产生异常。
- 修复导入入口的顶层异常风险：文件选择导入、书库拖拽导入、书库空白区域拖拽导入统一走 `TryImportSelectedFoldersAsync()`，导入链路的未预期 IO/权限/数据库异常会写入日志并在状态栏提示，不再直接触发全局崩溃退出。
- 导入失败时会主动收起导入进度遮罩和拖拽遮罩，避免失败后 UI 停留在“正在导入”状态。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.72/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.73

- 修复点击“书库”直接闪退：实际复现确认根因是 `VirtualizingWrapPanel.RealizeItems()` 在容器生成器尚未就绪时访问 `ItemContainerGenerator`，导致 UI 线程 `NullReferenceException`。
- `VirtualizingWrapPanel` 在 `ArrangeOverride()`、`RealizeItems()`、`CleanUpItems()` 中增加生成器空值保护；无效索引范围会直接跳过本轮布局，等待下一次 WPF 测量。
- 同步补强 `TagChip.TypeText` 展示绑定，显式声明 `Mode=OneWay`，避免只读展示属性再次被 WPF 模板路径误判为可回写绑定。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已通过 UI Automation 自动点击“书库”复测：进程保持响应，未生成 `crash-*.log`，退出码为 0。
- 已发布自包含测试版：`_release/0.3.73/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.74

- 增加软件数据目录指定能力：`app.db`、封面缓存、备份和日志不再只能固定放在 `.exe` 同级 `MangaReader_Data`。
- 新增 `AppStorage` 数据位置配置：软件同级 `MangaReader_DataLocation.txt` 只保存自定义数据根目录路径；该文件已加入 `.gitignore`，不会上传到 Git。
- 侧边栏日志面板新增 `打开数据` 和 `指定数据` 两个入口，和漫画库导入入口物理分离，避免把漫画源文件夹与软件保存文件混为一类。
- 数据目录切换采用“下次启动生效”策略，不在运行时热切换数据库连接，避免正在写库时迁移导致 `app.db` / WAL 损坏。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.74/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.75

- 修复阅读器交互“像完全失效”的问题：阅读区普通左键单击不再进入长按放大和鼠标捕获，而是恢复为漫画阅读器的基础翻页行为。
- 阅读区左侧约 36% 区域单击上一页，右侧区域单击下一页；双击不再用于隐藏/显示阅读器 UI，避免快速翻页时误触发。
- 长按放大保留但改为鼠标右键长按触发，避免和普通阅读点击冲突。
- 右侧详情面板新增显式 `开始阅读` 按钮，除双击卡片和首页卡片外提供稳定的单一可见入口。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.75/MangaReader.Native.exe`。

### 2026-06-12 原生软件 0.3.80

- 新增自动更新模块：侧边栏工具区增加 `检查更新`，主程序通过 GitHub latest Release 检查新版本，下载 Windows x64 zip 更新包。
- 新增独立更新器项目 `MangaReader.Updater`：主程序下载完成后启动更新器并退出，更新器等待主进程结束后替换程序目录文件，再重启 `MangaReader.Native.exe`。
- 更新器替换文件时显式跳过 `MangaReader_Data` 和 `MangaReader_DataLocation.txt`，避免覆盖用户数据库、封面缓存、备份、日志和自定义数据路径。
- `MangaReader.Native.csproj` 增加版本号 `0.3.80`，发布主程序时会自动把 `Updater/MangaReader.Updater.exe` 打入发布目录。
- 新增 GitHub Actions Release 流程：推送 `v0.3.xx` Tag 后自动构建自包含 Windows x64 发布包，并上传 `MangaReader-win-x64-v0.3.xx.zip` 到 GitHub Release，供软件内更新下载。
- 修正阅读器中心提示层策略：正常翻页不再显示“正在读取页面”遮挡画面，中心提示仅在图片读取失败时出现。
- 已通过 `dotnet build`，主程序与更新器均 0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.80/MangaReader.Native.exe`，发布目录包含 `Updater/MangaReader.Updater.exe`。

### 2026-06-12 原生软件 0.3.81

- 修复“无法指定数据路径”的可用性回归：`指定数据` 不再只藏在日志折叠块中，侧边栏工具区增加常驻入口。
- 修复指定数据目录后选择立即重启时的启动路径错误：改用 `Environment.ProcessPath` 启动当前 exe，避免用程序集 `.dll` 路径导致重启失败或不生效。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-12 原生软件 0.3.82

- 修复作者改名窗口输入框不可见：`RenameDialog` 高度从 280 提升到 430，并清理多余行定义，确保旧名称、新名称输入框和确认按钮完整显示。
- 侧边栏工具入口调整：移除低价值的 `同步书架` 按钮；`打开数据` 和 `指定数据` 移到工具区常驻显示，不再藏在日志折叠块内。
- `StatusText` 从日志折叠块中移出，检查更新、导入、备份等反馈即使不展开日志也能看到。
- 主页展示数量收紧：`继续阅读` 只显示 3 本；`之前读过` 只显示 4 本。
- `最近加入` 移除底部滚动条，鼠标悬停在该区域滚轮优先转换为横向滚动。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-12 原生软件 0.3.83

- 修复 `检查更新` 体验：发现新版本时先弹确认框，用户确认后才下载、关闭主程序并交给 `MangaReader.Updater.exe` 替换文件，避免“点了就闪退”的误判。
- 更新检查异常兜底扩大为全异常捕获，GitHub 403/404、Release 缺少 zip、网络超时、JSON 字段缺失等情况只写日志并在状态栏显示失败，不再穿透 UI 线程导致崩溃。
- `UpdateService` 兼容 GitHub Release `assets` 为空的情况，明确提示“没有可下载的 zip 更新包”。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.83/MangaReader.Native.exe`，发布目录包含 `Updater/MangaReader.Updater.exe`。

### 2026-06-13 原生软件 0.3.84

- 删除书籍详情只读区的 `角色名称` 展示，避免详情页继续暴露低价值字段。
- 删除书库筛选栏的 `编辑作者` 按钮，作者批量改名保留在独立作者页，避免同一功能入口重复。
- 书卡阅读信息合并为单行：`未读/在读/读过 n 次 · 当前页 / 总页`，不再把状态、页数、阅读次数拆成多行造成语义冲突。
- 增加收藏即时按钮，书籍详情页可直接收藏/取消收藏；书库卡片、首页卡片右上角显示星标，收藏状态可在浏览时直接识别。
- 性能优化：排序与筛选刷新合并为单次 `CollectionView.Refresh()`，收藏/阅读次数更新复用统一刷新链路，减少大书库下不必要的 UI 重刷。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.84/MangaReader.Native.exe`，发布目录包含 `Updater/MangaReader.Updater.exe`。

### 2026-06-13 原生软件 0.3.85

- 修复详情抽屉右上角视觉错位：详情头部改为稳定的 Grid 布局，收藏与编辑按钮统一对齐；详情滚动条改为隐藏显示，保留滚轮滚动能力，避免系统默认灰色滚动条破坏圆角区域。
- 编辑界面新增 `书名` 输入框，保存信息时会写回 `books.title`，支持对漫画重命名。
- 书库统计从独立大卡片区移入筛选块右上角，改为紧凑胶囊摘要，释放书库首屏垂直空间。
- 性能/产品优化：大书库继续使用虚拟化多列面板；统计摘要随专注浏览一起折叠，减少滚动时顶部 UI 的布局负担和视觉占用。
- 补充备份文件夹使用说明：`立即备份` 会复制当前数据库到 `MangaReader_Data/backups/`；`打开备份` 用于查看这些数据库快照。恢复时关闭软件，将需要恢复的备份文件复制并重命名为数据目录中的 `app.db`。
- 修正旧文档中过时的“书库筛选栏编辑作者”描述，当前作者批量改名入口以独立作者页为准。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.85/MangaReader.Native.exe`，发布目录包含 `Updater/MangaReader.Updater.exe`。

### 2026-06-13 原生软件 0.3.86

- 重构更新策略为“本地优先，GitHub 兜底”，适配国内网络与开发机手动 `git pull` 的真实使用习惯。
- `检查更新` 现在先查找本机 `_release/更高版本/MangaReader.Native.exe`，再查找 `_release/` 或 `updates/` 下的更高版本 zip；命中后直接交给 `MangaReader.Updater.exe` 替换当前运行目录。
- 如果用户已经手动 `git pull`，且源码 `MangaReader.Native.csproj` 版本号高于当前运行版本，软件会在确认后本地执行 `dotnet publish -c Release -r win-x64 --self-contained true` 到数据目录 `updates/local-build-*`，再用该发布目录更新自身。
- GitHub latest Release 只作为最后兜底；本地没有更新包且本地源码未升版本时才访问 GitHub。
- `MangaReader.Updater.exe` 支持 zip 更新包和本地发布目录两种输入；仍会跳过 `MangaReader_Data` 与 `MangaReader_DataLocation.txt`，避免覆盖用户数据库和数据目录设置。
- README 补充开发机推荐流程：先 `git pull`，再打开旧版 exe 点 `检查更新`；本地源码更新路径需要 .NET 8 SDK。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.86/MangaReader.Native.exe`，发布目录包含 `Updater/MangaReader.Updater.exe`。

## 15. 架构审查与优化建议

> 基于 0.3.45 代码实际走读形成的审查记录，并在 0.3.46 - 0.3.54 后更新执行状态。

### 15.1 最大问题：MainWindow 是一个 God Class

`MainWindow.xaml.cs` 目前约 2,100 行，承担了以下所有职责：

- 书库加载与扫描
- 批量导入与智能 Tag 推断
- Tag 的增删改查、重命名、批量更新
- 筛选（作者、状态、Tag、搜索）与排序
- 主页书架数据聚合
- 11 个 ObservableCollection 的状态管理
- 导航视图切换（主页 / 书库 / 标签）
- 阅读窗口的启动

**影响：** 任何一个功能的小改动都可能触碰到毫不相关的其他逻辑。目前不崩溃靠的是迭代足够快，一旦要加语言切换、主题、设置页，这个文件会变得无法维护。

**方向：** 逐步提取 ViewModel 和 Service 层。WPF 不要求 MVVM 完全纯粹，但至少应该把 **数据操作** 和 **UI 事件响应** 拆开。

---

### 15.2 Tag 系统存在 O(N²) 性能瓶颈（已落实）

`CreateTagChip()` 为每一个 Tag 芯片都调用了 `GetTagUsageCount()` 和 `GetTagBooks()`，这两个方法都是在内存的 `Books` 集合里遍历全部书籍来统计的。

`RefreshVisibleTags()` 和 `RefreshTagManagementItems()` 每次刷新都会重复触发这个逻辑。在筛选、导入、Tag 变更后这几个方法会被连续调用多次，等同于：

```
调用次数 = Tag 数量 × 全部书籍数 × 每次操作触发的刷新次数
```

书库规模达到几千本 + 几十个 Tag 之后，筛选操作会出现明显卡顿。

**执行状态：** 已在 0.3.46 中引入 `tag -> book[]` 索引缓存，`GetTagUsageCount()` 与 `GetTagBooks()` 不再为每个 Tag 反复遍历全部书库。

---

### 15.3 每次操作后连续执行五次全量刷新（已落实）

书库筛选或数据变更后，当前代码会依次调用：

```
RefreshVisibleTags()
RefreshTagManagementItems()
RefreshAuthorFilters()
ApplyBookSort()
RefreshBookFilter()
```

这五步都是对 `Books` 集合的独立遍历，而且它们经常被同一个用户动作连续触发。等于对几千本书做了五遍全量扫描。

**执行状态：** 已在 0.3.47 中新增 `RefreshLibraryViews()` 统一入口。导入、保存、隐藏/删除、拖拽添加 Tag、编辑/删除 Tag 等高频路径已收敛到统一刷新链。纯搜索输入仍保留局部刷新，避免过度刷新。

---

### 15.4 SQLite 连接与 WAL（部分落实）

`LibraryDatabase` 目前的 `Open()` 方法每次操作都新建一个 `SqliteConnection`。虽然 SQLite 有内置连接池，但每次的连接打开/关闭开销在批量导入时仍然可观。

**执行状态：** 已在 0.3.46 中启用 `PRAGMA journal_mode=WAL` 与 `busy_timeout`。暂不持有全局长连接，避免在当前快速迭代阶段引入连接生命周期和文件锁复杂度；批量写入继续优先依靠事务保证一致性。

---

### 15.5 缺少关键数据库索引（已落实基础版）

`books` 表目前没有索引，全部筛选都靠把所有书读出来后在 C# 层过滤。以下列是高频筛选字段，应该加索引：

- `author`
- `reading_status`
- `is_favorite`
- `is_hidden`

**执行状态：** 已在 0.3.46 中为 `author / reading_status / is_favorite / is_hidden / folder_path` 创建索引。当前筛选主体仍在内存集合中完成，索引主要服务后续 SQL 下推和启动加载。

---

### 15.6 封面缓存没有淘汰机制（后续观察）

`CoverCache.LoadOrCreate()` 返回 `BitmapImage` 之后没有释放机制。加载几千张封面缩略图后内存占用会持续增长，从不回收。

**后续方向：** 暂不立即做 WeakReference 或虚拟化重构。当前更重要的是保持封面视觉一致性和导入稳定性。等书库规模达到数千本、内存占用成为真实问题后，再优先做可视区懒加载与滚出释放。

---

### 15.7 Tag 颜色逻辑在两处各自维护（已落实）

`MainWindow.xaml.cs` 的 `TagPresets` 硬编码了分类色，`MangaBook.cs` 的 `TagColor()` 方法也有一套独立的颜色映射。两套逻辑分散，后续修改颜色时容易漏改一处。

**执行状态：** 已在 0.3.46 中抽出 `TagCatalog`，书库卡片、Tag 池、已选 Tag、标签管理页共用统一分类与颜色来源。

---

### 15.8 批量导入的错误处理不健全（已落实）

作者批量导入中，如果某一本漫画文件夹在处理过程中发生异常（文件锁定、权限不足等），整个导入任务会失败，没有"跳过这本、继续导入其余"的能力，也没有失败报告。

**执行状态：** 已在 0.3.46 中改为逐本捕获异常。单本失败不再中断整批导入，导入结束后会给出成功/失败数量与失败原因。

---

### 15.9 服务之间没有接口隔离（暂缓）

当前 `LibraryDatabase`、`LibraryScanner`、`CoverCache` 都是直接 `new()` 出来并通过字段持有，没有抽取接口。这意味着任何涉及文件系统或数据库的逻辑都无法在不启动真实环境的情况下验证。

**后续方向：** 暂缓。当前 UI 与数据模型仍在快速演进，强行抽接口容易让简单功能变成架构工作。后续应按实际边界渐进拆分，例如先提取 `ImportService`、`TagService`、`LibraryFilterService`，再考虑接口和测试替身。

---

### 15.10 架构审查执行状态

| 状态 | 项目 | 处理结果 |
|------|------|----------|
| 已落实 | SQLite WAL / busy timeout | 0.3.46 已启用 |
| 已落实 | 高频字段索引 | 0.3.46 已建立基础索引 |
| 已落实 | Tag 统计缓存 | 0.3.46 已建立 `tag -> book[]` 索引 |
| 已落实 | Tag 颜色统一 | 0.3.46 已抽出 `TagCatalog` |
| 已落实 | 批量导入错误处理 | 0.3.46 已支持逐本失败隔离与报告 |
| 已落实 | 刷新合批 | 0.3.47 已新增 `RefreshLibraryViews()` |
| 观察 | 封面缓存淘汰 | 等大规模书库或内存压力真实出现后再做 |
| 暂缓 | MainWindow 拆分 | 后续按功能域渐进抽 Service，不做一次性 MVVM 大重构 |
| 暂缓 | Service 接口隔离 | 待核心流程稳定后再引入接口和测试替身 |

### 15.11 当前后续优先级

- `P0`：继续修 UI 违和点，尤其是书库卡片、详情面板、Tag 与漫画卡片之间的空间关系。
- `P0`：继续保证 Tag 数据安全，任何扫描、同步、再次导入都不得覆盖用户手工 Tag。
- `P1`：把导入流程体验继续打磨，包括拖拽反馈、失败报告、批量确认窗口的信息密度。
- `P1`：按功能域小步拆分 `MainWindow.xaml.cs`，优先抽 `ImportService` 和 `TagService`，但不做一次性大重构。
- `P2`：当书库规模变大后，再处理封面懒加载、缓存淘汰和数据库筛选 SQL 下推。

---

## 16. 当前结论

建议把本项目做成一个“干净、离线、文件夹驱动”的漫画书库软件，而不是综合阅读器。

最关键的产品边界：

- 不联网。
- 当前不支持压缩包，后续视真实资源形态再评估 `.zip / .cbz`。
- 不做云同步。
- 不做复杂在线元数据。
- 只围绕本地漫画文件夹、作者、标签、封面、阅读体验打磨。

技术上当前采用 WPF / .NET 8 + SQLite，以获得真正的 Windows 原生 `.exe` 体验。

---

## 17. 深度走读补充（逻辑正确性与隐患）

> 本节聚焦此前未覆盖的四个文件：`BatchImportAnalyzer`、`ReaderWindow`、`MangaBook`、`MainWindow`（异步/资源部分）。以正确性 bug 和隐患为主，非代码风格问题。

### 17.1 BatchImportAnalyzer：图像分析逻辑有多处判断偏差

#### 超大图可能触发 OOM

`AnalyzeImage()` 先把图像转为 BGRA 格式，再分配 `stride × height` 大小的字节数组进行像素遍历（`BatchImportAnalyzer.cs:188-189`）。如果图片是 6000×8000 px（并不罕见），数组大小 = `6000 × 4 × 8000 = 192 MB`，连续内存分配在低内存环境下会直接 OOM。

**修法：** 在 `AnalyzeImage()` 入口处把图片 resize 到不超过 400×600 的分析尺寸，分析颜色不需要原始分辨率。

#### 横图判断阈值不一致

`BatchImportAnalyzer.cs:185` 用 `PixelWidth > PixelHeight`（宽略大于高即触发）判定为横图→CG 类型，而 `ReaderWindow.xaml.cs:214` 用 `PixelWidth > PixelHeight * 1.15` 来识别横图跨页。两处标准不统一，导致宽高比接近 1:1 的图片在分析阶段被误标为 CG，在阅读器里却不触发跨页。

**修法：** 统一阈值为 1.2 或以上，写入常量共享。

#### 画质阈值偏低，现代扫图会被低估

`BatchImportAnalyzer.cs:9-10` 把 1 MB 以上定为高清、3 MB 以上定为超清。现代高清漫画扫图单页通常在 2-5 MB，3 MB 的超清门槛偏低。如果用户的书库以现代扫图为主，几乎每本都会被标超清，失去区分意义。

**修法：** 调整为高清 ≥ 2 MB，超清 ≥ 5 MB，并加注释说明这两个值来自主观经验，未来可暴露为配置项。

#### 采样重复：书只有一页时同一图片被分析两次

`PickSamplePages()` 在 `pages.Count == 1` 时，`start`、`mid`、`end` 三个索引都等于 0，`yield return` 输出三次同一页（`BatchImportAnalyzer.cs:173-177`）。多次分析同一张图对结果没有影响，但浪费了两次图像解码。

**修法：** 在 yield 前去重：只输出 distinct 的索引。

---

### 17.2 ReaderWindow：两个运行时资源未释放

#### 计时器在窗口关闭时不停止

`_controlsRevealTimer` 是在构造函数里创建的 `DispatcherTimer`（`ReaderWindow.xaml.cs:16`），`Closing` 事件只调用了 `SaveCurrentProgress()` 和 `SaveWheelModePreference()`，没有 `_controlsRevealTimer.Stop()`（`ReaderWindow.xaml.cs:54-58`）。在某些情况下，窗口关闭后计时器回调仍可能触发，访问已销毁的控件。

**修法：** 在 `Closing` 里加一行 `_controlsRevealTimer.Stop()`。

#### 长按放大时 Mouse.Capture 没有 finally 保护

`ReaderScrollViewer_PreviewMouseLeftButtonDown`（`ReaderWindow.xaml.cs:358`）在 `UpdateHoldZoom` 可能抛异常的情况下，`Mouse.Capture(null)` 只在 `ReleaseHoldZoom` 里调用。如果 `UpdateHoldZoom` 抛出，鼠标捕获永远不会释放，用户会发现鼠标被"锁"在阅读器里点哪哪不响应。

**修法：** 在 `PreviewMouseLeftButtonDown` 里用 try-finally 包裹，确保 `Mouse.Capture(null)` 一定执行。

#### 阅读模式用字符串匹配 ComboBox 文本来判断

`IsDoublePageMode`（`ReaderWindow.xaml.cs:211`）和 `IsRightToLeftMode`（`ReaderWindow.xaml.cs:212`）都通过比对 ComboBoxItem 的 `.Content.ToString()` 是否等于 `"双页"` / `"从右到左"` 来判断当前模式。这意味着修改 XAML 里的 ComboBoxItem 文本就会静默破坏阅读逻辑，编译期没有任何提示。

**修法：** ComboBox 用枚举 Tag 绑定，判断逻辑读 Tag 而不是读显示文字。

---

### 17.3 MangaBook：一个溢出 bug

`BookStyleIndex`（`MangaBook.cs:103`）在 `BookStyle < 0` 时用 `Math.Abs(Id.GetHashCode()) % 4` 决定默认样式。`GetHashCode()` 在极端情况下可以返回 `int.MinValue`，而 `Math.Abs(int.MinValue)` 会抛 `OverflowException`（C# checked 模式外不会，但这是一个经典陷阱）。

**修法：** 改为 `(Id.GetHashCode() & 0x7FFFFFFF) % 4`，不用 `Math.Abs`，位运算更安全。

---

### 17.4 MainWindow：异步异常会静默崩溃

`MainWindow_Loaded`（`MainWindow.xaml.cs:73`）是 `async void`。如果内部的 `ScanRootsAsync()` 抛出未捕获的异常，异常会传递到 `SynchronizationContext`，大概率直接终止整个应用，且不显示任何提示。用户看到的现象是：软件启动后直接消失。

**修法：** 给 `MainWindow_Loaded` 加顶层 try-catch，或者把核心逻辑抽到一个返回 `Task` 的方法里，`async void` 事件处理器只做 `await` + catch。

---

### 17.5 书架虚拟化缓存偏小，快速滚动时封面会闪白

`MainWindow.xaml:1034-1035`：
```xml
VirtualizingPanel.CacheLength="2"
VirtualizingPanel.CacheLengthUnit="Page"
```
每次只预加载上下各 2 页的书籍卡片。书架密度高时一"页"大约有 15-20 张卡片，稍微快速滚动就会超出缓存范围，触发封面图片重新解码，出现短暂白框。

**修法：** 改为 `CacheLength="4"` 减少快速滚动时的重建频率。

---

### 17.6 本节优先级汇总

| 优先级 | 问题 | 文件 | 严重程度 |
|--------|------|------|----------|
| P1 | BatchImportAnalyzer 超大图 OOM（§17.1）| `BatchImportAnalyzer.cs:188` | 可崩溃 |
| P1 | `async void` 顶层未捕获异常（§17.4）| `MainWindow.xaml.cs:73` | 可崩溃 |
| P2 | 鼠标捕获未 finally 释放（§17.2）| `ReaderWindow.xaml.cs:358` | 操作卡死 |
| P2 | 计时器关窗未停止（§17.2）| `ReaderWindow.xaml.cs:54` | 潜在崩溃 |
| P2 | 横图阈值两处不一致（§17.1）| `BatchImportAnalyzer.cs:185` | 分类不准 |
| P3 | BookStyleIndex Math.Abs 溢出（§17.3）| `MangaBook.cs:103` | 极小概率崩溃 |
| P3 | 阅读模式靠字符串匹配判断（§17.2）| `ReaderWindow.xaml.cs:211` | 静默 bug 风险 |
| P3 | 书架虚拟化缓存过小（§17.5）| `MainWindow.xaml:1034` | 滚动体验差 |
| P3 | 画质阈值偏低（§17.1）| `BatchImportAnalyzer.cs:9` | 标注失真 |

### 17.7 0.3.58 执行状态

- 已修复：`ReaderWindow` 关闭时停止 `_controlsRevealTimer`。
- 已修复：长按放大入口异常时释放鼠标捕获，避免鼠标被阅读器捕获后卡死。
- 已修复：`BatchImportAnalyzer` 采样索引去重、横图阈值统一为 `1.2`、画质阈值调整为 `2MB / 5MB`。
- 已确认并强化：`AnalyzeImage()` 已使用缩略解码，本轮显式常量化分析尺寸，避免误读为原图级像素分析。
- 已修复：`MangaBook.BookStyleIndex` 改用位掩码，规避 `Math.Abs(int.MinValue)` 极端溢出。
- 已修复：`MainWindow_Loaded` 增加顶层异常保护，启动扫描异常不再直接终止进程。
- 已修复：书架虚拟化缓存从 `2` 提升到 `4`，改善快速滚动封面闪白。
- 暂缓：阅读模式改枚举 Tag 绑定。当前风险较低，后续做阅读器设置结构化时一起处理，避免本轮扩大 UI 行为改动。

### 17.8 代码走读核查（2026-06-12）

对 0.3.58 版本代码进行完整走读，核实各轮记录问题的实际状态。

**已确认解决：**

| 项目 | 确认位置 |
|------|----------|
| SQLite WAL 模式 | `LibraryDatabase.cs:32` |
| 数据库 5 个索引（author / reading_status / is_favorite / is_hidden / folder_path）| `LibraryDatabase.cs:80-84` |
| Tag 统计 O(N²) → 索引字典查表 | `RebuildTagIndex()` line 1882，在 `RefreshLibraryViews` 内按需调用 |
| 五次全量刷新 → 统一入口 | `RefreshLibraryViews()` line 1171，参数化选择性刷新 |
| 批量导入逐本错误隔离 | 已逐本 try-catch，单本失败不中断整批 |
| `MainWindow_Loaded` 顶层异常保护 | `MainWindow.xaml.cs:75-91` |
| `_controlsRevealTimer.Stop()` | `ReaderWindow.xaml.cs:56` |
| Mouse.Capture 释放保护 | `ReaderWindow.xaml.cs:359-369` try-catch + ReleaseHoldZoom |
| `AnalyzeImage` 图片尺寸上限 | `AnalysisDecodePixelWidth = 160`，加载时限制 |
| `BookStyleIndex` 溢出修复 | `MangaBook.cs:103` 改用位掩码 `& 0x7FFFFFFF` |
| 书架虚拟化缓存 | `MainWindow.xaml:1034` `CacheLength="4"` |
| TagCatalog 统一颜色 | `TagCatalog.cs` 已抽出，多处共用 |

**仍然存在，后续处理：**

| 项目 | 位置 | 影响 |
|------|------|------|
| 搜索框无防抖 | `MainWindow.xaml.cs:1057`（书库搜索）、`line 733`（Tag 搜索）| 中文输入每个候选字母触发全量过滤 |
| `SaveBookTagsBatch` N+1 SQL | `LibraryDatabase.cs:264-293` | 全局重命名 Tag 时每本一条独立 UPDATE |
| XAML 颜色 100+ 处内联 | `MainWindow.xaml` 全文 | 无 ResourceDictionary，后续做深色模式时必须解决 |
| ComboBox 两套重复模板 | `MainWindow.xaml:154-316` | 维护成本，行为改动需同步两份 |
| 阅读模式字符串匹配 | `ReaderWindow.xaml.cs:211-212` | 暂缓，等阅读器设置重构时处理 |

### 17.9 0.3.59 执行状态

- 已修复：作者批量导入复用分析阶段 `Pages`，避免候选确认后正式导入再次扫同一批目录。
- 已修复：`LibraryDatabase.UpsertBooksBatch()` 支持批量 `UPSERT`，扫描和作者批量导入不再逐本开启事务。
- 已修复：书库扫描把正常扫描结果与失效路径统一批量落库，降低 4000+ 书库 SQLite 写入开销。
- 已修复：`SaveBookTagsBatch()` 复用单个 `SqliteCommand`，不再每本创建命令对象。
- 已优化：`SaveBookAuthorsBatch()` 同步改为复用单个 `SqliteCommand`，保持批量元数据操作风格一致。
- 仍待处理：搜索框防抖。下一轮优先级高于 XAML 资源整理，因为它直接影响大书库输入体验。

### 17.10 0.3.60 执行状态

- 已修复：书库搜索框接入 `DispatcherTimer` 防抖，输入停止 `220ms` 后才刷新书库过滤。
- 已修复：书库 Tag 搜索和标签管理搜索同样接入防抖，减少大书库/大 Tag 集合下的重复刷新。
- 已修复：清空筛选、自动重置筛选和窗口关闭时停止防抖计时器，防止过期刷新回调干扰即时操作。
- 仍待处理：XAML 颜色资源整理、ComboBox 模板合并、阅读模式枚举化。这些属于维护性优化，优先级低于大书库性能链路。

---

## 18. 0.3.87 – 0.3.90 功能与修复记录（2026-06-13）

### 2026-06-13 原生软件 0.3.87

**主题：作者管理独立页面**

- 新增 `Models/AuthorItem.cs`：作者数据模型，持有 `Name`（作者名）和 `BookCount`（关联书籍数），实现 `INotifyPropertyChanged` 供 ItemsControl 数据绑定。
- 新增 `RenameDialog.xaml` / `RenameDialog.xaml.cs`：独立的重命名对话框窗口，展示旧名称（只读）、新名称输入框，确认时校验非空后将结果通过 `NewName` 属性返回，使用 `DialogResult = true` 语义与调用方约定。
- 主窗口侧边栏新增"作者"导航按钮（位于"标签"之后），点击跳转到 `AuthorsPagePanel`。
- 新增 `AuthorsPagePanel`：包含标题区、两列统计卡片（全部作者 / 关联书籍）、作者搜索框（防抖 220ms）、作者列表（每行显示名称、书籍数，含"筛选"与"改名"两个操作按钮）以及空状态提示。
- `MainWindow.xaml.cs` 新增：
  - `ObservableCollection<AuthorItem> AuthorManagerItems`：绑定到 `AuthorManagerList`。
  - `_authorSearchDebounceTimer`：220ms 防抖，接入 `ConfigureSearchDebounceTimers()` 和 `StopSearchDebounceTimers()`。
  - `ShowAuthorsView()` / `NavAuthors_Click()`：标准四面板切换 + `RefreshAuthorManagementItems()` + `UpdateNavigationVisuals()`。
  - `RefreshAuthorManagementItems(string? filter)`：从 `Books` 按 `Author` 字段 `GroupBy`（忽略大小写）、计数、按名称排序后填充集合，同步更新统计文本和空状态可见性。
  - `RenameAuthor_Click()`：打开 `RenameDialog`，取消或名称未变时直接返回；确认后通过 `SaveBookAuthorsBatch()` 批量写库，再对 `Books` 内存集合原地更新并 `NotifyAll()`，最后刷新书库排序和作者页列表。
  - `FilterByAuthor_Click()`：切换到书库视图并调用 `SetAuthorFilter()` 设置作者筛选条件。
  - `SetAuthorFilter(string)`：在 `AuthorFilters` 中找到对应项后设置 `AuthorFilterBox.SelectedItem` 并调用 `RefreshBookFilter()`。
  - `UpdateNavigationVisuals()` 更新：新增 `AuthorsNavButton` 高亮逻辑。
  - `ShowHomeView()` / `ShowLibraryView()` / `ShowTagsView()` 各增加 `MotionService.HideWithFade(AuthorsPagePanel)` 调用，保证切换到其他页时作者页正确隐藏。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-13 原生软件 0.3.87.1

**主题：修复作者改名与首页书架布局**

- 修复 `RenameDialog.xaml` 高度不足导致输入框被截断，调整为 `Height="430"`，Grid 行定义改为 `Auto / * / Auto` 确保按钮始终可见。
- 修复首页"继续阅读"书架上限过大（8 本）导致 WrapPanel 在窄屏溢出，调整为 3 本；"之前读过"调整为 4 本，与卡片宽度在 1280px 窗口下对齐。
- 删除书库筛选栏中遗留的"编辑作者"按钮（`EditSelectedAuthor_Click`），该入口已由独立作者管理页替代；同步删除对应的 `EditSelectedAuthor_Click` handler，清理 ~50 行代码。

### 2026-06-13 原生软件 0.3.88

**主题：更新检查健壮性与详情面板交互优化**

- 修复 `UpdateService.cs`：`GitHubRelease.Assets` 字段标记为可空（`IReadOnlyList<GitHubAsset>?`），反序列化结果迭代前改为 `?? []` 兜底，防止 GitHub API 返回无 assets 的 release 时产生 `NullReferenceException`。
- 修复 `CheckForUpdates_Click`：新增 `_isCheckingForUpdates` 防重入标志，多次快速点击不会并发发起多个 HTTP 请求；检查完成后（无论成功/失败）重置标志并恢复按钮状态。
- 详情面板头部收藏按钮从 `FavoriteBox` CheckBox 改为独立 `ToggleFavoriteButton` Button，点击后即时写库、刷新 `Books` 筛选视图并更新状态栏，不依赖保存按钮。
- 收藏状态变更（新增 `ToggleFavorite_Click`）和阅读次数变更（`ChangeReadCount`）由 `ApplyBookSort()` 改为 `ApplyBookSort(refresh: false)` + `RefreshBookFilter()`，将排序与视图刷新解耦，避免大书库下单次操作触发不必要的全量重排。
- 书籍卡片与首页书架卡片右上角增加收藏星标（通过 `IsFavorite` 绑定可见性），收藏状态可在浏览时直接识别，无需打开详情。

### 2026-06-13 原生软件 0.3.89

**主题：书库 Chrome 紧凑化与书名编辑支持**

- 书库统计从独立三列大卡片区（`LibraryMetricPanel`）迁移到筛选块右上角的紧凑文本摘要（`VisibleBookCountText` / `TotalBookCountText`），释放书库首屏约 120px 垂直高度。
- 书库统计文本格式调整：`TotalBookCountText` 改为 `"/ 共 N 本"` 紧凑格式，与当前列表计数并排展示。
- `LibraryMetricPanel` 随专注浏览模式一同折叠，减少滚动时顶部 UI 布局负担。
- 书籍详情编辑区新增 `TitleBox` 输入框，允许直接在应用内修改漫画书名（写入 `books.title`），保存前校验不可为空。
- `FillMetadataEditors()` 补充填写 `TitleBox.Text = book.Title`；`SetEditMode()` 把 `TitleBox` 加入 `IsReadOnly` 切换的 TextBox 数组，保证编辑/只读状态与其他字段一致。
- 专注浏览状态文本调整："统计卡片"改为"统计摘要"，与 UI 实际控件描述对齐。

### 2026-06-13 原生软件 0.3.90

**主题：本地优先更新策略**

- 重构 `UpdateService` 为三层查找链：
  1. 本地 `_release/` 目录：扫描所有子目录，找到含 `MangaReader.Native.exe` 且版本高于当前版本的最新发布目录，直接交给 `Updater` 复制替换，无需网络。
  2. 本地 zip：在 `_release/` 和 `updates/` 下查找版本号更高的 `.zip` 压缩包，解压后交给 `Updater`。
  3. GitHub latest Release（兜底）：前两步均无结果时，才发起 GitHub API 请求下载更新包。
- 新增"本地源码编译更新"路径：若用户已执行 `git pull` 且 `MangaReader.Native.csproj` 中声明的版本高于当前运行版本，软件在确认后自动执行 `dotnet publish -c Release -r win-x64 --self-contained true`，输出到数据目录 `updates/local-build-*`，再交给 `Updater` 更新自身。
- `MangaReader.Updater` 补充支持直接目录输入（不仅限于 zip），复用原更新包路径参数识别目录或 zip，逐文件复制替换，跳过 `MangaReader_Data/` 和 `MangaReader_DataLocation.txt`，避免覆盖用户数据。
- README 补充本地更新推荐流程：先 `git pull` 拉取源码，再打开旧版 exe 点击"检查更新"触发本地编译更新；该路径需要本机安装 .NET 8 SDK。
- 已通过 `dotnet build`，0 警告 0 错误。
- 仍待处理：XAML 颜色资源整理、ComboBox 模板合并、阅读模式枚举化。这些属于维护性优化，优先级低于大书库性能链路。

### 2026-06-13 原生软件 0.3.91

**主题：书库热路径性能优化**

- `RefreshBookFilter()` 不再自动调用 `RefreshHomeShelves()`。搜索防抖、Tag 筛选、作者筛选、状态筛选、收藏筛选只刷新书库可见集合与统计摘要；首页书架只在扫描/导入/阅读器关闭/元数据保存/收藏或阅读次数变化/切换到首页时刷新。
- `FilterBook()` 不再在每本书评估时读取 `BookSearchBox.Text`、`AuthorFilterBox.SelectedItem`、`StatusFilterBox.SelectedIndex`、收藏/隐藏勾选状态；改为在 `RefreshBookFilter()` 前缓存到 `_cachedSearchQuery`、`_cachedAuthorFilter`、`_cachedStatusFilter`、`_cachedFavoriteOnly`、`_cachedShowHidden` 和 `_cachedActiveTagFilters`。
- `RefreshShelfOverview()` 取消 `libraryBooks.ToList()` 和多次 `Count(predicate)`，改为一次遍历统计可见库总数、收藏、在读、已读；筛选摘要与空状态提示也使用缓存条件，避免重复访问 UI 控件。
- 新增 `RangeObservableCollection<T>`，提供 `AddRange()` 和 `ReplaceRange()`，用一次 `Reset` 通知代替逐本 `Add`；`Books` 与首页书架集合已切换到该集合。
- 扫描书库时将 `_database.LoadBooksByPath()` 移入 `Task.Run`，SQLite 读取不再卡 UI 线程；扫描结果和缺失书籍合并后通过 `Books.AddRange()` 一次通知 UI。
- 作者批量导入完成后新增书籍通过 `Books.AddRange()` 批量加入，避免 100+ 本作者导入时逐本触发布局刷新。
- `RefreshHomeShelves()` 的 `ReplaceBooks()` 增加序列相等检查：首页书架内容未变时不触发 `Clear/Add`，避免无意义布局。
- `RefreshTagManagementItems()` 将 `EnumerateKnownTags()` 结果缓存为局部列表，统计总数、已使用和独立标签时不再重复枚举。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.91/MangaReader.Native.exe`，发布目录包含 `Updater/MangaReader.Updater.exe`。

### 2026-06-13 原生软件 0.3.92

**主题：阅读器按钮高亮样式修正**

- 修复阅读器工具栏按钮点击/聚焦时出现系统默认正方形高亮的问题：阅读器 `Button` 样式统一设置 `Focusable="False"`、`IsTabStop="False"` 并移除焦点视觉。
- 调整阅读器按钮 hover/press 状态为暗色圆角胶囊内高亮，避免大面积浅蓝色块破坏沉浸感。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.92/MangaReader.Native.exe`，发布目录包含 `Updater/MangaReader.Updater.exe`。

### 2026-06-13 原生软件 0.3.93

**主题：阅读器适配模式、快捷键与背景色**

- 阅读器默认进入 `适应高度` 模式；`适高` / `适宽` 按钮一旦点击会保持亮色激活态，直到阅读器关闭或切换到另一种适配模式。
- 翻页后会继续应用当前适配模式，不再只在首图加载时执行一次适应高度。
- 删除“若干秒后自动隐藏 UI”的计时设计：用户打开 UI 后不会因为停留在某一页而自动消失；隐藏/显示只能由按钮、双击或快捷键触发。
- 整理阅读器固定快捷键：`W` 全屏、`E` 适应高度、`Q` 适应宽度、`D` 隐藏/显示 UI、`S` 单页/双页、`Z` 切换滚轮状态、`X` 退出、`C` 切换左右阅读顺序、`A` 切换背景色。
- 新增背景色循环：黑 / 白 / 纸色，工具栏提供“背景”按钮，快捷键 `A` 同步可用。
- 自定义翻页键默认值移除 `D` / `A`，避免与固定 UI/背景快捷键语义冲突；默认下一页为 `Right,Space`，上一页为 `Left`。
- “最后一页后前往下一本漫画”暂不落地，需要先确认下一本顺序规则。候选方案：当前书库筛选排序、同作者排序、导入时间排序、文件夹自然顺序。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.93/MangaReader.Native.exe`，发布目录包含 `Updater/MangaReader.Updater.exe`。

### 2026-06-13 原生软件 0.3.94

**主题：当前筛选顺序下一本与书库批量管理**

- 阅读器到最后一页时会询问是否前往下一本漫画；“下一本”使用当前书库视图的筛选条件与排序结果，不另起独立规则，保证和用户正在浏览的上下文一致。
- 书库卡片新增批量选择勾选状态，筛选区新增“全选当前”“清空选择”和已选数量摘要；“全选当前”只选择当前筛选/排序后的可见集合。
- 新增批量去前缀：输入一个书名前缀，只会修改以该前缀开头的选中漫画，适用于作者文件夹导入后子文件夹都带作者名前缀的情况。
- 新增批量应用卡片样式：选择样式 1-4 后统一写入选中漫画，不再逐本手动切换。
- 新增批量加 Tag / 减 Tag：加 Tag 会复用现有互斥 Tag 规则，减 Tag 可从选中漫画中移除指定标签。
- 批量写库增加事务化方法：批量标题、批量样式、批量 Tag 均在保存前创建备份，并在一次事务中写入。
- 书库顶部统计胶囊视觉收敛，减少红框区域那种突兀的硬边界感；批量管理控件放入筛选块下方的统一浅色胶囊区。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/0.3.94/MangaReader.Native.exe`，发布目录包含 `Updater/MangaReader.Updater.exe`。

### 2026-06-13 原生软件 0.3.94.1

**主题：批量管理交互修复与双页适高对齐**

- 批量管理从常驻工具条改为显式“多选管理”模式：未开启时书卡不显示选择控件，开启后才显示圆形选择按钮。
- 新增“退出多选”入口：关闭多选模式时会清空所有已选漫画，避免选择状态残留。
- 专注浏览/折叠筛选时，批量管理工具条和书卡圆形选择入口会随筛选控件一起收起，不再单独悬在书库顶部。
- 批量工具条改为更紧凑的低高度样式：按钮文案收敛为“全选 / 清空 / 去前缀 / 应用 / + Tag / - Tag”，降低溢出容器概率。
- 书卡选择入口去掉“选”字，只保留圆形按钮与选中勾号，减少卡面噪音。
- 阅读器双页模式下对左右页做同高归一化，再执行适应高度/宽度计算，缓解两页原始高度不同导致的适高显示不齐。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-13 原生软件 0.3.94.2

**主题：Tag / 作者管理集合刷新性能收敛**

- 继续检查大书库性能热路径，确认书本集合已经使用 `RangeObservableCollection<T>`，但可见 Tag 池、已选 Tag、标签管理列表、作者管理列表和作者筛选下拉仍是 `ObservableCollection<T>` 逐项刷新。
- 将 `VisibleTags`、`ActiveTagFilters`、`TagManagerItems`、`AuthorManagerItems`、`AuthorFilters` 统一切换为 `RangeObservableCollection<T>`。
- `RefreshVisibleTags()`、`RefreshActiveTagFilters()`、`RefreshTagManagementItems()`、`RefreshAuthorManagementItems()` 和 `RefreshAuthorFilters()` 改为 `ReplaceRange()` 一次性替换集合，避免大 Tag / 大作者集合下每次搜索、筛选、管理页刷新都触发大量 `Add` 通知和重复布局。
- 本轮不改变数据库结构、不改变筛选语义、不改变 UI 视觉，只压缩刷新通知数量。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-13 原生软件 0.3.94.3

**主题：UI 圆角规范收敛**

- 确认当前 XAML 不是单一圆角数值，而是存在多档硬编码；问题不在于“多个圆角”，而在于缺少语义 Token。
- 开发文档新增圆角规范：`RadiusClip / RadiusProgress / RadiusControl / RadiusPanel / RadiusField / RadiusTag / RadiusDialog / RadiusPill`。
- 主窗口资源层新增对应 `CornerRadius` Token，并先将按钮、输入框、下拉框、筛选控件、进度条、下拉项等高频样式接入资源。
- 设计结论：圆角不应全项目只用一个值；应按控件语义使用少量固定半径，禁止继续新增无语义的零散数值。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-13 原生软件 0.3.94.4

**主题：阅读器双页间距持久化与保存防抖**

- 阅读器底部工具栏新增“间距”滑块，范围 `0-80`，用于控制双页模式下左右两页之间的距离。
- 双页间距写入本地设置键 `reader.doublepage.gap`，重新打开阅读器会自动恢复；翻页、刷新页面、单双页切换都不会丢失当前间距。
- 翻页加载后会重新调用 `ApplyDoublePageGap()`，不再依赖 XAML 固定 `Margin=4`。
- 适宽计算改为读取左右两张图真实 `Margin`，双页间距变化后不会导致适宽比例计算偏差。
- 性能处理：拖动滑块时只更新 UI，不逐帧写 SQLite；停止调整后通过 `DispatcherTimer` 延迟 `260ms` 保存，关闭阅读器时再强制保存一次。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-13 原生软件 0.3.95

**主题：发布更新包**

- 主程序版本号更新为 `0.3.95`，确保本地优先更新能识别为高于 `0.3.94` 的新版本。
- 已发布自包含测试版：`_release/0.3.95/MangaReader.Native.exe`，发布目录包含 `Updater/MangaReader.Updater.exe`。
- 已打包 zip 更新包：`_release/MangaReader-win-x64-v0.3.95.zip`。

### 2026-06-13 原生软件 0.3.95.1

**主题：阅读器下一本确认层修复**

- 修复最后一页滚轮连续触发时会弹出多个“前往下一本”系统弹窗的问题。
- 移除阅读器里的系统 `MessageBox`，改为阅读器内置深色确认层，与工具栏、遮罩和按钮风格保持一致。
- 新增 `_isNextBookPromptOpen` 和 `_pendingNextBook` 状态，确认层打开期间重复触发“下一页”会直接忽略，不再堆叠弹窗。
- 确认层会拦截滚轮事件；`Esc` 或“留在当前页”关闭确认层，“前往下一本”沿用当前筛选顺序打开下一本。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-13 原生软件 0.3.95.2

**主题：阅读器即时反馈、筛选冻结与容量排序**

- 阅读器页切换取消 `PlayPageFade()` 透明度反馈，滚轮翻页不再叠加淡入动画；当前“不跟手”主要来自动画与图片异步解码叠加，先移除动画保证输入反馈直接。
- 书库筛选区不再因瀑布流滚动自动收起，只保留“专注浏览 / 展开筛选”按钮作为唯一入口；筛选开启时顶部筛选区域保持冻结显示。
- 首页“继续阅读”和紧凑书卡标题增加截断，长标题不再挤出卡片容器。
- 作品模型新增 `TotalBytes` 容量字段，扫描、批量作者导入和重定位都会计算图片文件总字节数并写入数据库 `books.total_bytes`。
- 资料库卡片元信息调整为一行：`状态 · 页数 · 容量`；容量小于 1G 显示 MB，超过 1G 显示 G。
- 书库排序移除“作者排序”，新增“页数排序”和“容量排序”；容量排序按原始字节数排序，不按显示单位字符串排序。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-13 原生软件 0.3.95.3

**主题：阅读器鼠标交互冲突修复**

- 修复阅读器左键双击翻页时会反复触发 UI 显示/隐藏的问题。
- 左键点击现在只负责按位置翻页，不再承担 UI 显隐切换；UI 显隐保留 `D` 快捷键、底部按钮和隐藏态提示入口。
- 临时按住放大从 `Ctrl + 左键` 调整为鼠标右键长按，松开右键恢复到原倍率。
- 缩放滑块继续保留，右键长按放大会以当前滑块倍率为基准临时放大，不破坏用户设置。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-13 原生软件 0.3.96

**主题：性能交互修复版本**

- 整合 `0.3.95.1` 到 `0.3.95.3` 的阅读器确认层、翻页即时反馈、筛选冻结、容量展示排序和鼠标交互修复。
- 主程序版本号更新为 `0.3.96`，用于 GitHub tag / Release 和本地优先更新识别。

### 2026-06-13 原生软件 0.3.96.1

**主题：首页继续阅读卡片修复**

- 修复首页“继续阅读”卡片内按钮被裁切、视觉变成圆形的问题。
- 将“继续阅读”按钮固定在卡片底部行，不再受标题、作者和进度文本挤压。
- “继续阅读”区域的三张作品卡片改为稳定单行横向排列，卡片宽度收敛为 `360`，间距收敛为 `12`。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-13 原生软件 0.3.96.2

**主题：阅读器页码显示与统计样式收敛**

- 阅读器页码不再被 `LoadPage()` 清空，顶部标题栏页码保持稳定显示。
- 阅读器底部工具栏新增页码显示，放在缩放比例前，非全屏模式下可直接看到当前页数。
- 隐藏 UI / 全屏阅读时，“显示 UI”提示下方新增当前页码，避免沉浸模式下完全丢失阅读位置。
- 书库顶部统计区去掉椭圆形浅色底，改为透明纯文本统计，只保留文字色和数字加粗。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-13 原生软件 0.3.96.3

**主题：AI 技术交接文档**

- 新增 `AI_TECHNICAL_HANDOFF.md`，面向后续接手的 AI / 开发者说明项目架构、关键文件、数据库迁移流程、书库性能红线、阅读器交互约束、UI 规范、自动更新发布流程和建议改动步骤。
- 文档明确记录当前硬约束：功能改动必须同步开发文档、书库筛选与首页刷新解耦、大集合使用 `RangeObservableCollection`、阅读器下一本遵循当前筛选排序、不要恢复作者排序、不要恢复左键双击隐藏 UI 等。
- 本文档用于降低后续 AI 接手成本，避免重复踩已有性能和交互问题。

### 2026-06-13 原生软件 0.3.97

**主题：阅读器底部工具栏精简**

- 移除底部工具栏的"上一页"和"下一页"按钮，翻页操作保留鼠标点击、键盘快捷键和滚轮三种入口，减少工具栏拥挤。
- 移除冗余的 `ZoomText` 文本（此前显示"适高 · 24% · 翻页"），适配模式由按钮亮色激活态表达，滚轮模式由下拉框直接可见，百分比由缩放滑块位置直觉反馈。
- 移除底部重复的 `BottomPageText` 页码显示，页码保留在顶部标题栏和隐藏态徽章两处。
- 双页间距滑块从 `150px` 缩短为 `80px`，保留"间距 N"文本提示，减少工具栏横向占用。
- 底部工具栏移除固定 `Width="1120"`，改为自适应宽度，控件更少后不再溢出。
- 清理 code-behind 中 `UpdateZoomText()` 方法及其全部调用点，清理 `PreviousPageButton.IsEnabled` / `NextPageButton.IsEnabled` 赋值。
- 新增项目级 `.claude/CLAUDE.md`，记录版本号更新、开发文档同步、性能红线、阅读器约束和 UI 规范等日常开发指令。
- 已通过 `dotnet build`，0 警告 0 错误。
### 2026-06-13 原生软件 0.3.97.1

**主题：更新软件图标**

- 新增 `MangaReader.Native/AppIcon.ico`，采用用户提供的书脊图书馆风格图片（四本书籍，白/浅蓝/灰配色）。
- ICO 文件含 48×48、32×32、16×16 多尺寸，支持 Windows 任务栏和文件资源管理器自动选取最佳分辨率。
- 在 `MangaReader.Native.csproj` 中配置 `<ApplicationIcon>AppIcon.ico</ApplicationIcon>`，发布时自动嵌入 EXE。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已重新发布自包含测试版：`_release/0.3.97.1/MangaReader.Native.exe`。

### 2026-06-14 原生软件 0.3.97.2

**主题：性能优化 — UI 线程同步 I/O 消除**

- 构造函数中 `_database.Initialize()` 移至 `MainWindow_Loaded`，用 `await Task.Run()` 包裹，窗口秒开不再被数据库 DDL 阻塞。
- 20+ 个事件处理方法的数据库写操作包裹 `await Task.Run()`：`SaveMetadata`、`SetCover`、`CycleBookStyle`、`ToggleFavorite`、`ChangeReadCount`、`HideBook`、`DeleteBook`、`ManualBackup`、`Relocate`、`SaveShortcuts`、`AddTag`、`TagChip`、`BatchRemoveTitlePrefix`、`BatchApplyStyle`、`BatchAddTag`、`BatchRemoveTag`、`BooksList_Drop`、`RenameAuthor`、`EditTagAcrossLibrary`、`DeleteTagAcrossLibrary`。
- `CoverCache.LoadOrCreate` 在 `SetCover_Click` 和 `Relocate_Click` 中包裹 `Task.Run`。
- `SumFileBytes` 和 `Directory.EnumerateFiles` 在 `Relocate_Click` 中包裹 `Task.Run`。
- `SaveCurrentProgress` 和 `UpsertManagedTag` 改为 fire-and-forget `Task.Run`。
- ReaderWindow 中 `SaveProgress`、`SaveShortcut`、`SaveDoublePageGapPreference` 全部包裹 `Task.Run`。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.97.3

**主题：性能优化 — NotifyAll 简化与 Brush 缓存**

- `MangaBook.NotifyAll()` 从 38 次单独 `PropertyChanged` 合并为 1 次 `PropertyChangedEventArgs("")`（WPF 识别空字符串为"所有属性变更"）。批量操作 UI 更新开销降低 38 倍。
- `MainWindow` 和 `ReaderWindow` 中反复创建的 `SolidColorBrush` + `ColorConverter.ConvertFromString` 改为 `static readonly` 缓存并 `Freeze()`。导航切换省 8+ 次反射解析+对象分配。
- `MangaBook.Pages` 从 `ObservableCollection<string>` 改为 `List<string>`。Pages 从未绑定 XAML，500 页漫画省 500 次 `CollectionChanged` 事件。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.97.4

**主题：性能优化 — 视图计数、虚拟化、视口感知解码**

- `_booksView.Cast<object>().Count()` 替换为 `Books.Count(FilterBook)` 直接过滤计数。
- `EnsureLibraryViewCanShowBooks` 改用 `Books.Any(FilterBook)` 短路求值。
- `GetVisibleBooks` 改用 `Books.Where(FilterBook)` 避免 `Cast<object>()` 装箱。
- `TagManagerList` / `AuthorManagerList` 从 `ItemsControl` 改为 `ListBox` + `VirtualizingStackPanel` + `Recycling`。
- 阅读器页面解码从固定 2600px 改为视口感知动态计算（viewport × zoom × 1.2, 下限 800 上限 2600），双页模式自动减半。
- `SumFileBytes` 去重提取到 `ImageLoader` 作为 `public static` 方法，`MainWindow` 和 `LibraryScanner` 共用。
- `AppLogger` 从 `File.AppendAllText` 改为 `StreamWriter` 缓存，避免每行日志都开关文件。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.97.5

**主题：图标生成管线 + 打包脚本 + 侧边栏按钮对齐**

- 新增 `icon.png` 源图 + `_scripts/gen_icon.py` 自动生成多尺寸 `AppIcon.ico`（256/128/64/48/32/16）。
- 新增 `pack.bat` / `pack.ps1` 一键打包脚本，支持 standalone（~60MB 自包含）和 runtime-dep（轻量需 .NET 8）两种模式。
- 侧边栏工具区"立即备份/备份路径"和"指定数据/数据路径"四按钮改用 `UniformGrid Columns="2"` 等宽对齐。
- 版本号升至 0.3.97。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98

**主题：标签/作者页虚拟化修复 + 标签管理增强 + 对话框风格统一 + 侧边栏图标**

- 标签管理页 / 作者管理页从 `ScrollViewer + StackPanel` 重构为 `Grid` 三行布局，ListBox 拥有自己的 ScrollViewer，虚拟化正常生效（之前外层 ScrollViewer 给无限高度导致虚拟化失效）。
- `TagCreateDialog` / `TagEditDialog` ComboBox 模板添加 `PART_EditableTextBox`，修复 `IsEditable="True"` 无效问题。下拉框动态填充已有标签组。
- 对话框四角透明渲染修复：采用与 `RenameDialog` 相同的双层 Border 方案（外层承载阴影，内层承载内容）。
- 对话框风格统一：从暖色奶油调（`#F3EDE4` 黄底 + `#211D19` 暗输入框）改为冷色浅调（`#FFFFFF` 白底 + `#F8FAFC` 浅灰输入框），与主 UI 一致。
- 颜色选择器重新设计：7 预设色球一行排列 + "+" 圆圈按钮打开系统颜色选择器，自定义颜色追加到第二行（最多 8 个）。
- 收藏星星极简风格：从 `#9A5A00` 厚重深棕改为 `#F59E0B` 明亮琥珀色。
- 侧边栏"漫画库"图标从硬编码紫色渐变 + "漫"字改为引用 `icon.png` 实际应用图标。
- 版本号升至 0.3.98。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.1

**主题：Bug 修复 — 标签分组筛选下拉框重复 + 瀑布流阅读状态不一致**

- 修复标签管理页「分组筛选」下拉框（`TagGroupFilterBox`）重复项：`_isRefreshingTagFilter` 防重入锁从 `Items` 重建后移到 `Items.Clear()` 之前，防止 `SelectionChanged` 递归触发 `RefreshTagManagementItems()` 反复重建 Items 导致重复。
- 分类名和作者名添加 `.Trim()`，防止空白字符差异造成的去重失效。
- 修复瀑布流书卡 `LibraryMetaText` 在 `ReadCount > 0` 但 `ReadingStatus = "未读"` 时仍显示"未读"的问题：将 `ReadingStatusText` 替换为 `ReadStateText`（优先显示"读过 N 次"），与详情面板和首页书架保持一致。
- 已通过 `dotnet build`，0 警告 0 错误。
- 已发布自包含测试版：`_release/MangaReader.Native.exe`。

### 2026-06-14 原生软件 0.3.98.2

**主题：性能收敛 + 样式 Token 收口**

- 修复标签管理页 / 作者管理页虚拟化配置：`ScrollViewer.CanContentScroll` 改回 `True`，避免 `VirtualizingStackPanel` 在长列表中退化。
- 书库视图刷新增加 `Dispatcher` 合并调度：同一轮 UI 事件内多次筛选、排序、卡片样式变更只触发一次 `_booksView.Refresh()`，降低大书库下的重复过滤和重排成本。
- 阅读器 `LoadPage` 增加 `CancellationTokenSource`：快速翻页时取消旧页面解码任务，并继续保留 request id 防旧图回写。
- 新增全局主题资源字典 `Themes/MangaTheme.xaml`，集中管理 `Brush.*`、`Color.*`、`Radius*` 和基础间距 Token。
- `App.xaml` 全局合并主题资源；主窗口和标签创建/编辑对话框的核心按钮、输入框、下拉框颜色与圆角改为引用统一 Token，减少样式分裂和后续维护成本。
- 主程序版本号更新为 `0.3.98.2`。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.3

**主题：圆角、滚动条、调色盘继续统一**

- 补齐 `RadiusColorSwatch`、`RadiusComboPopup`、`RadiusDialogShadow` 等语义圆角 Token，标签创建/编辑弹窗不再直接写 `18/23/32/36` 这类裸圆角值。
- 在 `MangaTheme.xaml` 中新增全局隐式 `ScrollBar` 模板：细轨道、胶囊滑块、悬停/拖拽态颜色统一，替换 WPF 默认滚动条观感。
- 调色盘色块抽成 `ColorSwatchBorderStyle`，添加自定义颜色按钮抽成 `AddColorSwatchBorderStyle`，两个标签弹窗共用同一套尺寸、圆角、边框和悬停反馈。
- 标签弹窗 ComboBox 下拉层、弹窗外壳、颜色预览统一引用主题资源，减少弹窗内部局部样式分裂。
- 主程序版本号更新为 `0.3.98.3`。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.4

**主题：数据库恢复入口**

- 侧边栏工具区新增 `恢复DB`，可直接选择 `.db` 备份文件恢复为当前数据目录的 `app.db`，不再只能通过“数据目录”切换整个根目录。
- 恢复流程采用“暂存 + 重启后替换”：当前运行中不热覆盖 SQLite，避免数据库句柄仍打开时损坏数据。
- 恢复前会先创建当前数据库手动备份；下次启动时在数据库初始化前替换 `app.db`，并同步处理可能存在的 `-wal` / `-shm` 伴随文件。
- 侧边栏文案调整：`备份路径` 改为 `打开备份`，`指定数据` 改为 `数据目录`，`数据路径` 改为 `打开数据`，区分“数据根目录”和“备份 DB 恢复”。
- 主程序版本号更新为 `0.3.98.4`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.5

**主题：数据安全面板**

- 侧边栏备份相关按钮收敛为单一 `数据安全` 入口，减少 `立即备份 / 打开备份 / 恢复DB / 打开数据` 平铺造成的理解成本。
- 新增 `DataSafetyDialog`：顶部说明当前数据目录，中部列出 `backups/` 下的数据库备份，底部集中提供 `立即备份`、`打开备份目录`、`打开数据目录`、`选择其他 DB`、`恢复选中`。
- 备份列表显示文件名、时间、大小和完整路径，默认按修改时间倒序，用户可以直接选中某份备份恢复。
- `恢复选中` 复用 0.3.98.4 的安全恢复流程，仍然通过暂存和重启后替换避免热覆盖 SQLite。
- 主程序版本号更新为 `0.3.98.5`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.6

**主题：侧栏数据工具收口**

- 侧栏工具区只保留两个核心入口：`指定数据目录` 与 `备份数据DB`，降低高频区域按钮密度。
- `备份数据DB` 进入数据安全面板；恢复 DB、打开备份目录、打开数据目录、检查更新等低频动作全部收进该面板。
- 数据安全面板按钮从 `RadiusPill` 改为 `RadiusControl`，避免椭圆胶囊形态，统一为矩形圆角按钮。
- 主程序版本号更新为 `0.3.98.6`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.7

**主题：数据安全面板按钮防重叠**

- 修复数据安全面板底部按钮在固定窗口宽度下横向挤压、文字重叠的问题。
- 底部操作区改为两行分组：第一行保留 `立即备份` 与 `恢复选中` 两个核心动作，第二行使用 `WrapPanel` 承载打开目录、选择其他 DB、检查更新等辅助动作。
- 主程序版本号更新为 `0.3.98.7`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.8

**主题：性能审查与低风险优化**

- 书库封面懒加载增加离屏取消：卡片 `Unloaded` 后取消对应封面加载令牌，避免快速滚动时继续为不可见卡片排队解码。
- 扫描/刷新书库前统一取消并释放未完成的封面加载令牌，降低大书库重扫时的后台任务残留。
- 首页统计刷新合并可见数量计算，避免一次刷新中先统计再额外 `Books.Count(FilterBook)` 的二次全量遍历。
- 数据安全面板备份列表改为窗口加载后后台枚举，避免打开弹窗时 UI 线程同步扫描备份目录。
- 主程序版本号更新为 `0.3.98.8`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.9

**主题：日志面板重布局**

- 删除侧栏顶部无实际功能的 `快速入口` 容器块，减少左侧导航噪音。
- 日志从侧栏小折叠块改为主工作区底部 IDE 风格输出面板，通过侧栏底部 `展开日志 / 收起日志` 控制显示。
- 输出面板实时接收 `AppLogger` 写入行，并通过轻量定时器捕捉状态栏变化，以 `[STATUS]` 形式追加到输出流。
- 日志面板提供 `清空` 与 `打开日志目录` 操作；底部状态行保留当前操作反馈。
- 主程序版本号更新为 `0.3.98.9`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.10

**主题：日志面板排版修复**

- 修复日志面板被内容压成横条的问题：面板改为固定高度底部控制台区域，内容区获得稳定显示空间。
- 日志标题栏改为三列 Grid：左侧标题、中间说明、右侧操作按钮，避免 `输出 / 清空 / 打开日志目录` 横向挤压。
- 面板内边距、圆角和状态栏高度重新调整，减少文字贴边和视觉拥挤。
- 主程序版本号更新为 `0.3.98.10`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.11

**主题：滚动条圆角修正**

- 修复全局滚动条 Thumb/Track 复用 `RadiusPill` 导致长滚动条呈现巨大椭圆的问题。
- 新增 `RadiusScrollTrack` 与 `RadiusScrollThumb`，滚动条改为小圆角细矩形轨道和滑块。
- Thumb 最小长度从 36 收紧到 24，视觉更轻，不再抢占列表内容注意力。
- 主程序版本号更新为 `0.3.98.11`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.12

**主题：滚动条留白修正**

- 全局垂直滚动条占位宽度从 10 调整为 18，轨道靠右显示，为内容与滚动条之间预留缓冲空间。
- 水平滚动条占位高度同步从 10 调整为 18，轨道靠下显示，避免贴近内容。
- 滚动条视觉仍保持 6px 细轨道，不扩大实际滑块视觉重量。
- 主程序版本号更新为 `0.3.98.12`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.13

**主题：滚动条滑块对齐修复**

- 修复滚动条容器加宽后 Thumb 可见区域仍占满命中区、导致滑块与右侧轨道不对齐的问题。
- 垂直/水平滚动条 Thumb 拆分为独立模板：18px 命中区保持易操作，真实可见滑块固定为 6px。
- 垂直滑块靠右对齐轨道，水平滑块靠下对齐轨道，内容侧留白稳定。
- 主程序版本号更新为 `0.3.98.13`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.14

**主题：侧栏分区视觉优化**

- 侧栏 `浏览` 与 `工具` 改为独立浅色容器分区，标题居中并带底色，增强区域识别。
- `展开日志 / 收起日志` 入口改为底部浅色容器内的居中按钮，视觉上与其他工具区统一。
- 移除日志开关下方的数据目录说明，避免侧栏重复展示低频路径信息。
- 主程序版本号更新为 `0.3.98.14`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.15

**主题：侧栏分区减重与日志高度收缩**

- 移除 `浏览`、`工具` 两个分区的最外层容器，仅保留居中标题底色作为区域识别。
- `展开日志 / 收起日志` 去除外层容器，改为按钮本体居中承载，减少侧栏重复框线。
- 底部日志面板高度从 230 收缩到 160，降低对主工作区的占用。
- 主程序版本号更新为 `0.3.98.15`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.16

**主题：日志开关按钮高度收敛**

- 为侧栏 `展开日志 / 收起日志` 单独定义紧凑按钮样式，避免复用工具按钮样式导致视觉高度过大。
- 日志开关按钮固定高度收敛为 30，并压缩内边距与底部间距，使其回归单一按钮语义。
- 主程序版本号更新为 `0.3.98.16`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.17

**主题：侧栏层级与圆角规格修正**

- 主导航保持完整宽度，下方工具按钮组收窄为居中区域，明确主栏目宽于次级栏目。
- 侧栏选中导航颜色从深色块改为浅层级背景，标题条维持更深一档的区域底色，避免层级倒挂。
- 浏览/工具标题条统一改用 `RadiusControl`，与导航按钮、工具按钮保持圆角规格一致。
- 日志开关按钮收窄并居中，避免与主导航形成同宽竞争。
- 主程序版本号更新为 `0.3.98.17`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.18

**主题：侧栏二级容器宽度收窄**

- `主页 / 书库 / 标签 / 作者` 二级导航按钮固定为 132 宽并居中，不再占满侧栏内宽。
- 工具按钮组从 156 收窄为 132，保持低于浏览标题条的视觉权重。
- 日志开关按钮从 116 收窄为 96，进一步弱化底部辅助入口。
- 主程序版本号更新为 `0.3.98.18`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.19

**主题：工具分区宽度与按钮行布局修正**

- `工具` 标题条左右边距统一为 8，与 `浏览` 标题条保持同宽规格。
- `指定数据` 与 `备份数据` 从横向并排改为纵向两行，避免工具动作挤压。
- 工具动作区域继续保持 132 宽居中，维持低于分区标题的视觉权重。
- 主程序版本号更新为 `0.3.98.19`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.20

**主题：侧栏容器高度统一**

- 侧栏分区标题条、二级导航按钮、工具按钮和日志开关统一为 32 高。
- `浏览 / 工具` 标题条改为固定高度并垂直居中文本，不再由 padding 撑出不确定高度。
- 二级导航与工具按钮同步收窄内边距，保持紧凑一致的矩形块规格。
- 主程序版本号更新为 `0.3.98.20`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.21

**主题：日志开关宽度统一**

- `展开日志 / 收起日志` 按钮宽度从 96 调整为 132，与二级导航和工具动作按钮保持同一宽度规格。
- 主程序版本号更新为 `0.3.98.21`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.22

**主题：前端样式基类收敛**

- 在全局主题资源中新增 `AppButton`、`AppGhostButton`、`AppDangerButton` 三个基础按钮样式，统一按钮模板、圆角、边框、hover/pressed 反馈。
- 主窗口隐式按钮样式、`GhostButton`、`DangerButton` 改为基于全局按钮基类，避免主窗口继续维护一套局部模板。
- 数据安全弹窗的 `DialogButton`、`DialogGhostButton`、`DialogDangerButton` 改为基于同一全局按钮基类，仅保留弹窗自身高度和最小宽度差异。
- 侧栏导航、工具、日志按钮的背景/边框/文字色改为引用主题 Brush，减少硬编码色值导致的层级漂移。
- 主程序版本号更新为 `0.3.98.22`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.23

**主题：导入弹窗前端修正与作者新增**

- 导入弹窗三处控件规格收敛：`加入路径`、路径列表空态容器、底部 `取消 / 继续导入` 按钮统一使用全局按钮基类和矩形圆角规格。
- 导入弹窗路径列表高度收缩为固定 92，并增加内部滚动，避免空态区域过高遮挡主界面内容。
- `RenameDialog` 接入全局按钮样式和统一圆角/色值，避免新增作者弹窗继续使用旧胶囊风格。
- 作者管理页新增 `新增作者` 按钮，可创建暂未关联书籍的独立作者。
- 数据库新增 `managed_authors` 表，独立作者可持久化；作者列表和作者筛选会合并书籍作者与独立作者。
- 作者改名时会同步更新独立作者记录；若作者有关联书籍，仍会批量更新书籍作者字段。
- 主程序版本号更新为 `0.3.98.23`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.24

**主题：修正标签页误放作者入口**

- 移除标签管理页 header 中误插入的 `新增作者` 按钮。
- 保留作者管理页中的 `新增作者` 入口，确保作者新增功能只出现在作者界面。
- 主程序版本号更新为 `0.3.98.24`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.25

**主题：全项目圆角规格收敛**

- 全项目遍历 `*.xaml` 中的 `CornerRadius`，移除业务 UI 里的数字硬编码圆角、`999` 胶囊圆角和 `RadiusPill` 引用。
- 主窗口的指标卡、侧栏、书库筛选区、标签池、标签/作者管理页、日志面板、详情面板、导入覆盖层统一改用主题 radius token。
- `TagCreateDialog`、`TagEditDialog`、`AuthorBatchImportDialog` 接入全局按钮基类和统一矩形圆角，不再维护旧胶囊按钮模板。
- `ReaderWindow` 的工具栏、按钮、下拉框、提示浮层统一改用 `RadiusControl` / `RadiusPanel` / `RadiusScrollTrack`。
- 删除未使用的 `RadiusPill` token，防止后续继续引入胶囊规格。
- 复查命令 `rg -n 'CornerRadius="[0-9]|CornerRadius="999|RadiusPill' MangaReader.Native --glob '*.xaml'` 无业务 UI 命中。
- 主程序版本号更新为 `0.3.98.25`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.26

**主题：入口按钮圆角归正与前端规范落档**

- 新增 `HeaderActionButton`，统一页面 header 管理动作入口的矩形圆角、字号、背景和边框规格。
- `新建候选标签` 和 `新增作者` 改为共用 `HeaderActionButton`，不再分别使用过滤器按钮或默认按钮样式。
- 将主窗口历史 `Pill` 语义样式名改为中性命名：`FilterActionButton`、`BatchActionButton`、`FilterTextBox`、`FilterComboBox`、`SlimProgressBar`。
- `TagCreateDialog` / `TagEditDialog` 中的 `DialogPillButton` 改名为 `DialogButton`。
- 新增 `前端视觉规范.md`，记录圆角 token、按钮规格、容器层级、色彩层级和新增 UI 检查清单。
- 复查命令 `rg -n 'Pill|CornerRadius="[0-9]|CornerRadius="999|RadiusPill' MangaReader.Native --glob '*.xaml'` 无命中。
- 主程序版本号更新为 `0.3.98.26`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.27

**主题：修复标签分组下拉选择**

- 修复标签管理页 `全部分组` 下拉无法稳定选择的问题。
- 原因是 `TagManagerFilter_Changed` 每次触发都会重建 `TagGroupFilterBox.Items`，用户选择分组时下拉内容会被清空并重置。
- 新增 `_tagGroupFilterOptionsDirty` 标记，仅在标签集合、分组、删除或初始化变化时重建分组选项；普通分组选择只刷新列表。
- 删除标签时同步清理 `_managedTagColors`，避免标签颜色残留孤儿数据。
- 主程序版本号更新为 `0.3.98.27`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.28

**主题：书库过滤排序移出 UI 线程**

- 新增 `_allBooks` 全库主列表，`Books` 改为当前过滤/排序后的可见列表，避免 `ICollectionView.Refresh()` 在 UI 线程同步遍历全库。
- 书库搜索、状态、作者、收藏、隐藏、Tag 筛选和排序统一由后台任务执行，并通过 `CancellationTokenSource` 取消过期过滤任务。
- 移除 `_booksView.Filter` 和 `SortDescriptions` 排序，排序结果由后台 LINQ 生成后一次性 `Books.ReplaceRange` 写回 UI。
- 标签索引、作者筛选/管理、首页书架、书架统计、批量选择、作者重命名和标签重命名/删除等全库语义统一改用 `_allBooks`。
- 扫描、批量导入和进入书库时仅在过滤结果落地后执行空视图兜底，普通搜索无结果不会被自动清空筛选。
- 主程序版本号更新为 `0.3.98.28`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.98.29

**主题：修复标签编辑分组下拉**

- 修复 `编辑标签` 和 `新建标签` 弹窗中标签分组可编辑下拉框箭头区域无法稳定展开的问题。
- 原因是自定义 `DialogEditableComboBox` 模板把右侧 ToggleButton 设置为不可命中，导致视觉上有箭头但点击不会打开下拉列表。
- 将右侧下拉命中层改为纯透明 ToggleButton 模板，保留矩形圆角外观且不漏出系统默认按钮样式。
- 标签分组取值改为优先读取 ComboBox 文本，确保用户手动输入自定义分组时不会被旧 `SelectedItem` 覆盖。
- 主程序版本号更新为 `0.3.98.29`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.99

**主题：修复封面页刷新与阅读状态语义**

- 修复详情编辑中修改 `封面页` 后仅保存索引、不刷新当前封面图的问题；保存信息和 `设置为封面页` 都会清空旧封面图并重新生成封面缓存。
- 阅读状态收敛为 `未读 / 在读`，从书库筛选和编辑表单中移除 `已读 / 搁置` 入口。
- 旧数据库中的 `finished / paused` 会在模型加载时归一为 `reading`，避免历史状态继续污染当前 UI。
- 阅读次数与状态联动：`ReadCount > 0` 时自动归为 `在读`，`SaveReadCount` 会同步写入 `reading_status`，不再出现“读过 1 次但状态未读”。
- 阅读器翻到非首页时会把未读作品自动标记为 `在读`，并随进度保存到数据库。
- 书架统计中的 `已读` 改为 `读过`，统计口径改为 `ReadCount > 0`。
- 主程序版本号更新为 `0.3.99`。
- 已通过 `dotnet build -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.3.99.5

**主题：导入弹窗视觉规范收敛**

- 新增 `AppDialogButton`、`AppDialogGhostButton`、`AppDialogCloseButton`、`AppDialogTextBox`、`AppDialogTitleText`、`AppDialogSubtitleText`、`AppDialogLabelText`、`AppDialogPanelBorder`、`AppDialogListItemBorder` 主题样式。
- `ImportFolderDialog` 和 `AuthorBatchImportDialog` 移除局部复制的按钮、输入框模板，统一改用 `MangaTheme.xaml` 中的弹窗样式。
- `AuthorBatchImportDialog` 头部改为稳定 Grid 布局，长标题允许换行，不再挤压右上角关闭按钮。
- 批量导入列表项、统计卡、提示文字统一使用主题 Brush 和弹窗容器样式，减少硬编码色值。
- `前端视觉规范.md` 新增弹窗规格，禁止后续弹窗继续复制 `DialogButton` / `DialogTextBox` 这类局部样式。

**主题：阅读器交互与下拉视觉修复**

- 修复右键临时放大失效：右键按下/抬起事件从 `ReaderScrollViewer` 提升到 `ReaderRoot`，并使用 `CaptureMode.SubTree` 捕获鼠标，避免图片层或滚动层命中链路吞掉右键操作。
- 右键放大后的拖拽平移改由根层 `PreviewMouseMove` 兜底处理，保证捕获到根层后仍能双轴移动。
- 修复双页模式适应高度在图片尺寸不一或横构图时漂移的问题：双页模式下两页使用同一解码基准，适高计算优先读取归一化后的显示高度。
- 阅读器下拉项移除厚重蓝灰选中块，改为暗色半透明背景加细边框，避免出现系统感强的正方形矩形高亮。
- 修复阅读器 `ComboBox` 透明 ToggleButton 命中层漏出 WPF 默认 hover 背景的问题；该层现在只负责点击打开下拉，不再绘制任何系统矩形高亮。
- 明确适高/适宽语义：当前单页或双页展开整体应被约束进固定阅读视口。翻页加载完成后只允许当前请求应用适配，旧页面请求延迟返回时不再改写缩放。
- 进度保存从页面适配链路中移出，避免快速滚轮翻页时旧页 `SaveProgress` 完成后再次触发适高/适宽，造成最大显示高度不一致。
- 更新服务支持四段版本显示与本地 zip 识别，`0.3.99.5` 不再被 UI 显示或本地包扫描截断为 `0.3.99`。
- 清理 `MangaReader.Updater` 发布时的未使用异常变量警告，保持发布日志干净。
- 主程序版本号更新为 `0.3.99.5`。
- 已发布自包含测试版：`_release/0.3.99.5/MangaReader.Native.exe`，发布目录包含 `Updater/MangaReader.Updater.exe`。
- 已打包 zip 更新包：`_release/MangaReader-win-x64-v0.3.99.5.zip`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj` 和 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.4.99.7

**主题：阅读器快速翻页与适配稳定性补强**

- 翻页请求改为“请求页码先行 + 短延迟合并 + 串行解码”：快速滚轮时先推进 `_requestedPageIndex`，只保留最后一次目标页，避免旧逻辑反复基于已显示页计算下一页。
- 图片解码同一时间只允许一个任务执行；新请求到来时取消当前请求并排队最新页，避免 `BitmapImage` 不可中断解码在快速翻页时堆积。
- 阅读器页面成功加载不再写 `reader-load-page` 高频成功日志，避免每页同步 `Flush()` 把磁盘 I/O 放进翻页热路径。
- 阅读进度保存改为 `650ms` 防抖合并，快速翻页只保存最后停住的页；关闭阅读器时强制保存一次当前状态。
- 适应高度 / 适应宽度计算改用 `PixelWidth` / `PixelHeight`，不再使用受图片 DPI 元数据影响的 WPF `Width` / `Height`，减少不同图片最大显示高度不一致的问题。
- 单页显示显式按像素宽高建立未缩放基准；双页显示继续做同高归一化，再按双页展开整体执行适配。
- 适配可用阅读区统一扣除 `ScrollViewer.Padding`、图片边距和双页间距，避免适高 / 适宽后因为 6px 内边距产生细微溢出。
- 窗口 `SizeChanged` 触发的适配改为 `80ms` 防抖，拖动窗口或全屏切换时不再逐事件重算缩放。
- 单页 / 双页和阅读方向切换时使用强制重载；即使切换发生在当前页解码中，也会取消旧请求并按新模式重新显示，避免同页请求被吞掉。
- 修正翻页后一帧缩放闪烁：页面 Source、尺寸归一化、双页间距和适高 / 适宽缩放现在在同一 UI 更新批次内完成，不再延迟到 `DispatcherPriority.Render`。
- 翻页请求合并延迟从 `80ms` 下调到 `24ms`，保留滚轮合并能力，同时降低快速翻页时的输入滞后感。
- 主程序版本号更新为 `0.4.99.7`。
- 已发布自包含测试版：`_release/0.4.99.7/MangaReader.Native.exe`，并打包 zip：`_release/MangaReader-win-x64-v0.4.99.7.zip`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.4.99.9

**主题：标签分组与作者批量导入界面修复**

- 修复新建 / 编辑标签弹窗的分组选项来源：此前误传 `_managedTagCategories.Keys`，导致标签名被当作标签分组显示；现在改为传入真实分类集合。
- 标签分类读取增加脏数据保护：若历史数据里分类名等于标签名，或分类名本身是已知标签名，则 UI 侧回落为 `自定义`，避免继续污染标签管理筛选框。
- 作者批量导入弹窗的候选列表禁用横向滚动，文件夹名使用单行省略号截断并保留完整 ToolTip。
- 候选标题编辑框改为单行裁切显示，长日本漫画名不再撑开列表布局；完整标题仍可通过 ToolTip 查看，也仍支持逐本手动修改。
- 修复阅读器目录右键设封面被右键放大逻辑拦截的问题：根层右键抬起事件现在和右键按下一样避让目录覆盖层，允许缩略图 `ContextMenu` 正常弹出。
- 阅读器适配策略调整为“固定视口舞台 + 固定页槽 + 图片等比居中”：适高 / 适宽前先把 `ImageScrollContent` 的最小宽高锁到当前阅读视口；竖向单页和双页都进入固定比例页槽，图片使用 `Uniform` 在槽内居中显示，扫图宽高差异由页槽留白承接，避免外层布局随图片宽度收缩、翻页产生细小抖动。横构图单页保留自身宽高比，避免被竖向页槽压缩过小。
- 主程序版本号更新为 `0.4.99.9`。
- 已发布自包含测试版：`_release/0.4.99.9/MangaReader.Native.exe`，并打包 zip：`_release/MangaReader-win-x64-v0.4.99.9.zip`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.5

**主题：Tag 交互、批量导入对齐与书籍详情重排**

- Tag 池改为双击执行筛选，单击不再直接给当前漫画添加 Tag，避免和拖拽意图冲突。
- Tag 拖拽改用专用数据格式 `MangaReader.TagName`，修复 `BooksList DragOver` 将 Tag 拖拽误判为无效导入数据导致投放几乎失效的问题。
- 书卡 / 瀑布流中的 Tag 颜色改为同步主窗口管理色表、分组和脏分类保护，保证与 Tag 堆显示一致。
- 作者批量导入弹窗候选项改为两行 Grid，右侧统计信息卡与标题输入框同行对齐，避免容器错位。
- 书籍详情右上角恢复收藏按钮，编辑状态不再额外出现“收藏”字段。
- 书籍详情只读态移除第二行阅读状态，将阅读状态与阅读次数合并到同一行状态区，并保留 `-1` / `+1` 操作。
- 书籍详情底部右侧新增“返回书库”按钮，点击后关闭详情并清除当前选择。
- 书籍详情右侧信息区重新按统一列距和行距排列，减少字段错位和视觉跳动。
- 主程序版本号更新为 `0.5`。
- 已发布自包含测试版：`_release/0.5/MangaReader.Native.exe`，并打包 zip：`_release/MangaReader-win-x64-v0.5.zip`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.5.1

**主题：书籍详情编辑内页视觉修正**

- 修正书籍详情编辑态只调整外层、内页内容仍未变化的问题：编辑表单现在包入统一浅色内容卡，边框、圆角、背景与主题 Token 对齐。
- 书籍详情编辑态的 `作者` 字段从裸文本改为只读输入框外观，并增加长文本省略与 ToolTip，避免与其它字段规格断层。
- 统一详情编辑态输入框、下拉框、日期按钮和底部动作按钮的高度、内边距与 Hover/Focus 边框反馈，减少系统默认控件感。
- 主程序版本号更新为 `0.5.1`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.5.2

**主题：弹窗 UI 规则全局收口**

- 遍历项目 XAML 后，优先清理可直接统一的弹窗样式残留：`TagCreateDialog`、`TagEditDialog`、`DataSafetyDialog`、`RenameDialog`、`TagNameDialog` 不再维护各自的 `DialogButton` / `DialogTextBox` / `DialogComboBox` 局部样式。
- `MangaTheme.xaml` 新增 `AppDialogDangerButton`、`AppDialogLinkButton`、`AppDialogReadOnlyText`、`AppDialogComboBox`、`AppDialogEditableComboBox`、`AppDialogComboBoxItem`，把弹窗按钮、只读字段、链接按钮和下拉框模板集中到全局主题资源。
- `TagCreateDialog` 与 `TagEditDialog` 移除复制粘贴的 ComboBox / ComboBoxItem 模板，标签分组和规则下拉统一引用全局 `AppDialog*` 规格。
- `TagNameDialog` 从旧暖色系统窗口改为无边框透明弹窗结构，阴影、背景、标题、输入框和按钮规格与其它管理弹窗一致。
- `DataSafetyDialog` 和 `RenameDialog` 移除局部按钮与输入框样式别名，直接使用全局 `AppDialog*` 资源。
- 主程序版本号更新为 `0.5.2`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.5.3

**主题：阅读器图片管线与交互热路径优化**

- 阅读器页面解码接入小型 LRU 内存缓存，缓存键包含页码、单/双页模式和当前解码宽度，避免前后翻页时重复解码同一张大图。
- 当前页显示完成后异步预解码下一页和上一页；预解码带 90ms 延迟，并在用户继续翻页、窗口关闭、已有解码进行中或内存压力升高时自动取消。
- 阅读器缓存默认保留 5 个页项；检测到 GC 内存压力较高时收缩到 2 个页项，并跳过邻页预解码，防止连续翻页时把大图堆进内存。
- 保留既有按视口宽度下采样策略：单页 / 双页分别按当前阅读视口、缩放值和安全上限计算 `DecodePixelWidth`，避免 4K / 8K 原图直接进入 UI。
- 复核封面管线：封面已按缓存文件生成，书卡进入可视区才加载，离开可视区取消；首页书架和书库共用同一 `CoverThumbnailPipeline` 内存 LRU。
- 复核 SQLite 热路径：数据库初始化已开启 `WAL` / `synchronous=NORMAL`，并已建立 `folder_path`、`author`、`reading_status`、`is_favorite`、`is_hidden` 索引。
- 书库搜索合并延迟从 220ms 下调为 160ms，在 3000+ 书库下减少输入滞后，同时继续避免每个按键都触发完整过滤。
- 实时日志面板保持 300 行上限，并新增单行 900 字符截断，避免异常长日志把 TextBlock 撑成高成本渲染对象。
- 主程序版本号更新为 `0.5.3`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.5.4

**主题：日志面板扩大 / 还原**

- 底部日志面板新增 `扩大 / 还原` 按钮，普通高度保持 160，扩大后高度提升到 420，便于查看长日志和异常堆栈。
- 日志面板处于隐藏状态时点击 `扩大` 会直接展开大日志面板，避免需要先展开再切换尺寸。
- 展开 / 收起逻辑保持不变，扩大状态只影响日志面板高度和按钮文案，不改变日志缓存、截断和滚动策略。
- 主程序版本号更新为 `0.5.4`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-14 原生软件 0.5.5

**主题：第二轮 UI 规则残留清理**

- 继续遍历项目 XAML，清理弹窗外壳复制问题：`AuthorBatchImportDialog`、`ImportFolderDialog`、`DataSafetyDialog`、`RenameDialog`、`TagCreateDialog`、`TagEditDialog`、`TagNameDialog` 统一使用全局 `AppDialogShadowBorder`。
- `MangaTheme.xaml` 新增 `AppDialogShadow`、`AppDialogShadowBorder` 和 `RadiusClipLeft`，把弹窗阴影、遮罩、书卡左侧裁切圆角纳入主题资源。
- 主窗口移除旧按钮别名 `GhostButton` / `DangerButton` 的使用路径，局部派生样式和按钮实例直接基于 `AppGhostButton` / `AppDangerButton`，避免后续继续扩散旧命名。
- 书库书卡封面模板中的硬编码圆角统一改为 `RadiusClip`、`RadiusProgress`、`RadiusClipLeft`，消除剩余 `CornerRadius="数字"` 写法。
- 本轮保留 `MainWindow` 的窗口级默认 TextBox / ComboBox / CheckBox 模板，以及 `ReaderWindow` 的暗色阅读器控件模板；这两类有明确页面语义，后续若抽全局需要单独做大范围主题重构。
- 主程序版本号更新为 `0.5.5`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-15 原生软件 0.5.7

**主题：漫画详情浮层重塑（数据库表单 → 漫画作品详情页）**

- `DetailShell` 浮层整体重写：去除左侧米色容器，单封面卡 320×480 居中；右栏 Hero 区呈现书名（28 号粗体）、`作者 …` 副标题、`{页数} 页 · {Tags}` 元信息行，下方为唯一主按钮“开始阅读”。
- 顶栏统一为左侧 `← 返回书库`，右侧 `★/☆ 收藏 · 编辑 · 更多 ▾`；更多菜单收纳：打开所在文件夹 / 重新定位 / 切换卡片样式 / 隐藏作品（恢复显示） / 删除库记录。
- 展示态信息分块：阅读状态行（`未读 · 0 次阅读`）、简介、作品信息（作者 / 外文名 / 页数 / 封面页 / 出品时间 / 录入时间，空值统一灰色“未填写”）、文件区（打开所在文件夹 / 重新定位）。
- 编辑态独立顶栏“编辑作品信息 + 取消 / 保存”，底部新增“危险操作”分组（隐藏作品 / 删除库记录）。
- 标签从纯文本改为胶囊（`DetailTagChipBorder/Text`），无标签时整段隐藏。
- `MangaTheme.xaml` 集中追加详情页样式 token：`DetailHeroTitleText`、`DetailHeroAuthorText`、`DetailHeroMetaText`、`DetailReadingStatusText`、`DetailSectionTitleText`、`DetailSummaryText/EmptyText`、`DetailMetaLabelText/ValueText/EmptyValueText`、`DetailCoverPageHintText`、`DetailHeroPrimaryButton/SecondaryButton`、`DetailTopBarButton/FavoriteButton`、`DetailFileActionButton`、`DetailDangerInlineButton`、`DetailEditFormHeaderText`，禁止再硬编码颜色。
- 代码后置 `FillMetadataEditors` 同步填充 Hero / TagChips / Meta / ReadingStatusInline / 收藏图标 / MoreActionsHideMenuItem；新增 `OpenMoreActionsMenu_Click` 以 `Placement.Bottom` 弹出更多菜单。
- 主程序版本号更新为 `0.5.7`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-16 原生软件 0.5.8

**主题：书库 Tag 体验与详情页前端修正**

- 书库瀑布流卡片新增 `CardTagItems` 轻量预计算：卡片 Tag 最多占两行，超长 Tag 会跳过并尝试展示后续 Tag，隐藏数量统一显示为 `+n`，避免 Tag 撑出卡片。
- Tag 颜色策略收口到分组：新建 / 编辑 Tag 时按分组继承已有颜色，选择或输入分组会自动切换颜色，避免同组 Tag 颜色分裂。
- 作品编辑页 Tag 字段从逗号文本框改为彩色圆角矩形 Tag 编辑器：已选 Tag 点击移除，候选 Tag 点击添加，并保留 `+ 新建` 入口。
- 全局 Tag 块视觉统一为圆角矩形样式，详情页、书库筛选池、瀑布流卡片、作品编辑页和详情页展示态不再混用胶囊/椭圆。
- Tag 交互统一：所有可交互 Tag 块双击均进入书库并按该 Tag 筛选；单击提供状态提示，hover / 按压增加即时反馈，降低“点了没反应”的感知。
- 详情页展示态清理“文件”独立小节，将 `打开所在文件夹` / `重新定位` 移到 `开始阅读` 同一行，减少底部信息区割裂。
- 主程序版本号更新为 `0.5.8`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-16 原生软件 0.5.9

**主题：Tag 双击筛选热修**

- 修复在作品详情页双击 Tag 进入书库筛选后，详情浮层仍停留在当前界面的问题：Tag 筛选会清空当前书籍选择并强制收起详情面板。
- 修复瀑布流卡片内双击 Tag 时事件冒泡到书卡，导致同时打开作品详情的问题：书卡双击逻辑会识别 Tag 事件源并直接吞掉。
- 主程序版本号更新为 `0.5.9`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-16 原生软件 0.5.9.1

**主题：Tag 颜色绑定与详情退出热修**

- 修复 Tag 颜色绑定逻辑：单个 Tag 自身颜色优先，不再被同组默认颜色覆盖；编辑已有 Tag 时切换分组不会擅自改掉原有颜色。
- Tag 管理页的 `已关联 n 本漫画` 现在可直接点击，进入书库并按该 Tag 筛选。
- 详情浮层增加透明空白点击层，打开作品详情后点击卡片外空白区域会清空当前选择并回到瀑布流。
- 主程序版本号更新为 `0.5.9.1`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-17 原生软件 0.5.9.2

**主题：瀑布流 Tag 与阅读器画质热修**

- 瀑布流卡片 Tag 改为独立紧凑样式，不再复用全局 Tag Border；卡片 Tag 固定单行高度，整体最多两行，避免底部文字被裁切。
- 卡片 Tag 容量算法收紧为单行可控宽度：过长 Tag 不再强行换行塞入卡片，隐藏项继续计入 `+n`。
- 修复瀑布流 Tag 颜色与筛选栏不一致的问题：卡片 `CardTagItems` 会在窗口同步阶段使用同一套分组颜色解析逻辑刷新。
- 修复阅读器正文图被降采样的问题：主阅读页与相邻页预加载不再按视口设置 `DecodePixelWidth`，改为原图解码；封面、目录缩略图和批量分析路径保持低开销缩略图策略。
- 阅读器正文图加载不再忽略图片色彩配置文件，降低色彩偏差。
- 主程序版本号更新为 `0.5.9.2`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-17 原生软件 0.5.9.3

**主题：阅读器质量模式发布修复**

- 阅读器底部工具栏新增 `质量 / 性能` 模式切换，并通过 `reader.qualitymode` 持久化用户选择。
- 质量模式默认启用：当前页按原图解码，并以 `BitmapSource.PixelWidth / PixelHeight` 作为布局基准，不再先压到固定 1600 高页槽。
- 性能模式保留受控降采样和固定页槽布局，供极大图或低内存环境使用。
- 切换质量 / 性能模式会清理阅读器页缓存并重新加载当前页，避免跨模式复用旧缓存。
- 主程序版本号更新为 `0.5.9.3`。
- 已通过 `dotnet publish .\MangaReader.Native\MangaReader.Native.csproj -c Release -r win-x64 --self-contained true -o .\publish_test`，可正常生成 Windows x64 发布目录与 Updater 子目录。

### 2026-06-17 原生软件 0.5.9.4

**主题：阅读器清晰适配与更新识别修复**

- 阅读器 `质量模式 + 适宽 / 适高` 改为后台生成清晰适配图：先即时显示，随后用原图生成当前视口尺寸的锐化适配图并自动替换。
- 新增清晰适配图 LRU 缓存，同一页、同一适配尺寸会复用结果，避免频繁重复计算。
- 快速翻页、切换页面或窗口尺寸变化时会取消过期适配任务，降低后台计算浪费。
- 修正上一轮仅推送 `main` 未发布新 tag 导致自动更新检查不到清晰适配版本的问题。
- 主程序版本号更新为 `0.5.9.4`。
- 已通过 `dotnet build MangaReader.Native\MangaReader.Native.csproj -c Release`，0 警告 0 错误。

### 2026-06-17 原生软件 0.5.9.5

**主题：阅读器适配图缓存与清晰度可观测性**

- `质量模式 + 适宽 / 适高` 的清晰适配图缓存从单纯数量限制升级为“数量 + 像素内存预算”双限制，默认预算 96MB，内存压力高时收缩到 48MB。
- 后台清晰适配图生成新增性能日志，记录页码、适配模式、缩放比例、结果内存、锐化强度、生成耗时和当前适配缓存占用，便于后续用真实数据继续调参。
- 锐化强度改为按缩放比例动态调整，避免所有缩放场景都套用同一个固定锐化值。
- 内存压力高时会同步修剪阅读页缓存和质量适配图缓存，降低大图阅读时的内存峰值。
- 新增 `阅读器放大与适配清晰度问题说明.md`，记录 DPI、适配缩放、WPF 缩放边界、当前实现策略与后续 SkiaSharp / WIC 方向。
- 主程序版本号更新为 `0.5.9.5`。
- 已通过本地 Windows x64 `dotnet publish` 与 zip 打包验证，确认发布目录包含主程序与 `Updater/MangaReader.Updater.exe`。

### 2026-06-17 原生软件 0.5.9.6

**主题：阅读器近 1:1 适配清晰度修正**

- 将 `质量模式 + 适宽 / 适高` 的清晰适配旁路阈值从 `scale >= 0.98` 放宽到 `scale >= 0.93`，避免窗口模式下接近 1:1 的图片仍被 CPU 适配图重采样。
- 将近 1:1 缩放档位的锐化强度从 `0.08` 降至 `0.04`，减少线稿、网点和文字边缘的人工锐化痕迹。
- 新增 `适配清晰度修复说明.md`，记录 2K / 高 DPI / 窗口 chrome 导致实际 scale 落在 0.93-0.95 的原因。
- 主程序版本号更新为 `0.5.9.6`。
- 已通过本地 Windows x64 `dotnet publish` 与 zip 打包验证，确认发布目录包含主程序与 `Updater/MangaReader.Updater.exe`。

### 2026-06-17 原生软件 0.5.9.7

**主题：书库加载反馈、主页交互与详情目录**

- 进入书库扫描时新增独立加载进度浮层，显示当前扫描路径、已扫描文件夹数、已发现漫画数，并在写入数据库阶段切换为明确保存提示，避免启动或重扫时只有空白列表。
- `LibraryScanner.Scan()` 支持 `IProgress<LibraryScanProgress>` 与取消令牌，空文件夹也会上报进度，避免进度条停滞。
- 主页“继续阅读”区改为完整等宽容器，三张作品卡片按宽度均分；主页卡片补齐圆角、边框和 hover 反馈，点击作品改为打开详情面板而不是直接进入阅读器。
- 阅读器底部工具栏按语义拆分为“显示”（适宽 / 适高 / 原始）与“渲染”（质量 / 性能），保留 `FitMode` 与 `ReaderQualityMode` 两个正交枚举，避免把显示比例和渲染策略混成一个状态。
- 书本详情页 Hero 区将作者、页数和 Tag 放到同一行；“作品信息”从 6 行改为 3 行双列布局。
- 详情页封面下新增“打开目录”按钮，弹出详情页内目录浮层；左键只预览页面，不启动阅读器，右键可设为封面。
- 将阅读器目录项抽为共享 `PageCatalogItem` 模型，阅读器和详情页复用目录数据模型，但各自维护 UI 与行为，避免详情页直接耦合 `ReaderWindow`。
- 主程序版本号更新为 `0.5.9.7`。
- 已通过本地 Windows x64 `dotnet publish` 与 zip 打包验证，确认发布目录包含主程序与 `Updater/MangaReader.Updater.exe`。

### 2026-06-17 原生软件 0.5.9.8

**主题：书库扫描与详情目录稳定性修复**

- 修复书库重扫取消边界：为 `ScanRootsAsync()` 增加扫描运行身份校验，旧扫描在取消后不会再覆盖新扫描的书库列表、状态文本或加载进度浮层。
- 优化 `LibraryScanner.Scan()` 的目录遍历策略，从预先枚举整棵目录改为边发现边扫描；枚举子目录和图片时均检查取消令牌，进度反馈与取消响应更及时。
- 降低详情目录性能风险：打开目录时只加载首批缩略图，后续通过 hover 懒加载，避免长篇漫画一次性解码整本书所有页面缩略图。
- 修复详情目录预览竞态：预览加载增加请求序号和取消检查，快速切换页面时旧预览不会覆盖当前预览。
- 修复设为封面的异步生命周期问题：目录记录所属书籍，保存封面后会校验当前详情仍是同一本书，避免切换详情后被旧任务回写污染。
- 主程序版本号更新为 `0.5.9.8`。
- 已通过本地 Windows x64 `dotnet publish` 与 zip 打包验证，确认发布目录包含主程序与 `Updater/MangaReader.Updater.exe`。

### 2026-06-17 原生软件 0.5.9.9

**主题：隐私模式与更新自动重启**

- 新增全局隐私模式：左侧工具区新增“隐私模式：开/关”按钮，开启后书库卡片、主页继续阅读 / 最近阅读卡片、详情页大封面统一被遮挡，不显示真实封面。
- 隐私模式状态持久化：新增 `app_settings` 表存储 `library.privacy_mode`，重启后保持开关状态；遮挡层只负责视觉隐藏，不破坏封面缓存，关闭后立即恢复。
- 修复点击更新后无法自动重启的问题：`UpdateService` 启动 Updater 前先复制到临时目录运行，避免运行中的更新器锁住安装目录内的 `Updater/MangaReader.Updater.exe`。
- 增强 `MangaReader.Updater` 自动重启：复制完成后使用绝对路径启动主程序并增加多次重试；启动失败时写入 `%TEMP%\MangaReader_Updater_Error.log`，便于排查。
- 主程序版本号更新为 `0.5.9.9`。
- 已通过本地 Windows x64 `dotnet publish` 与 zip 打包验证，确认发布目录包含主程序与 `Updater/MangaReader.Updater.exe`。

### 2026-06-17 原生软件 0.5.9.10

**主题：隐私模式启动崩溃热修**

- 修复 `0.5.9.9` 启动即崩溃的问题：隐私模式遮罩颜色误写为非法 Brush `#F111827`，WPF 在启动解析 XAML/BAML 时抛出 `FormatException: 令牌无效`。
- 将遮罩颜色修正为合法 8 位 ARGB `#F1111827`，保留近黑半透明遮罩效果。
- 主程序版本号更新为 `0.5.9.10`。
- 已通过本地 Windows x64 `dotnet publish` 与 zip 打包验证，确认发布目录包含主程序与 `Updater/MangaReader.Updater.exe`。

### 2026-06-17 原生软件 0.5.9.11

**主题：详情页排版与单书隐私封面**

- 优化书籍详情页只读布局：作者、页数和 Tag 胶囊统一中线对齐，阅读状态、简介、作品信息增加统一缩进，避免贴左造成的视觉拥挤。
- 清理编辑作品信息界面：移除可见的阅读状态、阅读次数、封面页字段；保存编辑信息时不再写入这些阅读/封面状态，避免编辑页继续干预阅读进度和封面页。
- 新增单书隐私封面：危险操作中增加“保持隐私封面 / 取消隐私封面”，新增 `is_privacy_cover` 数据库字段；即使全局隐私模式关闭，该书也会保持封面遮挡。
- 调整封面遮罩逻辑：书库卡片、主页卡片、详情页封面统一响应全局隐私模式或单书隐私封面。
- 保留 Tag 新建/编辑弹窗的分组颜色手动选择逻辑，避免切换分组时强制覆盖用户已选颜色。
- 主程序版本号更新为 `0.5.9.11`。
- 已通过本地 Windows x64 `dotnet publish` 与 zip 打包验证，确认发布目录包含主程序与 `Updater/MangaReader.Updater.exe`。

### 2026-06-17 原生软件 0.5.9.12

**主题：书库排序、批量 Tag 与详情目录布局修复**

- 书库排序新增 `升序 / 降序` 独立切换按钮；标题默认升序，进度、页数、容量、阅读次数、录入时间、出品时间默认降序，用户可手动反向排序。
- 修复多选模式下 `+ Tag` 只能新建 Tag 的问题：批量加 Tag 会优先解析已有 Tag，空输入时提供已有 Tag 候选，只有输入不存在的 Tag 时才进入创建流程。
- 修复瀑布流和 Tag 管理页筛选误套用“互斥 Tag 清同组”规则的问题；筛选态允许多个同组 Tag 同时保留，不再清空已经选中的筛选 Tag。
- 调整详情页目录浮层布局：整体宽度扩大，左侧目录列表固定为四列缩略图宽度，右侧预览页占用剩余空间，减少列表与预览之间的浪费间距。
- 主程序版本号更新为 `0.5.9.12`。
- 已通过本地 Windows x64 `dotnet publish` 与 zip 打包验证，确认发布目录包含主程序与 `Updater/MangaReader.Updater.exe`。

### 2026-06-17 原生软件 0.5.9.13

**主题：详情编辑态压缩与目录预览交互修正**

- 压缩书籍详情编辑态布局：移除编辑表单强制最小高度，Tag 编辑区改为一行式横向结构，左侧整合搜索和已选 Tag，右侧显示可添加 Tag。
- 调整编辑态底部操作区：危险操作与 `取消 / 保存信息` 同行排列，减少独立按钮行造成的高度跳变，使编辑态与详情态尺寸更接近。
- 降低编辑态简介输入框高度，进一步减少编辑模式相对详情模式的额外垂直占用。
- 优化详情目录预览：标题改为 `目录预览 · 作品标题`，删除标题下方说明小字，减少顶部噪声。
- 详情目录预览支持点击遮罩空白处返回上一级，内容面板内部点击不会误关闭。
- 主程序版本号更新为 `0.5.9.13`。
- 已通过本地 Windows x64 `dotnet publish` 与 zip 打包验证，确认发布目录包含主程序与 `Updater/MangaReader.Updater.exe`。

## 19. 未来迭代方向（产品视角）

写在 0.5.9.18 这个节点。本节是从产品层面给的一个长期展望，不预设时间表，**用户随时可调整**。技术细节与优先级见 `AI_TECHNICAL_HANDOFF.md §14`。

### 项目当前的定位很清晰，需要捍卫

- **本地 + 离线 + 文件夹优先**：这条主线让项目避开了 80% 同类项目陷入的"协议/格式/同步泥潭"，未来任何"为什么不支持 zip"、"为什么不连 Anilist" 的诱惑都要顶住。
- **管理优先 + 阅读舒适**：评分系统 0.5.9.14 落地后，"管理"这条线的工具集已经很完整（作者 / 标签 / 评分 / 收藏 / 状态 / 隐私 / 备份）。后续的产品功夫应该更多花在"让这些工具的协同更顺滑"，而不是堆更多孤立按钮。
- **桌面 Windows 单点**：不做 Web、不做移动、不做跨平台 —— 这是当前阶段最高效的资源分配。

### 我看到的最大产品机会

**1. 把"评分"做成决策中心**

评分系统目前只是"标了个数"，但它真正的价值是**帮用户从几百几千本里挑出今天想读哪一本**。下一步的串联：
- 首页"今天读什么"卡片：从 ≥4 分但未读 / 在读未完结里挑 3 本
- "评分 + 标签 + 阅读状态" 三维组合筛选
- 按月看自己的评分分布，发现"评分门槛是不是变高了"

**2. 把"备份"做成可见的安全感**

当前备份是后台静默的 —— 用户其实没感受到"它在保护我"。可以：
- 侧边栏显示"上次备份：2 小时前 · 共 38 个备份点"
- 备份恢复界面可视化时间轴
- 升级前主动备份（见 AI handoff §14 P0 第 2 条）

**3. 把"阅读器"做成沉浸的延伸**

0.5.9.14 修了翻页/中键/全屏三个 bug 后，阅读器基础已经稳。下一步：
- 阅读统计（"今晚读了 3 本 · 共 412 页"）
- 章节书签（一本书内多个名场面记忆）
- 单本阅读偏好覆盖（这本默认双页，那本默认滚动）

### 需要警惕的陷阱

- **功能堆叠**：用户偏好极简紧凑，新功能必须**替换或合并**旧入口，不要永远只加不减。
- **过度智能化**：不要为了"AI 自动打 tag" / "智能推荐"等花哨功能，破坏离线本地的核心承诺。
- **跨平台诱惑**：`MangaReader.Avalonia/` 目录是早期试验，不要看到它就以为该激活——当前阶段 WPF 主线 ROI 最高。
- **重构无目的**：现在的代码结构（`MainWindow.xaml.cs` 5000+ 行）虽大但聚焦，**只在新加大功能时**做局部抽取，不要为了"代码美观"做大重构。

### 我对接手 AI 的一句话嘱托

> 这个项目的灵魂是「为一个把漫画当严肃爱好的人，做一个安静、可靠、不打扰的本地工具」。每加一个功能前，先问自己：**这是让用户更专注于读漫画，还是让用户更专注于使用软件本身？** 答案是前者才做。

---

### 2026-06-18 原生软件 0.5.9.18

**主题：批量多选交互升级（Shift / Ctrl / 框选）+ AI 接手文档对齐 0.5.9.14~17**

数据安全：本轮**零 schema 改动**、不触碰任何 DB 写入路径；多选交互全部围绕内存中 `MangaBook.IsSelectedForBatch` 字段，与持久化无关。

**批量多选交互重构**（仅 `IsBatchSelectionMode=true` 时生效）：

- 进入多选模式后，点击卡片本身（非 CheckBox）也能触发选择动作；之前只有点 CheckBox 才能勾选，且点卡片其他位置会打开详情面板，体验割裂。
- 修饰键支持：
  - **普通点击**：toggle 单本，记录该卡片为 `_batchAnchorBook` 锚点。
  - **Shift+点击**：从 `_batchAnchorBook` 到当前卡片在 `Books` 集合中的索引范围**整段置 true**，等同桌面文件管理器范围选。
  - **Ctrl+点击**：强制移出选择（`IsSelectedForBatch=false`），更新锚点。
- 框选（rubber-band）：
  - `BooksList_PreviewMouseLeftButtonDown` 检测起点 `e.OriginalSource` 是否在 `ListBoxItem` / `ButtonBase` / `ScrollBar` 内，否则启动框选。
  - 在 BooksList 同层 `Canvas x:Name="RubberBandLayer" IsHitTestVisible="False"` 内画 `RubberBandRect`（蓝色虚线描边 `#3B82F6` + 半透明填充 `#223B82F6` + 圆角 3）跟随鼠标。
  - 松开时遍历 `BooksList.ItemContainerGenerator`，对已实例化的 `ListBoxItem` 用 `TransformToAncestor` + `Rect.IntersectsWith` 求交，命中者 `IsSelectedForBatch = !_rubberSubtract`。
  - 修饰键叠加：Ctrl+拖 = 减选；Shift+拖 = 不清空既有再加；普通拖 = 清空后选中框内。
  - **已知约束**：受 `VirtualizingWrapPanel` 虚拟化限制，框选**只能命中已实例化的可见项**，框选窗口外滚动区域的卡片不会被选中。这是当前实现的边界，非 bug。
- `BookCard_PreviewMouseLeftButtonDown` 在 batch 模式下设 `e.Handled = true`，阻止 `BooksList.SelectionChanged` 触发详情面板。CheckBox 子元素的 toggle 仍走自身路径（通过 `FindAncestor<CheckBox>` 检测短路）。

**AI 接手文档（`AI_TECHNICAL_HANDOFF.md`）对齐 0.5.9.14~17**：

- 第 3 节"数据模型"补 `rating` 字段说明 + 引入"半托管字段模式"（`is_favorite` / `rating` 故意不进 `UpsertBookSql` / `AddBookParameters`，避免扫描器覆盖）。
- 第 5 节"阅读器交互约束"加中键 = 切 HUD、翻页 `ScrollToVerticalOffset(0)`、全屏切换 `ScheduleFitModeApply()` 三条新规。
- 第 6 节"UI 规范"补瀑布流胶囊金银双态、详情页 Path 5 星倒角、SidebarToggleButton 颜色态、`BatchSelectCheckBox` 22×22 规格。
- 第 7 节"标签、作者与批量管理"加 `TagCatalog.PresetColors` 单一颜色来源、新建 Dialog 锁色规则、批量多选 Shift / Ctrl / 框选交互规约。
- 第 11 节"约束清单"补 10 条新约束（rating 半托管 / 翻页重置滚动 / 全屏适配 / 中键切 HUD / 胶囊单容器 / Path 星形 / 颜色集统一 / 新建 Tag 锁色 / 多选交互 / FilterComboBox 横滚禁用）。

- 版本号 0.5.9.17 → 0.5.9.18。

### 2026-06-18 原生软件 0.5.9.17

**主题：Tag 管理 UX 修复 + 详情编辑 Tag 区瘦身 + 瀑布流多选按钮缩小**

数据安全说明：本轮**零数据库 schema 改动**，未触碰任何 `SaveMetadata` / `SaveBookTagsBatch` 等写入路径，全部是 UI / Dialog / 静态字段重构，老库可直接平滑升级。

- **新建 Tag 空名禁用提交**：`TagCreateDialog.xaml` 创建按钮加 `x:Name="ConfirmButton"` + `IsEnabled="False"`；`TagNameBox` 加 `TextChanged="TagNameBox_TextChanged"`；code-behind `UpdateConfirmEnabled()` 在构造和每次文本变化时联动按钮可用性，杜绝空名提交。
- **选已有 Tag 组时颜色锁定 + 显示该组关联数**：
  - `TagCategoryBox` 加 `SelectionChanged="TagCategoryBox_Changed"` + `LostFocus="TagCategoryBox_LostFocus"`（覆盖编辑态自定义输入回填）。
  - `TagCreateDialog` 构造新增 `IReadOnlyDictionary<string, int>? categoryTagCounts` 参数。
  - `ApplyCategorySelection()`：选中 category 若在已有 `_categoryColors` 里 → 强制 SelectColor 为该组色、`ColorPickerArea.IsHitTestVisible=false`、隐藏自定义色按钮、`CategoryHintText` 提示「已选分组 X 下已有 N 个标签，颜色锁定。如需改色请到左侧标签 → 编辑标签」。
  - 输入新分组名 → 颜色可选 + 提示「将创建新分组 X，颜色可自定义」。
  - 移除「在新建对话框里可改组色」的错误操作面。
- **颜色集统一**：`Services/TagCatalog.cs` 把原 `private CustomColors[]` 改成 `public PresetColors[]`（8 个色）。`TagCreateDialog.xaml.cs` / `TagEditDialog.xaml.cs` 都改为 `private static readonly string[] PresetColors = TagCatalog.PresetColors;` 引用同一份。`TagCatalog.GetColor` 内部 fallback 也用 `PresetColors`——已 managed 的 tag 颜色保存在数据库 `managed_tags.color`，不受影响；只对未 managed 的 tag 的 hash-推断显示色保持一致来源。
- **`MainWindow.xaml.cs` 新增 `BuildTagCategoryCountMap()`**：按 `_managedTags` 的 category 分组计数，传给 `TagCreateDialog`，作为关联数显示数据源。
- **详情编辑 Tag 搜索栏瘦身**：`MainWindow.xaml` 详情编辑模式 Tag 区第一列宽度 `220` → `140`，搜索框收窄，已选 Tag 容器自动获得更多空间。
- **瀑布流批量多选 CheckBox 缩小美化**：`BatchSelectCheckBox` 样式 Width/Height `30` → `22`、CornerRadius `RadiusControl` → `6`、`BorderThickness` `1.2` → `1`、勾路径从 `M 5 10 L 8 13 L 15 6` 缩为 `M 4 8 L 7 11 L 12 5`、选中态背景从 `#111827` 改为 `#1F2937`（与 SidebarToggleButton 深态一致）、边框默认色由 `#94A3B8` 改为 `#CBD5E1` 更柔。视觉权重下降一档，与右上评分/收藏胶囊更均衡。
- 版本号 0.5.9.16 → 0.5.9.17。

### 2026-06-18 原生软件 0.5.9.16

**主题：瀑布流胶囊去重复 ★，金银双态切收藏**

- 之前合并容器内是「`4.5★ │ ★`」两个星视觉重复。删除竖线分隔与右侧独立收藏 ★，胶囊内容简化为「`RatingText` + ★」单段。
- 收藏与否改由**容器整体配色**表达：
  - `IsFavorite=True` → 亮金容器（`#FFEEAA / #E4B95F` + 文字 `#B45309`）
  - `IsFavorite=False` → 银灰容器（`#F3F4F6 / #D1D5DB` + 文字 `#6B7280` / 星 `#9CA3AF`）
- 4 个状态组合后：
  - 已评 + 已收藏：亮金「`4.5★`」
  - 已评 + 未收藏：银灰「`4.5★`」
  - 未评 + 已收藏：亮金「`★`」单星
  - 未评 + 未收藏：容器隐藏
- ★ 字符 `Margin` 由 DataTrigger 自适应：`HasRating=True` 时 `3,0,0,0` 留出与 `RatingText` 间距；False 时 `0`。
- 版本号 0.5.9.15 → 0.5.9.16。

### 2026-06-18 原生软件 0.5.9.15

**主题：评分胶囊合一容器 + 详情页星形 Path 化（去重影 + 倒角）+ 未评不再显示**

- 瀑布流胶囊合并为单 Border 容器：原来「评分胶囊 + 收藏胶囊」两个独立圆角矩形并排，视觉割裂。改为单一暖金色 Border（`#FFEEAA / #E4B95F`）内部 StackPanel Horizontal：评分段（`RatingText` + 小金 ★）/ 1px 竖线分隔（`#E4B95F`）/ 大金 ★ 收藏。
  - 显示逻辑：`HasRating=False && IsFavorite=False` → 容器 Collapsed（"未评不显示分数" + 未收藏直接隐藏）。
  - 评分段：仅 `HasRating=True` 时显示。
  - 分隔线：仅 `HasRating=True && IsFavorite=True` 时显示（MultiDataTrigger）。
  - 收藏 ★：仅 `IsFavorite=True` 时显示。
  - 删除 `MangaBook.RatingCapsuleText` 派生属性（不再使用「未评」文案）。
- 详情页 5 颗星编辑器从 TextBlock `★` 双层叠加改为 `System.Windows.Shapes.Path` + `Geometry.Parse` 标准 5 角星矢量：
  - 解决「双层 ★ 抗锯齿错位导致的金灰重影」（截图证实），单 Path 单色填充无错位。
  - 加 `StrokeLineJoin=PenLineJoin.Round` + 同色 Stroke 1.5px 实现「倒角圆润」星尖，去除 TextBlock 字符渲染的锐角。
  - 半 / 全显示仍由 Border 套 ClipToBounds + Width 控制；命中区域两个透明 Rectangle 保持不变。
  - 新增 `BuildStarPath(size, brush, alignment, fillToWidth)` 复用方法。
- 版本号 0.5.9.14 → 0.5.9.15。
- push tag v0.5.9.15 触发 GitHub Actions 打包 win-x64 zip 并上传 Release。

### 2026-06-18 原生软件 0.5.9.14

**主题：作者下拉 UI 修正 + 阅读器 3 个体验 bug 修复 + 评分系统并入打包**

- 作者筛选下拉（`AuthorFilterBox`）单独加 `ItemTemplate`，长作者名 `TextTrimming="CharacterEllipsis"` 截断，`ToolTip` 悬停显示完整名。同时全局 `FilterComboBox` 样式的下拉 `ScrollViewer` 加 `HorizontalScrollBarVisibility="Disabled"`，杜绝水平滚动条破坏圆角矩形美感。
- 阅读器修复**翻页时垂直滚动位置跨页保留**的 bug：滚动模式下，从第 N 页底部翻页本应跳到第 N+1 页顶部，原本却保留 `VerticalOffset` 直接到下一页底部。修复方式：在 `LoadPageCore` 的 `ApplyDoublePageGap()` 之后统一 `ReaderScrollViewer.ScrollToVerticalOffset(0)`。所有翻页路径（左键点击 / 滚轮 / 键盘 / 目录跳页）都汇聚到此入口。
- 阅读器**中键行为简化**：原来按一下循环切「窗口 / 全屏+HUD / 全屏+无 HUD」三态，过重。改为只 `SetControlsHidden(!_controlsHidden)` 切 HUD 显示/隐藏。`CyclePresentationMode()` 函数整段删除，全屏入口仍由 `W` 快捷键（既有）触发。
- 阅读器**全屏切换后自动重新适配**：在 `EnterFullscreen` 和 `ExitFullscreen` 末尾各调用 `ScheduleFitModeApply()`。修复窗口模式下按 `E`（适高）/ `Q`（适宽）、切到全屏不自动按新视口重算的问题。`ScheduleFitModeApply` 有 80ms 防抖，足够 `WindowState` 切换完毕、视口尺寸更新到位。

### 2026-06-18 原生软件 评分系统 + 详情页标题改造 + UI 修正

**主题：评分系统接入、瀑布流与详情页 UI 紧凑化、隐私 / 日志按钮颜色态统一**

- **数据层**：`books` 表新增 `rating REAL NOT NULL DEFAULT 0`，沿用 `is_favorite` 处理路径：`CREATE TABLE` / `EnsureColumn` / `LoadBooksByPath` SELECT 与 reader（索引 21）/ `SaveMetadata` UPDATE + 参数。`UpsertBookSql` 与 `AddBookParameters` 不写 `rating`，避免扫描器覆盖用户评分。
- **模型**：`MangaBook` 新增 `Rating`（double，clamp 0~5 + 0.5 量化）、`HasRating`、`RatingText`、`RatingCapsuleText`（"4.5★" / "未评"）。
- **瀑布流胶囊**（`MainWindow.xaml` 原 767-786）：原单个收藏 ★ Border 改为右上角 `StackPanel`，含「评分胶囊 + 收藏胶囊」两枚并排。HasRating 控制评分胶囊亮（`#FFEEAA / #B45309`）/ 暗（`#F3F4F6 / #9CA3AF` "未评"）；IsFavorite 同理控制收藏胶囊亮 / 暗。两胶囊保持原高度，无额外行高占用。点击卡片任意位置（含胶囊）触发详情面板，事件传给外层卡片。
- **详情页标题改造**：
  - `BookTitleText` 用 `DockPanel` 包裹，右侧 `RatingStarsHost`（5 颗 Grid，每颗带 `★/☆` 双层叠加 + 左右半透明 Rectangle 点击区）+ `ToggleFavoriteButton`（从顶栏移过来）。
  - 标题加 `TextTrimming="CharacterEllipsis"` + `ToolTipService.ShowDuration="20000"` + `Cursor="Hand"` + `MouseLeftButtonUp="CopyBookTitle_Click"`。
  - 顶栏 `DetailTopBar` 原收藏按钮删除。
- **评分编辑**：`MainWindow.xaml.cs` 新增 `BuildRatingStars()` / `CreateRatingStar()` / `RatingStar_Click()` / `CopyBookTitle_Click()`。`RatingStarsHost` 在 `FillMetadataEditors()` 末尾重建。点击左半 → `N - 0.5`，右半 → `N`；再次点击当前评分位置 → 清零。保存走 `await Task.Run(() => _database.SaveMetadata(book))`，沿用元数据备份与节流。
- **排序**：`SortBox` 第 2 项 `排序：进度` 改为 `排序：评分`，`SortBooks()` switch case 1 改为按 `book.Rating` 排序；SelectedIndex 数值不变，向后兼容。
- **BatchStyleBox**：宽度 `96` → `120`，避免「样式 N」关闭态被截。
- **侧边栏 Toggle 按钮**：新增 `SidebarToggleButton` 样式（基于 `SidebarActionButton` + `Tag="active"` Trigger 切换 `#1F2937 / #FFFFFF`）。`PrivacyModeButton` Content 固定为「隐私模式」，去掉「开 / 关」尾缀，状态由 `Tag` 表达；`LogPanelToggleButton` Content 固定为「展开日志」，同样由 `Tag` 表达，`UpdateLogPanelVisibility()` 不再切换文字。两按钮启动时由各自的 `Load*` / `Update*` 初始化 Tag。
- **静态画刷**：评分星 `#D1D5DB / #F59E0B` 缓存为 `static readonly SolidColorBrush` + `Freeze()`，瀑布流胶囊全部用 XAML 字面色，编译期常量化。
- **歧义处理**：因 `using WinForms = System.Windows.Forms`，`Brush / Color / ColorConverter / HorizontalAlignment` 与 `System.Drawing` 同名，全部 fully qualified `System.Windows.Media.*` / `System.Windows.HorizontalAlignment.*`。
- 已通过 `dotnet build`，0 警告 0 错误。

### 2026-06-19 原生软件 0.6.0

**主题：瀑布流 Tag 排序入口 + README 重构 + 版本号迈入 0.6**

- **瀑布流 Tag 排序**：`MainWindow.xaml` 的 `LibraryTagPanel` 顶部 DockPanel 在右侧新增 `WaterfallTagSortBox`（110×32，`FilterComboBox` 样式），4 项：`默认` / `按时间` / `按颜色` / `按数量`。`ActiveTagSummaryText` 同时改为 Dock=Left 与「显示隐藏作品」并排，避免与新排序框争抢右侧位置。
- **排序逻辑**：`RefreshVisibleTags()` 内根据 `WaterfallTagSortBox.SelectedIndex` 分支：
  - `0 默认`：保持原行为，`TagCategoryOrder → Name`。
  - `1 按时间`：先按 `UpdatedAt` 是否非空（避免书籍标签的空时间顶到前面），再按 `UpdatedAt` 字符串降序，最后按名称。`UpdatedAt` 来自 `_managedTagUpdatedAt`（managed tag 编辑时写入），未管理的扫描标签为空字符串，统一沉底。
  - `2 按颜色`：按 `tag.Color` 十六进制升序分组，组内再按 `TagCategoryOrder → Name`。同色聚集，便于视觉上分块挑选。
  - `3 按数量`：按 `UsageCount` 降序 + 名称，高频标签置顶。
- 新增 `WaterfallTagSort_Changed` handler，仅调用 `RefreshVisibleTags()`。
- **README 重构**：旧 README 还停留在 0.3.xx 心智，重写为「这是什么 + 核心特性 + 快速开始 + 数据安全 + 发布 + 项目结构 + 不做的方向」结构，与 `AI_TECHNICAL_HANDOFF.md` §14「未来迭代方向」对齐。
- **版本**：`0.5.9.18` → `0.6.0.0`。从 0.5.9.x 的小版本叠加切换到 0.6 主线，标志评分系统 / 批量交互 / Tag UX 三大支线收敛完成。
- 已通过 `dotnet build`，0 警告 0 错误。

### 待解决问题：阅读器推荐面板"打开书本详情页"的导航断裂

**背景**：用户希望在阅读到最后一页的推荐面板中新增「打开书本详情页」按钮。

**核心矛盾**：当前 `OpenBook()` 方法（`MainWindow.xaml.cs:4397`）**不切换** `BooksList.SelectedItem`。从推荐跳转到新书后，书库的选中项仍停留在跳转前的书。

**复现场景**：
1. 在书库中选中漫画 A-01，双击打开阅读器
2. 阅读到最后一页，推荐面板弹出
3. 点击推荐的漫画 B-12，`OpenBookFromRecommendation()` 触发
4. 旧 ReaderWindow 关闭 → `OpenBook(B-12)` 打开新 ReaderWindow
5. 此时 `BooksList.SelectedItem` 仍为 A-01，`_currentBook` 仍为 A-01
6. 若直接调用 `SetDetailVisible(true)` → 显示的是 A-01 的详情，不是 B-12

**需要解决的前置问题**：
- `OpenBook()` 需要在打开阅读器前，先将目标书设为 `BooksList.SelectedItem`（触发 `BooksList_SelectionChanged` → `SetDetailVisible(true)` → `FillMetadataEditors`）
- 或者引入独立的「打开指定书的详情」方法，绕过 `BooksList.SelectedItem`

**待定交互方案**（二选一）：
1. **关闭阅读器 → 跳回书库**：按钮触发关闭阅读器 + `OpenBook(nextBook)` 切换选中项 + 打开详情面板。用户关闭详情后回到书库，B-12 选中态。
2. **阅读器内弹出详情覆盖层**：在 ReaderWindow 内部弹出一个详情面板覆盖层，不关闭阅读器。关闭覆盖层后回到阅读器最后一页。

**涉及文件**：`MainWindow.xaml.cs`（`OpenBook`、`SetDetailVisible`、`BooksList_SelectionChanged`）、`ReaderWindow.xaml`（推荐面板 XAML）、`ReaderWindow.xaml.cs`（按钮 handler）

### 2026-06-21 原生软件 0.7.3.1

**主题：目录间距回归 + CheckBox 不换色**

#### 目录间距恢复正常

阅读器目录（`PageCatalogList`）和详情目录（`DetailCatalogList`）在引入 `VirtualizingWrapPanel` + `ScrollViewer.CanContentScroll="True"` 后，尽管 ItemWidth/ItemHeight/Spacing 数学上与原始 `WrapPanel` + `Margin` 等价，但实际运行后用户反馈间距过宽。多轮修复均因未重新编译打包而导致运行旧二进制、无法验证效果。

**修复方式**：将两个目录的 `ItemsPanelTemplate` 从 `<controls:VirtualizingWrapPanel ItemWidth="142" ItemHeight="210" HorizontalSpacing="14" VerticalSpacing="14" IsVirtualizing="True" VirtualizationMode="Recycling" CacheLength="2"/>` 恢复为 `<WrapPanel/>`，移除 `ScrollViewer.CanContentScroll="True"`，Border 加回原始 `Margin="7"`（阅读器）/ `Margin="6"`（详情）。`controls` 命名空间保留不动（主书架 `BooksList` 仍在使用）。

| 文件 | 位置 | Panel 修复 | Border Margin |
|------|------|-----------|:---:|
| `ReaderWindow.xaml` | `PageCatalogList` | `VirtualizingWrapPanel` → `<WrapPanel/>` | `Margin="7"` |
| `MainWindow.xaml` | `DetailCatalogList` | `VirtualizingWrapPanel` → `<WrapPanel/>` | `Margin="6"` |

#### 筛选 CheckBox 不再换色

`MainWindow.xaml` 中全局 `CheckBox` 样式的 `IsChecked` 触发器将 `CheckRoot` 的 `Background`、`BorderBrush`、`Foreground` 全部替换为 `Brush.TextPrimary`，导致"只看收藏"和"只看隐藏作品"两个筛选 CheckBox 在勾选时整条按钮变色。

**修复方式**：`IsChecked` 触发器中移除 `CheckRoot` 的背景/边框/前景色变更，仅将小勾选框 `CheckMark` 的 `Background`/`BorderBrush` 设为 `Brush.TextPrimary` 并显示 `CheckGlyph`。勾选行为变为仅在小勾选框内显示勾号，不再整条换色。

**涉及文件**：
- `MangaReader.Native/MainWindow.xaml` — 详情目录 Panel + CheckBox `IsChecked` 触发器
- `MangaReader.Native/ReaderWindow.xaml` — 阅读器目录 Panel
- `MangaReader.Native/MangaReader.Native.csproj` — 版本号 `0.7.3.1`

- 已通过 `dotnet build`，0 警告 0 错误，已打包 `_release\MangaReader.Native.exe`。

### 2026-06-21 原生软件 0.7.3.2

**主题：主页评分徽章显示修复**

#### 主页评分徽章完整显示

"之前读过""收藏""最近加入"三个主页区域的评分徽章（`RatingBadgeTemplate`）此前因 `HomeCompactBookTemplate` 中徽章 `HorizontalAlignment="Left"` + `Margin="42,0,0,0"` 导致徽章与封面图（68×84）严重重叠（重叠约 26px），视觉上被封面渐变背景遮挡，无法完整显示评分。

**修复方式**：将徽章改为 `HorizontalAlignment="Right"` + `Margin="0,0,10,0"`，贴在封面图右上角，不重叠封面主体。

**涉及文件**：
- `MangaReader.Native/MainWindow.xaml` — `HomeCompactBookTemplate` 徽章 Border 定位

**涉及区域**：`RecentReadingBooks`（之前读过）、`FavoriteShowcaseBooks`（收藏）、`RecentlyAddedBooks`（最近加入）均使用 `HomeCompactBookTemplate`。

### 2026-06-21 原生软件 0.7.3.3

**主题：评分徽章布局优化 + 复制反馈修复**

#### 评分徽章移至卡片容器右上角

主页评分徽章从封面图内部移至整个卡片横向容器的右上角（`Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,0,10,0"`），标题右侧留 56px 给徽章，标题加 `TextTrimming="CharacterEllipsis"` 截断。

#### 详情页复制标题反馈修复

- 添加 Toast 提示"已复制标题：xxx"
- `SafeSetClipboard` 改为 `Dispatcher.BeginInvoke` 异步执行，修复 UI 卡顿
- 重试异常全部吞掉，不再冒泡，修复闪退和"复制失败"误报

**涉及文件**：
- `MangaReader.Native/MainWindow.xaml` — 徽章定位 + 标题截断
- `MangaReader.Native/MainWindow.xaml.cs` — `CopyBookTitle_Click` + `SafeSetClipboard`
- `MangaReader.Native/MangaReader.Native.csproj` — 版本号 `0.7.3.3`

### 2026-06-21 原生软件 0.7.4.0

**主题：性能优化**

#### P0-1 添加 last_opened_at 索引

`LibraryDatabase.Initialize` 新增 `CREATE INDEX IF NOT EXISTS idx_books_last_opened_at ON books(last_opened_at);`，首页"继续阅读"按 `LastOpenedAt` 排序不再全表扫描。

#### P0-2 LoadSetting 内存缓存 + LoadLibraryRoots 异步化

- `LibraryDatabase` 新增 `_settingsCache` 字典，`LoadSetting` 先查缓存再查 DB，`SaveSetting` 写 DB 后同步更新缓存。避免右键菜单等高频场景反复查库。
- 启动流程 `LoadLibraryRoots()` 改为 `await Task.Run()`，不再阻塞 UI 线程。

#### P0-3 RebuildTagIndex 脏标记优化

新增 `_tagIndexDirty` 标志，`RebuildTagIndex` 仅在脏时执行全量重建。所有修改 `_allBooks` 或书籍 `Tags` 的位置调用 `MarkTagIndexDirty()` 标记脏数据。`RefreshLibraryViews` 默认 `tags=true` 不再无条件重建。

#### P1-2 GetCachePath 缓存封面文件时间戳

`CoverCache` 新增 `_coverTimestampCache` 字典，`GetCachePath` 首次访问 `File.GetLastWriteTimeUtc` 后缓存结果，后续直接从内存读取。

#### P2-2 封面并发限制提高

`CoverThumbnailPipeline._loaderGate` 从 `SemaphoreSlim(2)` 改为 `SemaphoreSlim(4)`，封面加载并发翻倍。

#### P2-3 Tag 过滤 HashSet 优化

标签过滤从 `book.TagItems.Any()` 线性扫描 O(N×M) 改为先构建 `HashSet<string>` 再 `Contains` O(N+M)。

#### P2-4 RefreshHomeShelves 单次遍历

4 个独立 LINQ 查询合并为单次 `foreach` 分类填充，减少中间列表和重复遍历。

#### P3-4 EnumerateKnownTags 直接用 _tagBooksByName.Keys

不再 `_allBooks.SelectMany(book => book.TagItems...)` 全量扫描，直接使用已维护的 `_tagBooksByName.Keys`。

**涉及文件**：
- `MangaReader.Native/Services/LibraryDatabase.cs` — 索引 + 缓存
- `MangaReader.Native/Services/CoverCache.cs` — 时间戳缓存
- `MangaReader.Native/Services/CoverThumbnailPipeline.cs` — 并发限制
- `MangaReader.Native/MainWindow.xaml.cs` — 脏标记 + HashSet + 单次遍历 + EnumerateKnownTags
- `MangaReader.Native/MangaReader.Native.csproj` — 版本号 `0.7.4.0`
