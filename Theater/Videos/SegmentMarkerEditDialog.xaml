<Window x:Class="Theater.Videos.SegmentMarkerEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑分段"
        Width="500"
        Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Grid Margin="22">
        <Border Style="{StaticResource AppDialogShadowBorder}"/>
        <Border CornerRadius="{StaticResource RadiusDialog}"
                Background="{DynamicResource Brush.Surface}"
                BorderBrush="{DynamicResource Brush.BorderSubtle}"
                BorderThickness="1"
                Padding="26"
                SnapsToDevicePixels="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0">
                    <Button DockPanel.Dock="Right" Content="×" Click="Cancel_Click" Style="{StaticResource AppDialogCloseButton}"/>
                    <StackPanel>
                        <TextBlock Text="编辑分段" Style="{StaticResource AppDialogTitleText}"/>
                        <TextBlock x:Name="TimeText" Margin="0,6,0,0" Style="{StaticResource AppDialogSubtitleText}"/>
                    </StackPanel>
                </DockPanel>

                <ScrollViewer Grid.Row="1"
                              Margin="0,22,0,18"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <TextBlock Text="标题" Style="{StaticResource AppDialogLabelText}"/>
                        <TextBox x:Name="TitleBox" Margin="0,8,0,16" Style="{StaticResource AppDialogTextBox}"/>

                        <TextBlock Text="备注" Style="{StaticResource AppDialogLabelText}"/>
                        <TextBox x:Name="NoteBox"
                                 Margin="0,8,0,16"
                                 Height="100"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 Style="{StaticResource AppDialogTextBox}"/>

                        <TextBlock Text="颜色" Style="{StaticResource AppDialogLabelText}"/>
                        <WrapPanel x:Name="ColorPanel" Margin="0,10,0,0"/>
                    </StackPanel>
                </ScrollViewer>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="取消" Width="104" Margin="0,0,10,0" Style="{StaticResource AppDialogGhostButton}" Click="Cancel_Click"/>
                    <Button Content="保存" Width="104" Style="{StaticResource AppDialogButton}" Click="Confirm_Click" IsDefault="True"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
